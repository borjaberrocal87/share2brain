// @hivly/bot — Discord ingestion process (AD-1: standalone Node process).
// Gateway connection + messageCreate listener land in Epic 3; scaffold stub.
import { PACKAGE_NAME } from '@hivly/shared';

console.log(`[bot] starting — depends on shared kernel ${PACKAGE_NAME}`);
