# generated: 2026-06-30
# last_updated: 2026-07-15 (story 2-6 backlog → ready-for-dev, bmad-create-story): login demo
# Discord invite link (SCP 2026-07-15); epic-2 re-opened to in-progress to append the story.
# || (prior) last_updated: 2026-07-12 (story 10-2 review → done, bmad-code-review): 3 adversarial layers
# (Blind/Edge/Auditor). Auditor confirmed all 7 ACs + D1–D10 SATISFIED, zero violations. Blind/Edge
# raised 6 robustness items → triaged: 2 decisions (D1 pre-render gate kept as ratified; D5 search
# plural resolved to ADD forms), 3 patches applied, 1 deferred (parity-test placeholder hardening),
# 3 dismissed (index.html lang="es" is AC3-mandated; useSuspense:false is a spec anti-pattern;
# non-reactive error notes are informational — no in-app switcher). Patches: search.resultsCount
# _one/_other in both locales (es byte-identical, en fixes "1 result"); ChatWidget errorNote ?? → ||
# (empty frame.message no longer blanks the bubble); main.tsx bootstrap try/catch (changeLanguage
# rejection mounts in default lang, no white screen). Gate re-run green: lint 0 / 1051 unit+web /
# build clean (5 pkgs) / 28 e2e passed (0 spec changes, AC6 re-confirmed post-patch). Epic 10
# COMPLETE (10-1 + 10-2 done). Next: PR + bmad-checkpoint-preview.
# || (prior) last_updated: 2026-07-11 (story 2-5 backlog → ready-for-dev, bmad-create-story): guest access for
# demos, config-gated. Story file 2-5-acceso-de-invitado-para-demos.md. GET+POST /api/auth/guest
# (404 GUEST_ACCESS_DISABLED when off; GET = availability probe D1 — no unauth config endpoint
# exists and AC1 requires the login link HIDDEN when disabled), shared guest_access optional config
# block (NO .default(), streams precedent D4; backend resolveGuestAccessConfig resolves defaults),
# seedGuestUser upsert on sentinel discord_id="guest" w/ fixed v4-shaped GUEST_USER_ID but ALWAYS
# use the returned id (D5), session isGuest via SessionData augment + per-session TTL via
# req.session.cookie.maxAge (connect-redis v9 cookie.expires beats store ttl, verified — D7),
# AppOptions.guestAccess presence=enabled (main.ts passes it ONLY in the enabled branch; tests/e2e
# default disabled), guest button/divider design mock-verbatim ("o para la demo", D2), "Modo
# invitado" pill + "Salir" have NO mock reference → live-pulse-pill mirror ratified (D3), e2e
# auth-guest.spec.ts sorts SECOND (analytics<auth-guest<chat) and must be non-mutating (D8), guest
# chat proven via POST /api/chat w/ fake model (D9), 404 error message AC-verbatim "Not found"
# (D10). Historia 2.5 inserted into epics.md (pending SCP backlog action). No new FR (extends
# FR9/FR22). Fresh-context validation: 3 critical (spec-order claim was wrong — auth-guest sorts
# 2nd not 1st; main.ts unconditional-pass ambiguity; seed-reset guidance violated the never-widen-
# e2e-deletes invariant) + 8 enhancements + 3 optimizations, ALL applied. Branch feat/2-5-guest-
# access off e9dba76. Next: bmad-dev-story 2-5.
# || (prior) last_updated: 2026-07-11 (epic-2 done → in-progress, bmad-correct-course): guest-access story 2-5
# added → backlog (sprint-change-proposal-2026-07-11-guest-access.md). Trigger: demo needs a login
# guest entry without Discord. Design (Borja-approved): synthetic `guest` role scoped to demo channels
# via channel_permissions (AD-12 intact), full access incl. RAG chat via a seeded guest `users` row
# (discord_id="guest") for the conversations FK, session gains isGuest (AD-10, still no sessions table),
# gated by access_control.guest_access.enabled: false default (POST /api/auth/guest → 404 when off →
# honors "never auth-bypass in production"). Scope Moderate, MVP unaffected, no existing-story re-work.
# Next: bmad-create-story 2-5, then bmad-dev-story. || (prior) last_updated: 2026-07-10 (epic-9-retrospective optional → done, bmad-retrospective): Epic 9 (Stats)
# closed. 5/5 stories done, all PRs merged (#54/#56/#57/#58/#59), 0 prod incidents, 0 AC violations,
# AD-12 held end-to-end. Retro doc: epic-9-retro-2026-07-10.md. 5 action items appended (AI-1..AI-5):
# AI-1 Blind-Hunter down-weight (3rd recurrence, open), AI-2 formalize post-roadmap addition-mode,
# AI-3 real deploy+observability (CRITICAL — never deployed to prod), AI-4 promote P2 correctness
# items to stories, AI-5 load-sensitive integration intermittency. Significant reorientation: no
# Epic 10 feature — work shifts from features-via-correct-course to operational hardening;
# operational-backlog.md becomes the active plan. Borja's direction: "endurecer para producción real".
# || (prior) last_updated: 2026-07-10 (story 9-2 in-progress → review, bmad-dev-story): StatsView + 3rd nav
# entry implemented on feat/9-2-web-statsview exactly per plan (D1-D6 honored verbatim). Gate
# green: lint 0 / 892 unit+web (+10) / build clean (5 pkgs) / 16 existing e2e chromium unchanged.
# Integration not run (web-only, no shared/backend touch). Manual smoke confirmed real-data
# rendering in both themes via the e2e harness (see story Debug Log for 2 environment hiccups: a
# stale leftover e2e:server process serving a pre-9.5 response, and an external docker-compose
# full-stack restart mid-verification — neither caused by this session, flagged not acted on).
# New stats visual/CSS assertions deferred BY NAME to 9.3 (7.5->7.6 precedent). Docs synced
# (epics.md UX-DR5/6 + new UX-DR24, TECHNICAL-DESIGN.md §5.5, frontend-standards.md, global.css).
# NOT committed to a PR yet. Next: commit + PR + bmad-code-review 9.2, then 9.3.
# || (prior) last_updated: 2026-07-10 (story 9-2 ready-for-dev → in-progress, bmad-dev-story): starting
# implementation on branch feat/9-2-web-statsview off 10e071f.
# || (prior) last_updated: 2026-07-10 (story 9-2 backlog → ready-for-dev, bmad-create-story): web StatsView +
# 3rd nav entry "Estadísticas" rendering all 5 StatsResponse blocks (kpis/activity/channels/coverage/
# topUsers) — packages/web ONLY, no shared/backend change (contract done, PR #57 merged @ 10e071f).
# Full isStats design extracted into the story (incl. the 4 KPI icon SVG paths + StatsIcon — mock
# never re-parsed downstream). 6 ratified defaults flagged for review: D1 KPI content API-verbatim
# (mock labels/subs = pre-ratification placeholders, "de 6 del guild" rejected in 9.1), D2 de-mensajes
# copy ("Recursos por canal", "N recursos · últimos 14 días"; 7.5-F4 precedent), D3 Top-5 bars KEEP
# blurple #5865F2→#8891F5 (semantically Discord users — NOT the 8.1 leftover case), D4 derived avatars
# (authorId-hash over the mock's 6-color palette + initials rule; snowflake authorName degrades),
# D5 toLocaleString('es'), D6 zero-safety (Math.max(1,divisor) — empty scope would NaN; [] sections
# get inline empty lines). Touches 4 files + 3 new; components.css expected UNTOUCHED (screen has no
# hover states). Testids pre-named for 9.3. Docs sync: UX-DR5/6 (2→3 nav), NEW UX-DR24 (23 taken),
# TD §5.5 + frontend-standards views tables 4→5, global.css raw-hex policy comment. Gate: lint +
# web unit + build + 16 existing e2e stay green; integration NOT run (web-only); new stats visual
# ACs deferred BY NAME to 9-3. Fresh-context validation: 2 critical (UX-DR23 collision; missing KPI
# icon paths) + 3 enhancements applied. Branch feat/9-2-web-statsview off 10e071f. Next:
# bmad-dev-story 9-2 (9-3 parallelizable after).
# || (prior) last_updated: 2026-07-10 (story 9-5 review → done, bmad-code-review): 3 adversarial layers @ Opus
# (Blind Hunter / Edge Case Hunter / Acceptance Auditor). Auditor 0 AC/D/AD violations — AC1–AC6 +
# D1–D7 all verified faithful vs source/schema/seed math (3+2+1+1+1+1=9 reconciles with resources KPI);
# Edge Hunter called the diff clean. Triage: 0 decision-needed, 2 patch (both applied), 3 defer (Low),
# 7 dismissed. Patches: (1) Completion Notes test-delta miscount +12/9+3 → +13/11+2 (headline 882
# correct); (2) reworded the "same COALESCE chain" comment in embeddingSearch/documentRepository to
# state anchor-row vs topUsers latest-across-messages resolution. Defers → deferred-work.md: SQL-vs-JS
# ordering-collation coupling in the topUsers superRefine (unreachable for numeric snowflakes),
# created_at-tie non-determinism in the latest-name array_agg pick (cosmetic; +d.id DESC if revisited),
# tier-3 author_id "never ''" asserted-not-enforced (theoretical, mirrors 9.4 deferred Low). Dismissed =
# LEFT JOIN users fan-out FP (idx_users_discord_id UNIQUE, verified schema.ts:100), integration-rows-not-
# in-diff FP (base seed, math verified), ORDER BY count alias (ratified getChannelCounts convention),
# superRefine <= intentional per D3, OpenAPI can't express ordering, unit-test naming (coverage adequate
# via paired test), deleted-exclusion over whole message_ids (consistent-by-design with authors KPI).
# Re-gate after comment-only SQL patches: lint 0 / build clean (5 pkgs); unit/integration inert (SQL
# semantics byte-identical, PG ignores -- comments). Re-run (2nd pass, extra scrutiny at Borja's
# request): Auditor re-confirmed 0 AC/D/AD violations + both patches truthful; review converged with
# +2 new Low defers (tier-2 u.username='' asymmetry folded into the COALESCE-tiers defer; documents-repo
# /blank-fallback coverage gap beyond AC5 scope, downgraded from Blind's Medium via Edge source-verify)
# + 3 dismissed (redundant D5 empty-scope guard nit, 2 dup defers). Story stays done. UNCOMMITTED status
# flip + 2 review patches on feat/9-5-stats-top-users (5 code commits already landed, PR #57 open).
# Next: bmad-checkpoint-preview or bmad-dev-story (9-2 render, 9-3 e2e).
# || (prior) story 9-5 in-progress → review, bmad-dev-story): topUsers block in
# StatsResponse shipped on feat/9-5-stats-top-users. shared StatsTopUserSchema + required
# topUsers key (count DESC/authorId ASC superRefine) + backend getTopUsers port/Drizzle
# adapter (D1 array_agg…FILTER latest-non-blank-name pick, NULLIF('') hardened, D4 LIMIT 5) +
# statsService wiring (D5 empty-scope gated) + D6 authorName COALESCE stub upgrade in
# embeddingSearchRepository/documentRepository (ragRetriever needed zero changes). Integration
# seed plan executed exactly: 9 new stats.integration.test.ts rows prove all 3 COALESCE tiers +
# latest-name pick + ordering + LIMIT-5 truncation + RBAC/deleted/cross-channel exclusions in
# one exact toEqual; 4 existing absolute asserts updated (resources 3→9, authors 3→6,
# channels[0].count 3→9, coverage totalCount 3→9/readPct 33→11); activity/queries asserts
# verified unchanged. embeddingSearchRepository integration stub flipped to a real seeded name.
# Docs synced (api-spec.yml StatsTopUser + required topUsers, TECHNICAL-DESIGN §11). No DDL, no
# migration, no new dependency, no web/bot/workers change, no e2e run (scoped out — COALESCE
# inert on the e2e seed). Gate green: lint 0 / 882 unit+web (+12) / build clean (5 pkgs) / 128
# integration (+1, 20 files). NOT committed to a PR yet. Next: commit + PR + bmad-code-review.
# last_updated: 2026-07-10 (story 9-5 ready-for-dev → in-progress, bmad-dev-story): starting
# implementation on branch feat/9-5-stats-top-users.
# last_updated: 2026-07-10 (story 9-5 backlog → ready-for-dev, bmad-create-story): topUsers block in
# StatsResponse + RBAC-scoped Top-5 query (shared+backend, NO DDL/migration/index). D1: name pick =
# latest scoped non-blank author_name via (array_agg(NULLIF(name,'')) ORDER BY d.created_at DESC
# FILTER)[1] → u.username → author_id (SCP's bare COALESCE under-specified under GROUP BY; NULLIF
# because the create path has no runtime '' guard — one '' row must not 500 via authorName.min(1)).
# D3: required key, .max(5), count.min(1), order count DESC/authorId ASC pinned via superRefine
# (P3 precedent). D6: search/documents authorName stub upgrade RIDES HERE (9.4 Dev Notes reserved
# it verbatim); ragRetriever/citations inherit via composition; e2e byte-identical (seed degrades
# to author_id). Integration seed plan proves 3 COALESCE tiers + latest-pick + LIMIT-5 truncation
# (6th author) + RBAC/deleted/cross-channel exclusions; new embeddings seeded 20d-ago so only 4
# absolute asserts change (resources 9, authors 6, channel count 9, coverage {1,9,11}); seedMessage
# gains authorName+createdAt params; memberOAuth username must differ from discordId (tier-2
# discrimination). Fresh-context validation: 1 critical (createdAt param) + 2 enhancements applied.
# Branch feat/9-5-stats-top-users off 5e0621b. Next: bmad-dev-story 9-5, then (9-2, 9-3).
# last_updated: 2026-07-10 (story 9-4 backlog → ready-for-dev, bmad-create-story): nullable
# discord_messages.author_name (schema.ts + drizzle 0005) + bot capture. Topology finding: bot
# INSERTs discord_messages directly on create (persistMessage tx) → create-capture = one slice
# change, backfill rides free; edit handler is publish-only → D1: MessageUpdatedEvent gains
# wire-optional authorName + Sync worker persists it (touches workers; flagged). D2 name =
# author.displayName (NOT member nickname — null on REST fetches). MessageCreatedEvent unchanged.
# Fresh-context validation applied 2 critical fixes (compile-only test fixtures; TD §6 ERD sync).
# last_updated: 2026-07-10 (bmad-correct-course, sprint-change-proposal-2026-07-10-topusers.md):
# "Top 5 usuarios" promoted from the 9.1 review (D9) to its own layered pair — Moderate. New stories
# 9-4 (bot+shared: capture discord_messages.author_name, nullable + COALESCE fallback, no backfill)
# and 9-5 (shared+backend: topUsers block in StatsResponse + RBAC-scoped query + integration) →
# backlog; 9-2/9-3 scope extended (render + e2e). Sequence: 9-4→9-5→(9-2,9-3). No new FR (FR24/FR25).
# last_updated: 2026-07-10 (story 9-1 review → DONE + re-run, bmad-code-review): 3 layers @ Opus x2,
# Auditor PASS both rounds (6/6 ACs + D1–D10). 2 decisions: queries KPI → 30d (was all-time); "Top 5
# usuarios" → own story via bmad-correct-course (FOLLOW-UP; data gap: no author display name). Round 1:
# 4 patches (readPct clamp; scoped→emptyScope; superRefine pins KPI order; queries 30d). Re-run found
# 4 refinements of those patches (RP1 30d window single-clock via service; RP2 clamp readCount not just
# readPct; RP3 port doc; RP4 verify 30d cutoff excludes old msg) — all applied. 2 deferred, dismissed.
# Re-gate x2: lint 0 / 863 unit+web / build clean / 127 integration.
# last_updated: 2026-07-10 (story 9-1 in-progress → review, bmad-dev-story): shared StatsResponse
# contract (37 tests) + composite index idx_embeddings_channel_created (D2) + backend RBAC-scoped
# GET /api/stats (domain port + Drizzle adapter, every channel-scoped query embeds
# inArray(channel_id, allowedChannelIds) + D4 deleted-message NOT EXISTS; service assembles 4 KPIs
# D3, zero-fills 14-day activity D5, computes readPct AC5; controller/route mounted after the
# generic /api gate) + RBAC integration test (7 tests, channel B never surfaces in any figure, not
# even channels[] existence) + docs synced (api-spec, data-model, ARCHITECTURE-SPINE AD-12 Binds,
# TECHNICAL-DESIGN §11, epics.md FR24/FR25 + Épico 9 header fix, PRD.md). Gate green: lint 0 / 860
# unit+web (+44) / build clean (5 pkgs) / 127 integration (+7, 20 files). No new dep, no web change
# (9.2/9.3 out of scope). NOT committed to a PR yet — 3 commits on feat/9-1-stats-endpoint.
# project: share2brain
# project_key: NOKEY
# tracking_system: file-system
# story_location: _bmad-output/implementation-artifacts

# STATUS DEFINITIONS:
# ==================
# Epic Status:
#   - backlog: Epic not yet started
#   - in-progress: Epic actively being worked on
#   - done: All stories in epic completed
#
# Epic Status Transitions:
#   - backlog → in-progress: Automatically when first story is created (via create-story)
#   - in-progress → done: Manually when all stories reach 'done' status
#
# Story Status:
#   - backlog: Story only exists in epic file
#   - ready-for-dev: Story file created in stories folder
#   - in-progress: Developer actively working on implementation
#   - review: Ready for code review (via Dev's code-review workflow)
#   - done: Story completed
#
# Retrospective Status:
#   - optional: Can be completed but not required
#   - done: Retrospective has been completed
#
# Action Item Status:
#   - open: Committed during a retrospective, not yet addressed
#   - in-progress: Actively being worked on
#   - done: Completed
#
# WORKFLOW NOTES:
# ===============
# - Epic transitions to 'in-progress' automatically when first story is created
# - Stories can be worked in parallel if team capacity allows
# - Developer typically creates next story after previous one is 'done' to incorporate learnings
# - Dev moves story to 'review', then runs code-review (fresh context, different LLM recommended)
# - Retrospective appends its action items to action_items; sprint-status surfaces open ones

generated: 2026-06-30
last_updated: 2026-07-15  # story 2-6 review → done (bmad-code-review R1+R2, 3 layers @ Opus 4.8): all 7 ACs SATISFIED; 2 patches applied to packages/shared (blank invite_url→undefined so it never aborts boot; invite_url/inviteUrl scheme pinned to http(s) via canonical isHttpUrl, rejecting javascript:/data:, replacing deprecated z.string().url()); +5 tests (2 auth schema, 3 config). Re-gate green: lint 0 / 1111 unit+web (1 skip) / build 5 pkgs. E2E not re-run — change only tightens invalid-input validation; e2e harness uses a valid https URL so DOM is byte-identical. R2 re-review CONVERGED: 0 actionable / 3 dismissed (test-matcher follows file convention; inline msg cosmetic; api-spec format:uri doc-only), fixes correct + no regression, no code change in R2. Next: PR + bmad-checkpoint-preview. || (prior) story 2-6 in-progress → review (bmad-dev-story): config-driven demo Discord invite link on the login screen. Fully ADDITIVE + backward-compatible — optional `inviteUrl` threaded end-to-end; a deployment without access_control.guest_access.invite_url renders exactly as Story 2.5. shared: GuestAvailabilityResponseSchema.inviteUrl (z.string().url().optional(), enabled kept z.literal(true) so disabled=404) + guest_access.invite_url (optional URL, NO .default(), D4). backend: ResolvedGuestAccess.inviteUrl resolves block?.invite_url (no default); type-widening chain main→app→controller in lockstep (TS strict = safety net); controller emits inviteUrl in the 200 probe body via conditional spread (omitted when absent); e2e/server harness inviteUrl='https://discord.gg/e2e-demo'. web: fetchGuestAvailability() widened boolean→{enabled,inviteUrl?}; DiscordIcon gained local color prop (default currentColor → white login btn unaffected); LoginScreen renders the invite row inside the existing {showGuest && …} fragment, gated on inviteUrl (plain <a target=_blank rel=noopener>, AD-3); .kh-demo-invite-link CSS (cascade parity w/ .kh-guest-btn); i18n login.noAccess/joinDemoServer in es+en (parity.test green). Config-path corrected per Dev Notes (access_control.guest_access, NOT the SCP's auth. shorthand); secrets/behavior split honored (real discord.gg/bf8TvtGnZd only in local config.yml; placeholders in .example + TECHNICAL-DESIGN §13). Fixed 5 App.test.tsx mocks broken by the widened return shape. +8 new tests (3 backend controller/resolver, 3 auth-client, 3 LoginScreen component). Gate GREEN: lint 0 / 1106 unit+web (1 skip) / build clean (5 pkgs) / 38 e2e (auth-guest.spec.ts asserts the invite row). Branch feat/2-6-login-demo-invite off ed04d13. AD-3+AD-6 intact, no new dep, no DDL/migration. Next: PR + bmad-code-review 2-6 (different LLM), then bmad-checkpoint-preview. || (prior) 2026-07-13 story 11-5 re-review R2 (bmad-code-review, extra scrutiny): CONVERGED, 0 actionable (0 decision / 0 patch / 0 defer / 2 dismissed). Edge verified the R1 statsLine fix is a genuine non-vacuous guard; Auditor: all 6 ACs SATISFIED, no regression; Blind: no actionable defects. No code change → gate not re-run. Story stays done. || R1: story 11-5 review → done (bmad-code-review, 3 layers @ Opus 4.8: Blind/Edge/Auditor). Triage 0 decision / 1 patch (FIXED) / 0 defer / 13 dismissed. Patch: added statsLine desktop-only absence assertion to the mobile×dark AC2 test (adaptive-shell.spec.ts, /pgvector/) — closed the AC2 3-element enumeration gap (was asserting only live-pulse + username). Edge Case Hunter verified all token/geometry values, selectors, the spec-order invariant, seeded unread=3 and non-mutation against source. E2E re-gate GREEN: 38 passed (28 pre-existing byte-identical + 10 new) in 58s; lint clean. Frontend/e2e-only; AD-3/AD-6 intact. CLOSES EPIC 11 (only epic-11-retrospective remains, optional). Next: PR + bmad-checkpoint-preview. || (prior) story 11-5 backlog → ready-for-dev (bmad-create-story): extend the computed-style Playwright e2e harness (NOT pixel screenshots — no toHaveScreenshot) to a mobile viewport (390×844, matchMedia(max-width:760px) → isMobile) + light theme (data-kh=light), across login/search/docs/stats/chat for combos mobile×dark, mobile×light, desktop×light (desktop×dark already covered). Design: D1 parametrize loginAs(page,code,theme='dark') default dark → existing callers byte-identical; D2 NEW spec files + test.use({viewport}) NOT a project matrix (existing specs assert dark-desktop hard-coded, a matrix would break them + churn) — single chromium Desktop-Chrome dark-forced project untouched; D3 new specs NON-MUTATING + sort BEFORE mutating chat/docs specs (docs mark-all zeroes the unread bottom-nav-badge; workers:1 alphabetical order invariant, tests/README.md); D4 explicit viewport 390×844 (no isMobile/hasTouch ctx flag — design keys off matchMedia width only); D5 assert combo DELTAS only (mobile shell/geometry + light tokens), not full desktop×dark re-assert. Key anchors: BottomNav fixed bottom:0 h:62 bg --bg-deep border-top --line, Sidebar ABSENT from DOM (not display:none), header pad 0 14px + statsLine/live-pulse/username absent + hexagon present, chat-fab/chat-panel bottom:78 right:16 (vs 24/24), panel max-width calc(100vw-32px)→358px unchanged; light gotcha --accent-ink #F5A623→#9A5B00 rgb(154,91,0) (active tab/chips/focus rings) while raw #F5A623 badge stays. Frontend/e2e-only, touches packages/web/tests/** (+ optional session.ts sig + README), AD-3+AD-6 intact, no dep/no @media/no source change (contentPad testid = open Q, default no). Keep desktop×dark baseline (28 e2e, per 11.4) byte-identical. Story file 11-5-web-e2e-harness-visual-movil-tema-claro.md. LAST story of epic 11 (depends 11.1-11.4). Next: bmad-dev-story 11-5. || (prior) story 11-4 backlog → ready-for-dev (bmad-create-story): responsive reposition of the floating chat widget (FAB + panel). Drills isMobile App→ChatWidget (OPTIONAL prop, default false — D1, mirrors 11.3; keeps all existing ChatWidget tests green untouched). Sole job of isMobile in ChatWidget = corner offset: chatBottom/chatRight consts (isMobile?78:24 / isMobile?16:24, kept NUMERIC for byte-identical desktop → zero e2e churn, D3) applied to BOTH the closed FAB (ChatWidget.tsx:396-397) + open panel (:483-484). 78 clears the 62px bottom-nav (BottomNav height 62) + 16px gap — closes the 11.2 chat-FAB defer (App.tsx:46-47 marker). Panel maxWidth calc(100vw-32px)/maxHeight calc(100vh-48px)/kh-pop already shipped in 5.3 + match design → VERIFY-only, NOT recomputed (D4). Flagged edge (VERIFY/defer): bottom:78 + static maxHeight clips panel top ~30px below ~690px viewport height; no clip at the 390×844 primary target (natural 642<796). frontend-only, packages/web only (ChatWidget.tsx + test + App.tsx), AD-3+AD-6 intact, no new dep, no @media, no new @keyframes. Story file 11-4-web-chat-widget-responsive-fab-panel.md. Branch feat/11-4-chat-widget-responsive off ab0b527. Next: bmad-dev-story 11-4, then 11-5 (last, depends 11.1-11.4). || (prior) story 11-3 re-review → done (bmad-code-review, 2 rounds): R2 CONVERGED, 0 new actionable findings (Auditor: 8 ACs still SATISFIED + 3 R1 fixes no regression; Edge: no unhandled cases; Blind: 0 High/Med, 2 Low non-bugs → 4 dismissed). No code change in R2 → gate not re-run. || R1: 3 adversarial layers @ Opus 4.8 (Blind / Edge / Auditor). Auditor: all 8 ACs SATISFIED, Completion Notes truthful, 0 correctness defects. Triage: 0 decision / 3 patch (ALL FIXED) / 1 defer / 4 dismissed. Patches: (1) hardened DocsView grid test — regex-anchored full template + header==row equality (was loose toContain('280px') which also passes '1280px'); (2) removed dead `padding` from the 3 containerStyle consts (render-site ternary now single source of truth, rendered output byte-identical); (3) new AppLayout forwarding test — hoisted view spies assert each view receives isMobile (both values). Defer: StatsView bottom grid minmax(300px) overflows sub-~312px viewport w/o in-box overflow-x (pre-existing, above the 360px floor; in deferred-work.md). Re-gate green: eslint 0 / 1091 unit+web (+3 new) / typecheck / build 5 pkgs. E2E not re-run — patches touch only tests + dead const property → rendered DOM byte-identical → zero baseline churn holds. Next: PR + bmad-checkpoint-preview.
# || (prior) last_updated: 2026-07-13  # story 11-3 backlog → ready-for-dev (bmad-create-story): responsive adaptation of the 3 content views (Search/Docs/Stats). Drills isMobile App→AppLayout→views (OPTIONAL prop, default false — D1, keeps ~dozens of direct-render view tests green untouched, diverges from 11.2's required-prop shell). Sole job of isMobile in views = dynamic contentPad ('22px 16px 104px' mobile / '34px 40px 60px' desktop, Web.dc.html L1097) — closes 11.2's D6 interim + both 11.2 review defers (mobile last-row occluded by 62px bottom-nav; 104px clears bar+safe-area). DocsView table aligned to updated design (minmax(240→280px,1fr) / min-width 620→720 in BOTH header+row grids, D3) — desktop-safe (min-width never engages at 980-max table → ZERO e2e churn). Stats auto-fit grids (210/300) + Search input (width:100%/max-880) VERIFY-only (D4, already match design). frontend-only, packages/web only, AD-3+AD-6 intact, no new dep, no @media. Story file 11-3-web-adaptacion-responsive-vistas-search-docs-stats.md. Branch feat/11-3-responsive-views off 3cd5871. Next: bmad-dev-story 11-3 (∥ 11-4).
# || (prior) last_updated: 2026-07-13  # story 11-2 review → done (bmad-code-review, 2 rounds): all 7 ACs SATISFIED. R1: 0 patch / 2 defer (11.3 contentPad + 11.4 chat FAB) / 9 dismissed. R2 (challenged all R1 dismissals — all UPHELD): 1 NEW patch FIXED — Header community-name span had dropped the design-verbatim overflow/ellipsis/nowrap + wrapper minWidth:0 (Web.dc.html:163-164), long guild name overflowed collapsed mobile header. Re-gate green (typecheck / web 219 / build / eslint 0)
# story 11-2 in-progress → review (bmad-dev-story): useIsMobile hook + responsive shell
# SHIPPED. useIsMobile.ts over window.matchMedia('(max-width:760px)'), the matchMedia guard in BOTH initializer+effect
# (jsdom has none → false/desktop → all web unit tests green, ZERO churn — the linchpin held). App.tsx drills isMobile →
# AppLayout ({isMobile ? BottomNav : Sidebar}, conditional render, /Documentos/i matches ONE button) → Header (28px Hexagon
# via new EXACT_MIDDLE[28]=16; statsLine+live-pill+username hidden; headerPad 0 14px). New BottomNav.tsx
# reuses exported Sidebar.NAV_ITEMS (accessible-name parity), Documentos badge via App.totalUnread path (data-testid
# bottom-nav-badge). index.html viewport-fit=cover. .kh-bottom-nav-item:focus-visible added. contentPad=11.3, chat FAB=11.4
# (NOT here — mobile bottom padding interim-clipped by design/D6). Gate green: lint 0 / 1081 unit+web (+8: 5 hook + 3 shell) /
# build 5 pkgs / 28 e2e Desktop-Chrome 1280px, ZERO baseline churn. git diff packages/ = web ONLY, AD-3+AD-6 intact, no new dep.
# D3 REVERSED per Borja: guest-mode badge kept VISIBLE on mobile (only isGuest gates the pill, no !isMobile) — re-gate green.
# Next: PR + bmad-code-review 11-2 (different LLM), then (11-3 ∥ 11-4).
# || (prior) last_updated: 2026-07-13  # story 11-2 backlog → ready-for-dev (bmad-create-story): useIsMobile hook + responsive shell.
# useIsMobile over window.matchMedia('(max-width: 760px)') — MUST guard `typeof window.matchMedia !== 'function'` (jsdom has
# no matchMedia) → returns false/desktop → all ~100 web unit tests + App.test.tsx stay green with ZERO churn (the linchpin).
# App.tsx calls the hook, drills isMobile → AppLayout (isMobile ? BottomNav : Sidebar, conditional render so /Documentos/i
# matches ONE button) → Header (28px Hexagon logo; hide statsLine+live-pill+username; headerPad 0 14px). New BottomNav.tsx
# reuses exported NAV_ITEMS+Screen from Sidebar (accessible-name parity), Documentos unread badge via App.totalUnread path.
# index.html gains viewport-fit=cover for env(safe-area-inset-bottom). contentPad=11.3, chat FAB=11.4 (NOT here). Playwright
# chromium=Desktop Chrome 1280px>760 → 28 e2e stay desktop/green. Frontend-only, AD-3+AD-6 intact, no new dep. Story file
# 11-2-web-useismobile-shell-responsive-applayout-header.md. Flags: D3 (hide guest badge on mobile). Next: bmad-dev-story 11-2.
# || (prior) last_updated: 2026-07-13  # story 11-1 re-review #2 (3rd pass, bmad-code-review): CONVERGED, 0 new findings.
# Convergence-breaker re-verified the docs patch (both lines correct, phantom-token-free), ran tsc -p packages/web
# (exit 0) + DocsView.test.tsx (21/21), confirmed remaining --text-* refs are only historical _bmad-output artifacts +
# the design snapshot (out of scope). Story stays done, fully converged. Next: PR + bmad-checkpoint-preview, then 11-2.
# || (prior) last_updated: 2026-07-13  # story 11-1 re-review #1 (bmad-code-review, identical diff, extra scrutiny): all 3 layers
# re-confirm done — Auditor + Edge re-verified AC1-AC5/D1-D6 byte-exact, rename airtight. Widened grep BEYOND
# packages/web → 1 NEW patch APPLIED: docs source-of-truth (frontend-standards.md:205 + bmad-story-mandatory-steps.md:110)
# still cited the old --text-* token names post-rename (Task 6 wrongly said "no frontend-standards change") → fixed to
# --tx/--tx4/--tx5 + corrected the Story-2.1 history note. Docs-only patch (no runtime, AD-3+AD-6 intact, no gate re-run).
# 7 dismissed (CSS-comment "lies" = misread cascade-pattern comments; hover-blurple+focus-amber = D5 a11y convention;
# blurple contrast = same as sibling Discord links, ratified; [data-kh=dark] redundant-but-harmless D4; ACCENT_INK
# unused = FALSE POSITIVE still used docs.spec.ts:55/62/89; "98 refs" count imprecision = no-AC bookkeeping; comment
# naming nit). Story stays done. Next: PR + bmad-checkpoint-preview, then bmad-create-story/dev-story 11-2.
# || (prior) last_updated: 2026-07-13  # story 11-1 review → done (bmad-code-review): 3 adversarial layers @ Opus 4.8 (Blind /
# Edge / Auditor). Edge Case Hunter AIRTIGHT (0 findings) — grep-verified 0 orphan --text-* across packages/web,
# every --tx* resolves to a defined token, cascade intact (.kh-doc-link:hover base lives in class), zero rgb/baseline
# churn, dark/light token parity, --dot-read reserved-unconsumed confirmed. Acceptance Auditor: AC1-AC5 SATISFIED +
# D1-D6 as ratified (token block byte-identical to design; --dot-read dark #272E39/light #C7CCD4 correct). Triage:
# 1 decision-needed (D3 doc-link hover blurple vs amber) RESOLVED by Borja → KEEP BLURPLE (design-canonical, coherent
# w/ sibling Discord links, sanctioned allowlist hex; "not theme-aware" refuted); 0 patch; 1 defer (AC3↔AC4 wording
# tension — spec-hygiene only, code resolution correct → deferred-work.md); 4 dismissed (hex casing deliberate per D3,
# --dot-read D2-reserved, orphan-grep clean, dark selector D4-mandated). No code change from review (implementation
# stands). Frontend-only, packages/web only, AD-3+AD-6 intact, no new dep. Gate was green at dev-story (lint 0 /
# 1073 unit+web / build 5 pkgs / 28 e2e). Next: PR + bmad-checkpoint-preview, then bmad-dev-story 11-2.
# || (prior) last_updated: 2026-07-13  # story 11-1 backlog → ready-for-dev + epic-11 backlog → in-progress (bmad-create-story):
# web refresh of design tokens + interaction states. KEY FINDING: the design has ALWAYS used --tx/--tx2..--tx5 +
# --dot-read (HEAD design too, same values); web's --text-primary..--text-subtle naming (Story 2.1) + missing
# --dot-read are STANDING web deviations, not new design changes. 11.1 = rename --text-* → --tx* (98 refs across 7
# tsx + 2 css + 2 unit assertions) + add --dot-read (#272E39/#C7CCD4, reserved/unused) + :root → :root,[data-kh=dark]
# + ONE real interaction fix (D3: .kh-doc-link:hover amber → blurple #5865F2, matches design "Ver en Discord" link).
# Values byte-identical → zero pixel change → existing dark-desktop visual harness stays green with ZERO baseline
# churn (e2e specs assert rgb VALUES, --text-* only in comments; only DocsView.test.tsx's 2 name-string assertions
# change). Decisions D1-D6 flagged for review at PR (D1 rename per epic's explicit --tx2..--tx5 list; D5 keep the
# chat-suggestion #fff→--tx + focus-visible a11y deviations). Frontend-only, AD-3+AD-6 intact, no new dep. Story
# file 11-1-web-refresh-design-tokens-estados-interaccion.md. Next: bmad-dev-story 11-1.
# || (prior) last_updated: 2026-07-13  # bmad-correct-course (sprint-change-proposal-2026-07-13-responsive-refresh.md): Épico 11
# added → backlog. Trigger: updated design Share2Brain Web.dc.html introduces a full responsive system (breakpoint
# 760px, mobile bottom-nav, adaptive header/padding/chat, safe-area) — 0 before — + refreshed token palette +
# hover/focus states; web impl was desktop-only (only @media = prefers-reduced-motion). Frontend-only, Moderate,
# additive. No schema/contract/backend (AD-3 + AD-6 intact), no new dep (useIsMobile over matchMedia). New FR27.
# 5 stories 11-1..11-5, binding seq 11-1 -> 11-2 -> (11-3, 11-4) -> 11-5. Approved by Borja incl. full re-skin +
# E2E mobile×light/dark. Next: bmad-create-story 11-1.
# || (prior) story 8-2 review → done (bmad-code-review, 3 layers @ Opus 4.8, + re-run #1 CONVERGED:
# Auditor "PATCHES CLEAN" AC1–AC5/D1–D8 hold, patch #2 does NOT violate D5; Edge surfaced 1 new low — whitespace-
# only description (guard used truthiness not trim) → FIXED: guard hardened to doc.description.trim(). Gate re-run
# green: lint 0 / 1072 unit+web / build 5 pkgs. + re-run #2 CONVERGED: Auditor "everything holds, 0 violations";
# Blind's 2 items verified false (z.string() required → no .trim() throw; all fixtures/seed non-empty); Edge 0
# code defects — only test-coverage gaps → added co-location assertion (title.parentElement === description.
# parentElement) guarding the single-cell merge. + re-run #3 CONVERGED: Auditor "everything holds, 0 violations,
# fix #4 reinforces D4/compatible D6"; Blind+Edge validated the co-location assertion robust & non-vacuous, 0 code
# defects → closed the last recurring gap by adding a falsy-branch test (description '   ' → no doc-row-description
# node; D5's no-empty-code premise no longer holds once the guard exists). Residual: only exact 5-grid-child count
# stays deferred under D6. Gate green: lint 0 / 1073 unit+web (+1) / build 5 pkgs). Orig triage below. Auditor: AC1–AC5
# + D1–D8 all SATISFIED, 0 scope violations. Triage: 1 decision-needed (→ patch), 1 patch, 1 defer, 5 dismissed
# (grid 5=5 children, block-stacking, badge order per AC2, no stray i18n refs, empty-desc test ruled by D5 — all
# verified false/handled). Patches APPLIED: (1) overflowWrap:'anywhere' on title+description spans — post-tweak
# clamp removal let a long unbroken token (URL) overflow → horizontal scroll; (2) guard {doc.description && …}
# — empty z.string() desc left a stray 4px marginTop gap. Defer → deferred-work.md: tests don't assert
# description nesting / 5-grid-child count (accepted by D6, optional hardening). Gate green: lint 0 / 1072
# unit+web (1 skip) / build 5 pkgs / 28 e2e (chromium dark, docs.spec 4.4/8.1/8.2 + 7.6).
# || (prior) story 8-2 backlog → ready-for-dev (bmad-create-story): DocsView merges título +
# descripción into a single «Documento» column (title + "Nuevo" badge + description stacked), grid 6→5 tracks
# (minmax(240px,1fr) 44px 92px 116px 84px, min-width 720→620). Frontend-only, DOM-merge re-skin — zero handler/
# effect/CSS changes (components.css + global.css UNTOUCHED). Story file
# 8-2-web-docsview-columna-documento-unificada.md. D1 description marginTop:4; D2 locale keys renamed
# (docs.columns.title→document) + docs.columns.description DELETED in BOTH es/en in lockstep (parity.test.ts is
# a hard gate); D4 description <span> moves into the title cell's content wrapper after the badge, testid kept;
# D5 the 8.1-deferred empty-description defer is RESOLVED by de-promotion (no code). epics.md UX-DR12/UX-DR13 +
# Historia 8.2 already synced by the correct-course step. Fresh-context analysis caught 2 header-label waits the
# SCP change-list omitted (DocsView.test.tsx:255 es empty-state, :369 en locale). Branch
# feat/8-2-docsview-documento-column off ab044ac. Next: bmad-dev-story 8-2.
# || (prior) ops-4-sentry-observability review → done (bmad-code-review, incl. 3 post-patch
# re-runs @ Borja's request). Re-run #3 FULLY CONVERGED: all 3 layers 0 new findings — Blind "ship it",
# Edge 0 edges, Auditor 0 AC violations (AC1–AC11 SATISFIED); graceful-shutdown flush verified sound on both
# clean+error paths, no hang/double-exit. Only a doc-hygiene note (story File List missing lifecycle.ts/
# lifecycle.test.ts) → fixed. Review CONVERGED; no further re-run needed. || (re-run #2) CONVERGED: Edge +
# Auditor 0 findings (26/26 shared tests, tsc clean,
# AD-2 grep empty, redactor hardening hand-traced sound); Blind surfaced B1 (graceful SIGTERM shutdown never
# flushed Sentry → tail logs dropped on normal restart) → FIXED: flushSentry() in the finally of all 3
# graceful paths (backend lifecycle.ts DI-injectable + bot/workers exit(0)), +1 lifecycle test. Blind's other
# 4 items were already deferred/dismissed. Gate: lint 0 / 1072 unit+web / build 5 pkgs.
# || (prior re-run #1): re-run re-confirmed 0 AC violations + P1/P2 sound (Edge verified flush no-hang/
# no-early-exit vs real Sentry SDK); re-run caught P2's redactValue had introduced a shared-reference (DAG)
# drop bug (N1 Med) + Date/Map-mangle (N4) + string-body-escape (N3) → all fixed in one consolidated
# hardening patch (path-scoped WeakSet, isPlainObject pass-through, string-body redaction, +3 tests); N2
# (headers throw) dismissed — refuted at Sentry source. Gate re-run green: lint 0 / 1071 unit+web / build 5
# pkgs. || (orig) 3 adversarial
# layers @ Opus 4.8 (Blind / Edge / Auditor). Auditor 0 AC violations — AC1–AC11 all SATISFIED vs real
# source; the 3 DECISIONs confirmed implemented. Triage: 1 decision-needed (→ DEFERRED by Borja: no
# per-request scope isolation / setUser cross-request bleed — accepted KNOWN LIMITATION, single-guild
# profile, import-time instrument.ts = future enhancement), 2 patch APPLIED, 3 defer, 5 dismissed. Patches:
# (P1 High) flushSentry (Sentry.close, bounded 2s, never-throws) chained into all 6 fatal handlers before
# exit(1) — fatal errors + buffered logs were being dropped (acute w/ crash notifications off, the default,
# so notify() resolves instantly); (P2 Med) deep/recursive redaction in sentry.ts (redactValue drops
# `content` at any depth + WeakSet circular guard) + beforeSend now scrubs breadcrumbs/request. +4 unit
# tests. Gate re-run green: lint 0 / 1068 unit+web (+4) / build clean (5 pkgs). Defers → deferred-work.md
# (redactSecrets URL-userinfo-only; JSON.stringify throw-on-circular; <500-status→client-500 escapes
# Sentry's >=500 capture). Story done; UNCOMMITTED on feat/ops-4-sentry-observability, no PR yet.
# Next: commit + PR + bmad-checkpoint-preview. || (prior) ops-4-sentry-observability in-progress → review
# (bmad-dev-story): implemented on
# feat/ops-4-sentry-observability. @sentry/node ^9.41.0 (→9.47.1) in shared ONLY; new
# @share2brain/shared/observability (initSentry + beforeSend/beforeSendLog scrubbing + captureException/
# setSentryUser/setupSentryErrorHandler wrappers — single integration point, services never import
# @sentry/node directly, AD-2). Logger dual sink (stdout + Sentry Structured Logs, same redactSecrets,
# gated by log_level). All 3 main.ts: initSentry after loadConfig (AD-8) + captureException in fatal
# handlers; backend app.ts: post-auth setSentryUser (internal userId + roles, never snowflake/content/PII)
# + setupSentryErrorHandler before the { error, code } mapper. bot/workers consolidated onto shared logger
# (local copies DELETED, 36 import sites redirected; [bot]/[workers] prefix unchanged). DECISION: setUser
# id = internal UUID (session has no discord snowflake; better fit for "id+roles, no PII"). Log type derived
# from beforeSendLog sig (not imported from @sentry/core). Docs synced (PRD stack+SNF-9, SPINE deferred→
# implemented, development_guide Observability section, project-context rule). Gate green: lint 0 / 1064
# unit+web (+13) / build clean (5 pkgs). AC3 empty-DSN no-op boot smoke PASS; AC11 live real-DSN smoke PASS
# (error+info log flushed to real Sentry, flush=true, eventId returned). NOT committed to a PR yet.
# Open Q resolved: environment=NODE_ENV??'production'; release tagging deferred. Next: commit + PR +
# bmad-code-review (different LLM). || (prior) ops-4-sentry-observability backlog → ready-for-dev (bmad-create-story):
# Sentry observability for backend/bot/workers (full logs + errors), @sentry/node ^9.41.0 in shared only,
# initSentry per main.ts after loadConfig, consolidate bot/workers onto shared logger. Source:
# sprint-change-proposal-2026-07-13-sentry.md + operational-backlog P1.3 (bmad-correct-course, approved).
# || (prior) story 10-2 in-progress → review (bmad-dev-story): implemented on
# feat/10-2-web-i18n off 4471cd2. i18next 26 + react-i18next 17 (single translation namespace,
# 103 keys, es/en structurally identical per parity.test.ts); pre-render boot language
# resolution (fetchUiLanguage never throws ⇒ es); ~80 literals extracted across 8 components,
# D9 module-scope trap (NAV_ITEMS/SUGGESTIONS/STATS_LINE) resolved via render-time t(); D6
# relativeTimeEs → relativeTime(iso, locale); D4 errors.<CODE> wired ChatWidget-only; D5
# plurals in StatsView, SearchView "1 resultados" byte-identical. Gate green: lint 0 / 1051
# unit+web (+32) / build clean (5 pkgs) / 28 e2e (0 spec changes). test:integration skipped
# (web-only, 8-1 precedent). AC7 manually verified via dev servers + headless-Chromium
# screenshots (login + guest-authenticated shell, all 3 views + chat, English; D3 KPI-Spanish
# limitation visually confirmed as expected; local config restored to es afterward). D3
# limitation appended to deferred-work.md. NOT committed to a PR yet. Next: commit + PR +
# bmad-code-review.
# || (prior) last_updated: 2026-07-12  # story 10-2 ready-for-dev → in-progress (bmad-dev-story): starting
# implementation on branch feat/10-2-web-i18n off 4471cd2.
# || (prior) last_updated: 2026-07-12  # story 10-2 backlog → ready-for-dev (bmad-create-story): react-i18next 17 +
# i18next 26 in packages/web, static es/en JSON locales (single translation ns, view-namespaced keys),
# pre-render boot fetch of GET /api/ui-config (fetchUiLanguage never throws, ANY failure ⇒ es,
# AbortSignal.timeout 3s) + changeLanguage before first render (no FOUC). Literal inventory CORRECTED
# vs epic: ~80 literals / 8 files (not ~31/9; icons.tsx & AppLayout have zero) + 3 extra hardcoded-locale
# surfaces beyond StatsView's 7 toLocaleString('es'): Intl.DateTimeFormat('es') SearchView:282 +
# DocsView:408, Intl.RelativeTimeFormat('es') lib/relativeTime.ts (relativeTimeEs → relativeTime(iso,
# locale), mechanical test-callsite update sanctioned). 🚩 D3 veto point: StatsView KPI label/sub are
# BACKEND-produced Spanish (statsService.ts:96–121) — stay untranslated under en (SCP pins backend
# bodies unchanged); deferred-work follow-up. D4: errors.<CODE> mapping (11-code vocabulary) wired
# ChatWidget-only (sole surface receiving codes; other 8 api clients untouched). D5: plurals in
# StatsView only — SearchView keeps '1 resultados' (SearchView.test.tsx:93 pins it). D9 trap: module-
# scope consts (NAV_ITEMS/SUGGESTIONS/STATS_LINE) must translate at render, not import. Unit i18n via
# vitest setupFiles importing src/i18n.ts (real es resources ⇒ 126 existing asserts untouched); e2e
# server uiLanguage-free ⇒ 28 e2e byte-identical (Epic 10 critical guard, AC6). Fresh-context checklist
# validation applied 3 critical fixes (SearchView singular assert exists; CustomTypeOptions needs
# { translation: ... } wrapper — bare JSON type fails TS2344; enableSelector already defaults false
# in 26.3.6) + 6 enhancements. Next: bmad-dev-story 10-2 (branch feat/10-2-web-i18n off 4471cd2) —
# final Epic 10 story.
# || (prior) last_updated: 2026-07-12  # story 10-1 review → done (bmad-code-review): 3 adversarial layers
# (Blind/Edge/Auditor). Auditor confirmed all 7 ACs + D1–D7 SATISFIED, no violations. 1 patch applied
# (Task-4.3 optional hardening: catch-all router.use 404 on /api/ui-config so unmatched paths/methods
# don't double-count the shared apiLimiters / ERR_ERL_DOUBLE_COUNT; router.use not all('*') for
# Express-5/path-to-regexp-v8) — re-verified lint 0 / build clean / 1003 unit+web / 4 uiConfig
# integration. Re-run after the patch confirmed the double-count fix effective + surfaced 1 new
# patch (the catch-all's plain-text 404 → unified { error, code } JSON envelope + a POST→404 test;
# uiConfig integration now 5/5). 2 deferred (no Cache-Control on ui-config; es/en enum duplicated
# config↔contract), 15 dismissed across both rounds. NOT committed to a PR yet.
# || (prior) last_updated: 2026-07-12  # story 10-1 in-progress → review (bmad-dev-story): implemented on
# feat/10-1-ui-config off fe1f56f. Shared optional `ui` config block (no .default(), D1) +
# UiConfigResponse contract (schemas/uiConfig.ts, barrel) + backend GET /api/ui-config mounted
# between /api/auth and the generic gate reusing apiLimiters (general tier, D2/D4). Docs drift
# closed (config example ui: block, api-spec.yml:16-17 + backend-standards.md:742/744 exception
# lists). 2 out-of-scope, Borja-approved fixes for issues confirmed pre-existing via git-stash
# A/B against fe1f56f: stale hivly DB credentials in local .env, missing CSRF header in the
# guest-login e2e helper (Story 2.5 territory). Gate green: lint 0 / 1003+1skip unit+web (+10) /
# build clean (5 pkgs) / 142 integration (deterministic with --no-file-parallelism — surfaced the
# Epic 9 retro AI-5 rate-limit test-isolation race concretely, not newly introduced) / 28 e2e.
# NOT committed to a PR yet. Next: commit + PR + bmad-code-review, then 10-2.
# || (prior) last_updated: 2026-07-12  # story 10-1 ready-for-dev → in-progress (bmad-dev-story): starting
# implementation on branch feat/10-1-ui-config off fe1f56f.
# || (prior) last_updated: 2026-07-12  # story 10-1 backlog → ready-for-dev (bmad-create-story) + epic-10 → in-progress
# (first story). Story file: 10-1-shared-backend-config-ui-language-endpoint-ui-config.md. Scope:
# shared optional top-level `ui:` block (language es|en, NO .default() — D4 convention) +
# UiConfigResponse in shared/schemas (barrel) + unauth GET /api/ui-config on the GENERAL api tier
# (reuse the SAME apiLimiters instance, rl:api:, NOT the auth tier), gate-exempt by mount order
# (between app.ts:220 auth mount and :226 generic gate). Ratified: single defaulting point =
# createApp `opts.uiLanguage ?? 'es'` (agentMemoryWindow precedent, NOT presence=enabled); no
# backend resolver module; handler never touches req.session; always 200 (no error branch).
# Docs: api-spec path/schema + TD §11/§13 ALREADY landed in fe1f56f (verify, don't redo);
# remaining = Share2Brain.config.yml.example ui block (REAL filename — epics.md bullet has it
# wrong) + no-auth exception-list drift (api-spec.yml:16-17, backend-standards.md:742/744).
# Factual correction baked in: /api/auth/me was NEVER re-tiered (the 429 lesson is placement-only).
# Critical guard: buildTestAppOptions + e2e/server.ts stay uiLanguage-free ⇒ default es ⇒ 28 e2e
# byte-identical. Fresh-context validation: 0 critical + 2 enhancements (empty `ui:` errors on Zod
# path `ui` not `ui.language`; unmatched-method fall-through double-counts the api limiter —
# ERR_ERL_DOUBLE_COUNT, accepted edge) + 3 optimizations, ALL applied. Next: bmad-dev-story 10-1,
# then 10-2.
# || (prior) last_updated: 2026-07-12  # epic-10 (UI i18n) added → backlog (bmad-correct-course,
# sprint-change-proposal-2026-07-12-i18n.md): UI language via Share2Brain.config.yml ui.language
# (optional block, absent ⇒ es; NOT .env — secrets-vs-behavior rule held), runtime GET /api/ui-config
# (no auth, general rate-limit tier), react-i18next + static es/en JSON bundled (sync init). New FR26
# (epics.md inventory + mapping + Épico 10 section). Docs synced: PRD §2.3/§16 (i18n de-scoped-out),
# TECHNICAL-DESIGN §5.5/§11/§13, api-spec.yml (/api/ui-config + UiConfigResponse), frontend-standards
# (new i18n rule). Critical guard ratified: default es keeps 28 e2e + unit Spanish-literal asserts
# byte-identical. Housekeeping: epic-2 in-progress → done (2-5 converged 2026-07-11, epic left open).
# Scope Moderate. Next: bmad-create-story 10-1, then bmad-dev-story (10-1 → 10-2).
# || (prior) last_updated: 2026-07-11  # story 2-5 re-review #4 (confirmation) → CONVERGED: all 3 layers agree the
# shared-sentinel blast radius is exhausted (every userId-keyed surface verified isolated or transitively
# covered by the empty-sentinel invariant). Only a docs-consistency patch applied, no code change. Done.
# || (prior) last_updated: 2026-07-11  # story 2-5 re-review #3 → done (bmad-code-review): allowlist sound; shared-guest
# identity also bled read-status + stats (state/aggregate, not content) → FIXED ephemeral (guest read-status
# writes no-op → all-unread; per-user stats aggregates → 0; threaded via req.session.isGuest). Anti-pattern
# amended (RBAC/visibility branch forbidden vs ownership-isolation branch allowed). 1 defer (allowlist
# growth/lost-update, demo-scoped). Gate green: lint 0 / 926 unit+web / build / 138 integration / 28 e2e.
# || (prior) last_updated: 2026-07-11  # story 2-5 re-review → done (bmad-code-review, 2nd pass): all fixes confirmed;
# NEW finding — ephemeral-guest isolation was incomplete on POST /api/chat resume (shared sentinel userId).
# FIXED (option A): per-session guestConversationIds allowlist in the Redis session + resolveConversation
# guestScope gate → same-session multi-turn kept, no cross-guest/cross-session resume. Gate green:
# lint 0 / 922 unit+web / build / 137 integration / 28 e2e.
# || (prior) last_updated: 2026-07-11  # story 2-5 review → done (bmad-code-review): 3 adversarial layers @ Opus 4.8.
# 1 decision (cross-guest chat-history leak — all guests share the sentinel userId) FIXED in-story:
# guest chat now ephemeral (GET /api/conversations → empty, /:id → 404, guarded on req.session.isGuest;
# ChatWidget hides "Historial"). 2 patches FIXED (guestLogin resolves getMe before establishing the
# session; RBAC integration asserts toEqual exact). 3 deferred (no boot warning on zero-channel guest
# role; POST /guest no CSRF like /logout; disable doesn't revoke live TTL-bounded sessions), 8 dismissed.
# Gate green: lint 0 / 917 unit+web (+3) / build / 136 integration (+1) / 28 e2e (+1).
# || (prior) last_updated: 2026-07-10  # ops-3 review → done (bmad-code-review): 3 adversarial layers @ Opus 4.8,
# 9/9 ACs PASS. All correctness-critical surfaces verified clean at source (eslint AD-2/AD-3 guards,
# events.ts share2brain: keys, config loader, docker-compose, package-lock symlinks). Applied 4
# doc/provenance patches: PRD example Postgres identifiers lowercased (were capital Share2Brain vs
# lowercase DATABASE_URL — copy-paste auth-fail), design-mock file git mv KeepHive→Share2Brain Web.dc.html
# (refs dangled), clone-URL owner → borjaberrocal87, and git mv of the dated snapshot dirs
# (architecture/prd-hivly-2026-06-30 → -share2brain-). 3 deferred (untracked local Hivly.config.yml
# un-gitignored = Borja Task 8; #Share2Brain-alerts Slack casing pre-existing; commit-count nit).
# 0 High/Medium code issues. STILL NOT PUSHED — push/PR + repo/dir/config ops = Borja's trigger.
# || (prior) last_updated: 2026-07-10  # ops-3 in-progress → review (bmad-dev-story): full rename hivly → share2brain
# shipped on branch chore/rename-share2brain (7 commits, inner-first shared→services→web→repo/infra→
# docs→bmad; repo/infra = 2 commits, a hollow-first-commit recovery). Gate green: lint 0 / test 892+1skip
# / build 5 pkgs / Playwright e2e 23/23 (vs a fresh migrated share2brain DB). npm install regenerated the
# lock + @share2brain/* symlinks. AD-13 stream prefix ratified hivly: → share2brain:. Residual "hivly"
# only in provenance (this story, the rename SCP, these notes) + Borja's untracked local config. Next:
# bmad-code-review, then flip → done + push/PR (Borja's trigger) + his repo/dir/config ops steps.
# || (prior) last_updated: 2026-07-10  # ops-3 backlog → ready-for-dev (bmad-create-story): full rename hivly →
# share2brain as a post-roadmap ops chore (ops-N convention, invoked explicitly). Story:
# ops-3-rename-hivly-share2brain.md. 1920 hivly refs / 279 files. Verified surface: no @hivly path
# aliases (npm workspace symlinks only → rename is atomic, npm install rebuilds); real Hivly.config.yml
# untracked (only .example in git); migrations clean of hivly; .opencode hivly refs only in untracked
# plans/ scratch. Preservation rule baked in: do NOT blind-sed — the SCP + this story keep "hivly" as
# the old name. Next: bmad-dev-story ops-3.
# || (prior) last_updated: 2026-07-10  # story 9-3 review → done (bmad-code-review): 3 adversarial layers @ Opus,
# Auditor PASS (AC1-AC8 + D1-D8 conformant), Edge Case Hunter 0 High/Medium (full source verify,
# refuted 2 Blind findings), 0 patch / 0 decision-needed / 2 Low defers (activity positive-path
# coverage ratified-out by D5; RBAC text canaries narrower than the count-based canaries, mitigated).
# FINAL Epic 9 story — epic-9 can flip to done (retrospective optional).
# || (prior) last_updated: 2026-07-10  # story 9-3 in-progress → review (bmad-dev-story): FINAL Epic 9 story
# implemented on feat/9-3-e2e-stats-harness off 23cdbe3. Seed extended (seed.ts ONLY): D1 authorName
# field, D2 partial author_name on e2e-msg-r1/r2 only, D3 RBAC canary trio (e2e-ch-secreto +
# e2e-msg-s1 'Eve Intrusa' + its embedding, all dated a shared SEED_NOW "today"), D4 conversation
# timestamps now SEED_NOW-relative. New packages/web/tests/analytics.spec.ts (7 tests, D7) sorts
# alphabetically first per D1's ordering rationale. §3.3 curl-cookie-jar smoke matched Dev Notes'
# precomputed /api/stats JSON byte-for-byte for both e2e-member and e2e-empty BEFORE writing specs.
# First live Playwright run: 6/7 green immediately; 1 Chromium serialization quirk (7.6-class) —
# linear-gradient(180deg,...) computes with the default direction dropped — fixed by asserting the
# color-only regex (documented inline). Re-run 7/7 green. Full gate: 23 e2e (analytics 7/chat 7/docs
# 4/interactions 2/search 3, ordering invariants held) / lint 0 / 892 unit+web unchanged / build
# clean (5 pkgs). git diff confirmed ZERO touch to the 4 existing specs, playwright.config.ts,
# helpers/session.ts, or any packages/web/src file. tests/README.md synced (D8): seed-identities
# table + RBAC canary note + full 5-file spec-ordering rationale. Integration NOT run (seed.ts only
# imported by src/e2e/server.ts, no shared/backend/DDL change — 7.6 precedent). Deferred-AC
# traceability recorded in Completion Notes (closes 6 named 9.2-deferred testid groups + nav-mount +
# real-author-name Top-5 + real-parse-failure path; name-truncation/statsInitials-emoji/unit-level
# gaps stay deferred by design). NOT committed to a PR yet. FINAL Epic 9 story — after
# bmad-code-review, epic-9 can flip to done (retro optional).
# || (prior) story 9-3 backlog → ready-for-dev (bmad-create-story): FINAL Epic 9 story —
# extend the Playwright harness to the Estadísticas view. NEW spec `analytics.spec.ts` (D1: name
# chosen to sort ALPHABETICALLY BEFORE the mutating chat/docs specs — a stats.spec.ts would run
# last and read post-mutation coverage 5/5/100 + queries drift, diverging from standalone runs;
# all 7 new tests non-mutating; 16→23 chromium) + e2e seed extension (packages/backend/src/e2e/
# seed.ts ONLY, no production code): D2 PARTIAL author_name (r1 'Linus Torvalds', r2 'Ada
# Lovelace', g1/g2/g3 stay NULL — search.spec:68 asserts initials 'E2' from g1's raw author_id,
# and the 9.5 latest-non-blank pick still yields real Top-5 names, proving COALESCE tier-1-vs-3
# live), D3 RBAC canary trio (e2e-ch-secreto ['e2e-role-none'] + e2e-msg-s1 'Eve Intrusa' + one
# embedding dated seed-boot today — a leak flips figures in ALL five blocks; absence asserts in
# AC6), D4 conversation timestamps → seed-relative (defuses the queries-KPI 2026-07-31 time bomb;
# chat.spec asserts no dates), D5 activity stays all-zero (no re-dating of the 5 embeddings; 4px
# stubs + today gradient; proportional geometry covered by channel/top-user 100%/67% fills).
# Expected member /api/stats JSON precomputed in-story (kpis 5/2/2/1, coverage 2/5/40, topUsers
# Ada 3 / Linus 2) + Chromium values (avatar hash 1295→#F5A623 / 1556→#C792EA, gradient rgb
# serializations, style-attr width asserts). Gate: 23 e2e + lint/unit/build; integration NOT run
# (seed imported only by e2e/server.ts); docs sync = tests/README.md only (D8). Fresh-context
# validation: 0 critical, 6 enhancements applied (curl cookie-jar smoke, README stale-order+
# identities rewrite + cross-run note, donut serialization candidate, bar-area locator, nav-icon
# width-attr assert). Branch feat/9-3-e2e-stats-harness off 23cdbe3. Next: bmad-dev-story 9-3;
# after its review Epic 9 can close (retro optional).
# || (prior) story 9-2 review → done (bmad-code-review): 3 adversarial layers @ Opus (Blind / Edge / Auditor). Auditor 0 AC/D violations — AC1–AC8 + D1–D6 all verified faithful vs real source (contract stats.ts, service literals statsService.ts:87-112, tokens, sibling patterns). Triage: 0 decision-needed, 1 patch (applied), 3 defer (Low → deferred-work.md), 5 dismissed. Patch: activity per-bar tooltip → toLocaleString('es') (consistency w/ activity-total; D2/D5-defensible either way). Defers: name/channelName span truncation (9.3 visual domain), statsInitials degenerate/emoji names (cosmetic, D4 algo pinned), unit-coverage gaps (parse-failure/readPct=100/alpha-initials/nav-mount — mostly 9.3's harness). Dismissed = 2 Blind FPs refuted at source (unread≥0 via coverage.readCount=Math.min(readCount,totalCount) statsService.ts:119; top-users bar order superRefine-pinned so topUsers[0]=max), "Reintentá." no-affordance = ratified SearchView/DocsView pattern (AC7-pinned), activity date-key upstream-guaranteed distinct, donut title "Cobertura de lectura" spec-consistent. Gate re-run green after patch (lint 0 / web unit 119 / build 5 pkgs). Story done; dev-story's 4 commits already on feat/9-2-web-statsview (e5a35f2 feat / af86ecc test / b1b327f + 8ae124f docs), the review patch (StatsView.tsx tooltip) + review-tracking edits UNCOMMITTED, no PR yet. Next: commit the patch + PR + bmad-dev-story 9-3 (final Epic 9 story). || (prior) story 9-4 review → done (bmad-code-review): 3 adversarial layers @ Opus (Blind Hunter / Edge Case Hunter / Acceptance Auditor). Auditor 0 AC/D violations — AC1–AC6 + D1–D6 all verified in source (schema+migration 0005 nullable, create-path capture, wire-optional MessageUpdatedEvent.authorName, conditional Sync UPDATE, create-event XADD unchanged, docs synced). Triage: 0 decision-needed, 0 patch, 1 defer (Low), 5 dismissed. Defer: edit-path producer has no runtime guard on author.displayName before xAdd — unreachable (discord.js displayName = globalName ?? username, non-null on hydrated author) + rides the same accepted hydration assumption as the existing author.bot read; deliberately not patched (producer-trusts-types / consumer-validates convention, parseUpdatedEvent already normalizes) → deferred-work.md. Dismissed = D3/D4/D5-sanctioned or not-reachable FPs (verbatim create-store, edit-refresh D4, historical NULL D5, rename-without-edit D5, direct-'' bypass). Gate stayed green (lint 0 / 869 unit+web / build 5 pkgs / 127 integration, 1 unrelated flake on rerun). Story done; UNCOMMITTED status flip on feat/9-4-author-name-capture (4 code commits already landed, no PR yet). Next: PR + bmad-dev-story 9-5. Re-run (identical diff, extra scrutiny at Borja's request): Auditor re-confirmed 0 AC/D/AD violations (opened every test file + docs, migration byte-exact incl. no trailing newline). +1 defer (out-of-order stale edit redelivery reverts author_name — pre-existing, content/updated_at already ride the same unguarded overwrite; monotonicity-guard fix out of scope). 9 dismissed incl. 2 Blind-Hunter Medium FPs refuted against source (Record<keyof>-forces-key comment IS accurate — producer typed so at messageUpdate.ts:117; authorName:undefined compiles — exactOptionalPropertyTypes unset repo-wide). Story stays done (both defers Low/pre-existing). Re-run #2 (3rd pass, identical diff): all three layers CONVERGE, 0 new findings — Auditor re-swept whole bot+workers for D6 (0 author-logging hits), create XADD byte-identical vs d9d05f2, Sync write inside existing step-6 UPDATE (no new tx), no api-spec leak, story Completion Notes counts truthful. Review fully converged. || (prior) story 9-4 in-progress → review (bmad-dev-story): implemented on branch feat/9-4-author-name-capture — nullable discord_messages.author_name (schema + migration 0005), bot create-path capture (persistMessage.ts + backfiller.ts), bot edit-path capture (messageUpdate.ts + wire-optional MessageUpdatedEvent.authorName, D1/D2/D3), Sync worker persistence (events.ts normalization + processUpdate.ts conditional UPDATE). 14 compile-only test-fixture fixes + 8 new/extended test cases per the Test Impact map. Docs synced (data-model.md, TECHNICAL-DESIGN.md). Gate green: lint 0 / 869 unit+web (+6) / build clean (5 pkgs) / 127 integration (1 unrelated flake on rerun, readStatus.integration.test.ts ECONNRESET — no backend files touched). NOT committed to a PR yet. Next: bmad-code-review 9-4, then bmad-dev-story 9-5. || (prior) story 9-4 ready-for-dev → in-progress (bmad-dev-story): starting implementation on branch feat/9-4-author-name-capture. || (prior) story 9-4 backlog → ready-for-dev (bmad-create-story): nullable discord_messages.author_name (schema.ts DDL + drizzle-generated 0005_*, never hand-edited) + bot visible-name capture on create/edit. Write topology baked into the story after source verification: the bot INSERTs discord_messages directly on create (persistMessage.ts:69-101 single tx w/ XADD; Indexer only stamps indexed_at) → create-capture = extend IngestibleMessage + .values() (live + backfill/gap-fill share the path; onConflictDoNothing = the no-backfill guarantee, old rows stay NULL); the 6.1 edit handler is publish-only → D1 ratified: MessageUpdatedEvent gains wire-optional authorName?: string (interface optional for legacy in-flight events; producers' Record<keyof T, string> pattern forces the key at compile time) + the Sync worker persists it inside its existing step-6 UPDATE — story touches workers although the epic label says bot+shared, flagged for veto. MessageCreatedEvent deliberately unchanged. D2: name = message.author.displayName (discord.js 14.26.4, globalName ?? username, never null; NOT member.displayName — Message#member is null on REST-fetched pages → nondeterministic nicknames). D3: consumer normalizes absent/'' → undefined, never writes '' nor blank-overwrites a stored name; D4 edits DO refresh; D5 offline name-change w/o content edit undetected (accepted); D6 author names never logged. 6.3 inherits edit capture verbatim (offlineSync reuses handleMessageUpdate; zero code change, compile-check only). Fresh-context checklist validation verified every cited path/line clean and produced 2 critical + 4 enhancement fixes, all applied: compile-only fixture breaks listed (messageCreate.test.ts, reconcile.test.ts — fix fixtures, never weaken slice types), TD §6 mermaid ERD added to the docs-sync list (now definitive: data-model §1 + TD §8 ~619 + TD §6 ~415-425; backend-standards newContent hits = unrelated DDD example DO NOT touch; spine AD-13 no change), messageUpdate.test inline author overrides ~99/107/126/136, workers events.test.ts owns the parse-normalization cases, backfill.integration fixture must add displayName + assert persisted author_name (else silent NULL). Next: bmad-dev-story 9-4 (branch feat/9-4-author-name-capture); then 9-5 → (9-2, 9-3). || (prior) story 9-1 backlog → ready-for-dev + epic-9 backlog → in-progress (bmad-create-story): shared StatsResponse contract (schemas/stats.ts, kpis[4]+activity[14]+channels+coverage) + RBAC-scoped GET /api/stats (mounted after the app.ts generic gate; every embeddings aggregation embeds inArray(channel_id, allowedChannelIds) + the deleted-message NOT EXISTS, AD-12) + composite index idx_embeddings_channel_created REPLACING idx_embeddings_channel (D2 — embeddings has NO indexed_at column; created_at IS the API's indexedAt, epic AC4 wording resolved as D1). 10 ratified defaults D1–D10 flagged for review, notably: D3 KPI subs (+N esta semana / de N accesibles / en tus canales / en total; mock's "de 6 del guild" rejected as a guild-size leak; queries KPI all-time not 30d), D6 empty-scope fast path still runs the per-user queries KPI, D9 mock's "Top 5 usuarios" section OUT of the ratified contract, D10 FR collision — proposal's FR22/23 already taken in epics.md inventory → stats FRs land as FR24/FR25 (Épico 9 header fix included in Task 7 docs sync). Authors KPI needs discord_messages join (no author column on embeddings). Next: bmad-dev-story 9-1 (branch feat/9-1-stats-endpoint; verify PR #53 merged first). || (prior) epic-8 → done (single story 8-1 done; retrospective optional, not run — tiny 1-story UX-polish epic). Story 8-1 committed + PR opened. || story 8-1 review → done (bmad-code-review): 3 adversarial layers @ Opus (Blind Hunter / Edge Case Hunter / Acceptance Auditor). Auditor 0 AC/D violations — all 6 ACs + D1–D8 verified against the extracted design values. 0 decision-needed, 0 patch, 2 deferred (Low), 6 dismissed. Dismissed = verified false positives / spec-sanctioned: CheckIcon import present (line 17, grep-confirmed), --dot-read grep-clean repo-wide (D5), overflow-x:auto design-mandated (no height→no vertical scrollbar), 150px title track design-verbatim (AC1), brittle box-shadow style-string unit assert = documented jsdom workaround w/ robust e2e computed coverage, light-theme badge brown-on-amber = D7-ratified (sanctioned amber literals + --accent-ink #9A5B00 readable text). Deferred to deferred-work.md: (1) read-row checkmark has no non-visual a11y label (not a regression, no AC), (2) empty description renders blank clamped cell (pre-existing z.string(), surfaced by the new dedicated column). Re-run (identical diff, requested for extra scrutiny): applied 1 a11y quality patch — added `.kh-doc-link:focus-visible` outline (matches the app's icon-btn/nav/chat focus convention; the new icon-only 28×28 link had only :hover). RETRACTED a 2nd "patch" (missing fetchChannels stub in the 3 new tests) as a false positive — it came from a hand-compressed diff fed to the Blind Hunter that dropped the mockResolvedValue lines; real tests (DocsView.test.tsx:123,136,149) already carry the stub (19/19 verified). Empirically verified this round: box-sizing border-box → getComputedStyle width returns 28px (Blind width FP), CheckIcon imported line 17, --dot-read grep-clean in code. Gate remains green (web build clean w/ the focus patch; unit 19/19; 16 e2e unaffected — focus not asserted). Re-run #2 (diff incl. focus patch): CLEAN — 0 new patches/defers, all 3 layers concur the focus-visible rule is sound; empirically refuted an Edge-Hunter corner-clipping FP (overflow-y computes to auto → corners still clip, corner-pixel probe returns body) and confirmed minWidth:720 (AC1 verbatim) is inert-but-harmless (grid intrinsic ~746px). Story done; UNCOMMITTED on feat/8-1-docsview-read-unread-redesign (now +1 CSS line) — ready for PR + bmad-checkpoint-preview. || (prior) story 8-1 in-progress → review (bmad-dev-story): DocsView 6-col read/unread redesign implemented on feat/8-1-docsview-read-unread-redesign (re-skin, zero handler/effect changes). Gate green: lint 0 / 816 unit+web (+3) / build clean (5 pkgs) / 16 e2e chromium (docs.spec rewritten, all exact computed values — row-accent inset box-shadow, badge colors, icon-button geometry — verified live against Chromium on first run, no rework). --dot-read removed repo-wide (D5, grep clean); epics.md UX-DR1/12/13 synced. Integration not run (no shared/backend change, stated explicitly). NOT committed — only code-review left before PR. || (prior) story 8-1 ready-for-dev → in-progress (bmad-dev-story): branched feat/8-1-docsview-read-unread-redesign, starting implementation. || (prior) story 8-1 backlog → ready-for-dev + epic-8 backlog → in-progress (bmad-create-story): DocsView read/unread redesign + 6-col layout. Design placeholder values EXTRACTED into the story (Share2Brain Web.dc.html is a 467KB single-line mock — do not re-parse): titleColor var(--tx) BOTH states (no more dim-to-muted), weight 700/500, rowAccent inset 3px 0 0 #F5A623/transparent, grid 150px minmax(160px,1fr) 44px 92px 116px 84px min-width:720 overflow-x:auto, unread dot 8px+glow+Nuevo badge, read = CheckIcon 14px --text-subtle (exact existing icon). 3 ratified defaults flagged for review: D1 link icon-button opens the RESOURCE w/ amber hover (design's blurple "Ver en Discord" tooltip = mock leftover; AC4 wording is authority), D5 --dot-read token removed (only DocsView consumed it), D6 title single-line nowrap → clamp-2 (150px column). Re-skin NOT re-wire: zero handler/effect changes; doc-row-dot now unread-only (new doc-row-check/doc-row-new-badge testids); .kh-doc-link NEW class (border+color in class per cascade gotcha); docs.spec 6-track grid regex + TEXT_TERTIARY const + describe order preserved. Next: bmad-dev-story 8-1 (branch feat/8-1-docsview-read-unread-redesign). || (prior) epic-9 created (bmad-correct-course, sprint-change-proposal-2026-07-10-stats.md): new feature — Statistics view (3rd nav) per design isStats. Stories 9-1/9-2/9-3 → backlog. Aggregates over existing data (no ingestion/table). AD-12 RATIFIED (Borja): every aggregation filters by allowedChannelIds in-SQL — no leak of private-channel volume. Single GET /api/stats. New FR22/FR23. Adds a timeseries index on embeddings(indexed_at) (migration, no new table). PRD/api-spec/data-model edits deferred to story-creation time. Next: finish 8-1 first, then bmad-create-story 9-1. || (prior) epic-8 created (bmad-correct-course, sprint-change-proposal-2026-07-10.md): post-roadmap UX-polish epic. Story 8-1 → backlog. Trigger: DocsView read rows read as "disabled" (grey dot + title dimmed to --text-muted) vs updated design Share2Brain Web.dc.html which emphasizes unread instead (read→checkmark, unread→dot+glow+"Nuevo" badge+row accent, 6-col layout título·descripción·link·canal·autor·indexado). Classification Moderate, frontend-only (no schema/contract/backend; AD-* intact). epics.md appended with Épico 8 + Historia 8.1 (6 ACs). Next: bmad-create-story 8-1. || (prior) epic-7 → done (retro complete + verified): all 6 stories done, retro done, feat/7-6 merged to main (PR #50, cae1ccb), code tree clean, no commits outstanding off the 7-6 branch. Roadmap CLOSED (all 7 epics done). || epic-7-retrospective → done (bmad-retrospective): 6/6 done, 0 incidents, roadmap truly closed (Epic 7 = post-Epic-6 product pivot). Follow-through best of roadmap: both Epic-6 P1s merged (ops-1 MAXLEN PR#39, ops-2 isolation PR#41) + standing real-provider LLM smoke DoD's first live trigger caught a real config bug in 7.2. 3 action items recorded (Blind-Hunter triage tuning, residual load-sensitive integration flake, standing LLM smoke DoD). Retro: epic-7-retro-2026-07-09.md. || (prior) story 7-6 review → done (bmad-code-review): 3 adversarial layers @ Opus (Blind/Edge/Auditor). Edge Case Hunter cross-verified every new assert against the real 7.5 components/CSS/seed; Auditor 0 AC violations, 16/16 counts, ordering invariants hold. 2 patches applied (P1: added overflow-x/overflow-y hidden to the citation title + docs title + description-clamp asserts — closed a discrimination gap where ellipsis/clamp could pass green with overflow dropped; P2: ratified the AC2/D2 clamp-assertion substitution in the Change Log). 11 dismissed as verified false positives / spec-mandated (Unicode NFC/NFD flake, ordering comment-only w/ workers:1 pinned, has:-multi-match links unique per embedding, getAttribute-null, search-no-submit=4.3 pattern, card p/h3 single-match, 15.5px inline not rem, max-width 180px=D4-mandated, popup-fires-before-abort verified, discord regex=unchanged coexistence smoke). Gate: eslint 0 / web typecheck clean / 16 e2e chromium green (search 3 / docs 4 / chat 7 / interactions 2). Integration skipped (tests-only, no shared/backend/seed change). Epic 7 feature-complete — only the retro remains. Patches UNCOMMITTED on feat/7-6-e2e-visual-new-fields.
# (prior) story 7-6 created (bmad-create-story) → ready-for-dev. FINAL Epic 7 story: tests-only,
# extend Playwright visual harness to the 7.5-rendered resource fields (h3 title, .kh-resource-link hover,
# doc-row-description clamp, citation title+href) 7.5 deferred. 4 forks w/ Borja: F1 extend 3 existing specs
# (chat folds into AC6 history-load test); F2 full coverage w/ exact seed hrefs; F3 add MUTATING bubbling
# mark-read test (popup handled, host route-blocked, ordered before terminal mark-all); no prod/seed change.
# ---- prior: story 7-5 review → done (bmad-code-review): 3 layers @ Opus (Blind/Edge/Auditor),
# all 8 ACs satisfied, 0 violations. 1 patch applied (title tightened .min(1)→.trim().min(1) across
# citation/search/documents + 4 whitespace-reject tests; re-gate lint 0 / 813 unit+web / build clean).
# 1 decision deferred (F2 mark-read misses middle-click/context-menu open-in-new-tab — accepted F2 limit).
# 3 deferred (parse-poison blast-radius ratified D3; per-card h3 a11y; AC-1 note under-reports 2 raw seeds).
# 3 dismissed (anchor re-marks read = false positive, doc-row-content testid = by-design D3, §12→§9 self-noted).
# --- prior: story 7-5 in-progress → review (bmad-dev-story): shared title
# tightened to .min(1) (F1); SearchView h3 title + "ver recurso" link (D1/D2); DocsView main cell
# restructured into title/description/bubbling-anchor (D3/F2) + de-chunked copy (F4);
# CitationChip shows resource title + links to citation.link (D4/F3); UX-DR11/12/13/21 + api-spec
# + SPINE AD-6 + TECHNICAL-DESIGN synced; deferred-work title-'' entry resolved. Gate green: lint
# 0 / 809 unit+web (+8) / build clean (5 pkgs) / 120 integration (unchanged) / 13 e2e chromium
# (unchanged, only docs.spec anchor patch). New-field visual/CSS ACs deferred to Story 7.6 by
# design (named, not silently passed). No new deps, no DDL, no backend change.
# story 7-5 ready-for-dev → in-progress (bmad-dev-story)
# story 7-5 backlog → ready-for-dev (bmad-create-story): web rendering of
# the curated resource index. 4 forks confirmed with Borja: F1 shared title tightened to
# z.string().min(1) on Citation/SearchFragment/DocumentFragment (closes the 7.4 review deferral
# "title accepts ''"; enrichment already guarantees non-empty, fixtures/seed real since 7.4);
# F2 DocsView gains a "ver recurso" anchor that bubbles (NO stopPropagation) so opening the
# resource also marks the row read via the existing optimistic handler; F3 citation chip
# redesigned to show the resource title + href = citation.link (placeholder discord.com/channels
# href + stale 5.4 comment deleted; keeps chat-citation testid, #channel text and blurple hover
# so chat.spec stays green untouched); F4 stale "chunk" header/intro renamed to resource wording
# now, docs.spec patched ONLY on the getByText('chunk') anchor. SearchView card: title h3 (Space
# Grotesk 600 15.5px) + description body + "ver recurso" next to the preserved "ver en Discord"
# deep link; new .kh-resource-link class (amber hover, base color in class per cascade rule).
# DocsView main cell: title keeps data-testid doc-row-content + read-state styling (docs.spec
# green), description gets doc-row-description clamp-2. UX-DR11/12/13/21 rewritten in epics.md
# (SCP §4.7 assigned it to 7.5). NEW-field visual/CSS harness assertions stay 7.6 by design —
# 7.5 only keeps the 13 chromium specs green. No new deps, no DDL, no backend change.
# story 7-4 review → done (bmad-code-review): 3 layers @ Opus, all 8 ACs
# FULLY SATISFIED, no scope leaks; 1 patch applied (ragRetriever warn now logs structural Zod
# issue path+code, not error.message — content-free invariant made structural; test discriminates,
# 3/3 + tsc + eslint clean); 1 decision dismissed (accept ratified D3 fail-fast asymmetry on
# search/documents/conversation read-parse — clean-slate runbook covers deploy); 2 deferred to
# deferred-work.md (title z.string() accepts '' vs F3 chip intent; all-rows-malformed → false
# "not enough info"); 4 dismissed as noise.
# story 7-4 ready-for-dev → review (bmad-dev-story): strict link contract
# (isHttpUrl, no more '' tolerance) + Citation.title landed in shared (bidirectional satisfies
# guard enforced both sides in one edit); mechanical compile ripples through graph.ts/chatService.ts/
# ChatWidget.tsx; SYSTEM_PROMPT reframed to curated-resource semantics (FR13, new prompt.test.ts,
# graph.test.ts single-system-message fold stayed green); ragRetriever wraps the per-row parse with
# skip+logger.warn (F2, closes the 7.1 review deferral) — createApp gained an optional logger
# (no-op default for tests/e2e), main.ts injects the real one; e2e seed gained realistic Spanish
# titles/example.com links (13 Playwright specs unchanged, seeded chat answer untouched); 8 backend
# integration suites upgraded to salted real fixtures + FIRST title/description/link round-trip
# assertions on search/documents; docs/runbook synced (operational-backlog 7.4 note, api-spec,
# data-model, SPINE AD-4/AD-6, TECHNICAL-DESIGN §9/§12). Gate green: lint 0 / 800 unit+web (+9) /
# build clean (5 pkgs) / 120 integration (+2) / 13 e2e chromium unchanged. No DDL, no migration, no
# new dependency. Unblocks Story 7.5 (web rendering of title/link + citation-chip redesign).
# story 7-4 backlog → ready-for-dev (bmad-create-story): backend projection
# of the resource index. 3 forks confirmed with Borja: F1 STRICT link contract (linkRefine drops the
# '' tolerance in search/documents/citation; pre-7.4 persisted data covered by the ratified clean-
# slate runbook; config base_url refines untouched); F2 ragRetriever skip-and-warn per row (closes
# the 7.1 review deferral — one corrupt row no longer 500s the whole chat); F3 Citation gains
# REQUIRED title (interface+Zod+SSE frame+persistence+mechanical ChatWidget construction ripple) so
# the 7.5 sources chip can show the resource title. Remaining substance (7.1 already did the
# plumbing): SYSTEM_PROMPT reframed to curated-resource semantics incl. cite-the-link (FR13) + new
# prompt.test.ts; e2e seed gains realistic title/link rows (EmbeddingSpec had neither) preserving
# the one-hot similarity ladder + exact seeded answer text (chat.spec asserts it verbatim); 8
# integration suites' placeholder fixtures upgraded + FIRST title/description/link round-trip
# assertions; docs/runbook sync. No DDL, no migration, no new dependency.
# story 7-3 review → done (bmad-code-review): 3 layers (Blind/Edge/Auditor), 0 AC violations. 2 Low patches applied+verified (lint/build/48 affected unit green): reused-embedding dimension assertion now covers the full row set; abort mid-update logs debug not error. Re-review adversarially confirmed both patches (0 regressions) + 2 regression tests added (revert-and-rerun discrimination-checked, 19/19 processUpdate unit green). 4 deferred (content-update ordering, Indexer/Sync race, unbounded newContent regex, embedder no abort signal — all pre-existing/design). 7 dismissed (F3 empty-purge=ratified + bot fetches full content; updated-before-created=AC-3; kept-link read-status loss=F1; cap-20 kept link; extractUrls dedupes; warn caller-context nit; EnrichmentError messages verified content-free). Patches UNCOMMITTED on feat/7-3-sync-link-diff-reindex.
# as the link-diff reconciler (FR6). Kept links reuse title/description/embedding (zero fetch/
# LLM/embed); new/changed links run the 7.2 pipeline via buildResourceRows (relocated from
# indexer/indexBatch.ts to enrichment/resourceRows.ts with an optional reuse hook); persistence is
# a single-tx wipe-and-reinsert keyed on the new positional chunk_key. Zero-URL/blank edits purge
# all rows (parseUpdatedEvent relaxed); tombstoned/unknown-message updates are ack'd no-ops.
# enrichModel/guard now injected into runSync/processUpdate from the same boot-built instances the
# Indexer uses. Demolished chunking.ts, @langchain/textsplitters, and the knowledge config block
# repo-wide (code + Share2Brain.config.yml(.example) + fixtures + stale doc references in
# TECHNICAL-DESIGN.md/ARCHITECTURE-SPINE.md/PRD.md/backend-standards.md). Gate green: lint 0 / 787
# unit+web / build clean (5 pkgs) / 118 integration (19 files, +convergence-on-redelivery
# idempotency evidence) / 13 e2e chromium unchanged. No migration, no schema change, no new dep,
# no bot/backend/web behavior change. Unblocks Story 7.4 (backend projection).
# story 7-3 ready-for-dev → in-progress (bmad-dev-story)
# story 7-3 backlog → ready-for-dev (bmad-create-story): sync worker rewritten
# as the link-diff reconciler (FR6). 3 forks confirmed with Borja: F1 rebuild-by-diff — in-memory diff
# by normalized link decides COST (kept links reuse title/description/embedding: 0 fetch/LLM/embed;
# only new links run the 7.2 pipeline), persistence is one-tx wipe+reinsert at new positional
# chunk_keys (avoids unique-index collisions when URL positions rotate; read-status loss on edit
# accepted = 6.2 precedent); F2 kept links NOT re-enriched on text-only edits (FR6 covers new/changed
# links; LLM cost control); F3 zero-URL/blank edit PURGES all rows + refreshes content + stamps
# indexed_at (parseUpdatedEvent relaxed to accept blank newContent, timestamp stays non-blank;
# closes the 6.2 "edit-to-blank keeps stale chunks" deferral). Embedded: tombstone guard
# (deleted_at set → ack no-op, closes 6.2 resurrect hazard), processDelete survives UNCHANGED,
# MAX_URLS cap=20 applies to edits, UPSERT kept for AD-13 convergence, demolition of
# chunking.ts/@langchain/textsplitters/main.ts clamp/knowledge config block (Zod strips unknown
# keys → old ymls keep booting), processMessage relocated indexBatch→enrichment/resourceRows.ts
# with an optional reuse hook. No migration, no schema change, no new dep.
# story 7-2 review → done (bmad-code-review): 3 layers (Blind/Edge/Auditor), acceptance clean (11 ACs + D1–D4). 8 patches applied+verified (lint/build/165 affected unit green): P1 HIGH — readCappedBody throw escaped fetchUrl's never-throws contract → poison message (wrapped→typed timeout/network_error); D1 MED — MAX_URLS_PER_MESSAGE=20 cap (grouping window was deleted, nothing replaced it); D2 MED — delimit untrusted page/message text in the LLM prompt; P2 MED — bound title(200)/description(1000) output; P3/P4/P5/P6 LOW/nit (abort rethrow in enrich, HTML hex+astral entities, spine link-refine note, types.ts grouping comment). 3 deferred (UTF-8 charset decoding, surrogate-split truncation, meta regex on full HTML). 2 dismissed as verified false positives (markdown-parens truncation, IPv4-mapped IPv6 SSRF). Patches UNCOMMITTED on feat/7-2-indexer-url-enrichment — commit+push to PR #46 + run integration before merge.
# story 7-2 in-progress → review (bmad-dev-story)
# story 7-2 ready-for-dev → review (bmad-dev-story 2026-07-09): Indexer rewritten as the resource
# pipeline — extractUrls (Discord angle-bracket/markdown unwrap, trailing-punctuation+balanced-
# paren stripping, URL.href normalization) → SSRF-guarded fetchUrl (ssrfGuard.ts: node:net
# BlockList Layer A IP-literal pre-check + undici Agent connect.lookup Layer B; manual-redirect
# loop re-checking scheme+SSRF per hop; streaming byte-cap) → htmlText (zero-dep title/meta/OG
# hints) → enrich (narrow EnrichmentChatModel interface, withStructuredOutput + JSON-fallback,
# empty-result=D1 failure) → indexBatch rewritten per-message (discard no-URL/all-blocked,
# persist resource rows in one tx, D1 LLM/embed failure=whole message un-ACKed, D2 SSRF-blocked
# URL=skip no row). grouping.ts demolished (partitionByIndexState relocated to partition.ts);
# main.ts builds enrichModel+guard once at boot and injects them. New dep: undici ^7.28.0
# (workers only). Shared: link refine hardened to a URL.canParse-based check (AC-7, 7.1 review
# deferral) in new schemas/linkRefine.ts. Gate green: lint 0 / 774 unit+web (+66) / build clean
# (5 pkgs) / 111 integration (19 files, backend+bot+workers) / 13 e2e chromium unchanged pass
# count. Real-LLM smoke run once locally (ENRICHMENT_SMOKE=1) against the configured custom
# provider — non-empty title/description confirmed. Docs synced (TECHNICAL-DESIGN §7/§5.3/§17,
# ARCHITECTURE-SPINE AD-13 + deferred batching, data-model.md, PRD.md incremental). Added
# operational-backlog.md P2.5 (FR21 knowledge-lifecycle notification deferral). Unblocks Story
# 7.3 (sync re-index by link-diff + purge).
# story 7-2 backlog → ready-for-dev (bmad-create-story 2026-07-09): Indexer rewrite as the
# resource pipeline (extractUrls + SSRF-guarded UrlFetcher + AI enrichment + discard). 4 forks
# confirmed with Borja: LLM hard failure → no-ACK/PEL replay; SSRF-blocked URL → skipped, no row;
# undici added as workers dep (Agent connect.lookup rebinding defense + CVE-2026-22036 pin);
# knowledge block/chunking retired in 7.3 (sync still consumes chunkContents until then).
# story 7-1 ready-for-dev → review (bmad-dev-story 2026-07-09): shared kernel pivoted to the resource-index contract (embeddings: content → title/description/link; migration 0003_bent_mandrill.sql generated+applied+verified locally after a full FK-safe wipe; search/documents/citation/sse contracts swapped; required enrichment config block added to Share2BrainConfigSchema + createChatModel widened to ChatModelConfig). Mechanical Placeholder-policy ripples applied across workers/backend/web (zero new behavior) + docs synced (data-model, api-spec, ARCHITECTURE-SPINE, TECHNICAL-DESIGN, epics.md Épico 7 appended, PRD scope note). Gate green: lint 0 / 685 unit+web / build clean (5 pkgs) / 110 integration (backend+bot+workers; 1 transient login-flow flake on first parallel run, green on isolated + full re-run) / 13 e2e chromium unchanged pass count. No new deps. Unblocks Story 7.2 (workers/indexer URL-extraction+fetch+AI-enrichment pipeline).
# epic-7 flipped backlog → in-progress and 7-1 → ready-for-dev on story creation (bmad-create-story 2026-07-09).
# Story 7.1 forks confirmed with Borja: Citation.link REQUIRED (clean-slate runbook extended to
# conversations+messages), link values validated empty-or-URL (base_url refine convention, tolerates
# '' placeholders until 7.2), enrichment config block REQUIRED (not optional), and 7.1 carries the
# pending Phase-0 doc edits (data-model/api-spec/SPINE/TECHNICAL-DESIGN/epics.md FR5+Épico 7/PRD).
# action_items triage 2026-07-08 (bmad-help housekeeping): flipped 12 stale/standing items → done. Group A (5, delivered but unflipped): epic-1 graceful-shutdown (Story 6.4), epic-3 + epic-6 P1.1 MAXLEN (ops-1), epic-6 P1.2 test-isolation (ops-2), epic-3 patch-as-new-code (adopted). Group B (7 standing DoD norms — no completion, re-arm per story): consolidated into operational-backlog.md § Standing Definition-of-Done. Remaining open = 4 genuine backlog items only (epic-2 carried debt → deferred-work.md; epic-3 transactional outbox → P2.1; epic-5 + epic-6 trace panel → F1).
# epic-6 development_status flipped in-progress → done 2026-07-08 (bmad-help housekeeping). All 4 stories (6-1…6-4) + epic-6-retrospective already done; the flag was left unflipped after the retro (same pattern as epic-4). Roadmap complete: epics 1–6 all done. Only the post-roadmap operational backlog (ops-N + P2/F1 in operational-backlog.md) and the deferred items remain outside the epic sequence.
# story 6-4 review → done (bmad-code-review 2026-07-08): 3 adversarial layers (Blind + Edge + Auditor, Opus 4.8). 2 decision-needed (both resolved w/ Borja → patch), 5 patches applied, 3 dismissed. Patches: (1) notifier formatText redacts `://user:pass@`→`://***@` + caps length (AC-1 secret-leak + Telegram 4096 drop); (2) fatal handlers guard on isShuttingDown() so an uncaughtException mid-drain can't abort the clean shutdown / fire a spurious alert (backend exposes it from createGracefulShutdown; bot/workers hoist their shuttingDown flag); (3) workers stop_grace_period 30s→35s (sequential drain = 7s loops + 3×5s redis.quit + 10s db.end = 32s > 30s; backend/bot stay 30s); (4) trust-proxy gated on opts.rateLimit so it's OFF in tests/e2e per note #6. Dismissed: rate-limit "chat tier dead" (false positive — api & chat are complementary burst-vs-sustained caps, matches AC-2 path map), post-uncaughtException 5s window (spec-sanctioned note #9), fetch-reason URL leak (undici msgs don't embed the URL). Gate: lint 0 / 640 unit+web (+3) / build clean / backend-integration 87 pass + security 6/6 (2 pre-existing rbac failures unchanged).
# story 6-4 ready-for-dev → review (bmad-dev-story 2026-07-08): External notifications + security hardening + graceful shutdown shipped across all three services + @share2brain/shared, the final Epic 6 story. New packages/shared/src/{logger.ts,notifier/index.ts} (subpath exports, no I/O at construction, notifier never throws/rejects, bounded ≤5s fetch, no-op when disabled); notifications config block + security.rate_limit reshaped to 3 tiers (api/auth/chat) in @share2brain/shared's Zod schema. Backend: helmet mounted first (incl. /health) + trust proxy 1 + 3 express-rate-limit tiers injected via AppOptions.rateLimit (optional, OFF by default — buildTestAppOptions/e2e harness omit it); new packages/backend/src/lifecycle.ts (createGracefulShutdown: bounded server.close/redis.quit/db.end, reentrancy-guarded, optional best-effort notifier on a caught shutdown error) replaces the old stub; new uncaughtException/unhandledRejection handlers (previously absent on the backend). Bot/workers: purely additive notifier wiring into their existing 6.1/6.2 handlers, zero drain-logic changes. docker-compose.yml stop_grace_period:30s on backend/bot/workers. 2 real gotchas found+fixed: helmet's frameguard defaults to SAMEORIGIN not the AC-required DENY; interpolateEnv substitutes ${VAR} across the WHOLE raw config file including comments, so the shipped Share2Brain.config.yml/.example omit the provider credential blocks entirely while disabled (also fixed the previously-stale tracked Share2Brain.config.yml.example). Gate green: lint 0 / 637 unit+web (+37: 6 logger, 8 notifier, 7 config, 8 lifecycle, 6 security-integration net-new plus fixture updates) / build clean (5 pkgs) / 104-106 integration (2 pre-existing unrelated rbac.integration.test.ts "test-guild role leak" failures, confirmed via git diff — packages/backend's rbac files untouched by this story) / 13 e2e chromium unchanged pass count with helmet enabled. No migration, no DDL.
# story 6-3 code review ROUND 2 (2026-07-08 — re-review of the 3 round-1 patches AS NEW CODE, diff isolated against reconstructed pre-patch snapshots; Blind Hunter + Edge Case Hunter, Epic 3 retro AI#1). Both layers converged: all 3 production patches CLEAN, zero regressions (path-traced: abort now guards every fetch incl. first-page + already-aborted; windowCapped correct at </<==/> limit boundaries with intentional full-page asymmetry; fail-safe continue is delete-branch-only so edits untouched). The 2 discriminating regression tests empirically fail against pre-patch. 1 test-quality finding: windowCapped=false test is non-discriminating for the windowCapped patch (pre-patch also returned false for collected<=limit) — DISMISSED as a valid forward-regression guard (the windowCapped=true test is the discriminator). 1 improvement applied: strengthened the P3 fail-safe test to also assert result.edits still fires with an unparseable anchor (proves the guard suppresses deletes but not edits). Gate: lint 0 / 608 unit+web (unchanged, 1 assertion strengthened) / build clean. Convergence — round 3 not warranted. Status stays done.
# story 6-3 review → done (bmad-code-review 2026-07-08 — 3 adversarial layers: Blind Hunter + Edge Case Hunter + Acceptance Auditor, Opus 4.8, over the uncommitted working tree vs baseline 71e84cc). Consensus: the load-bearing delete-conservatism invariant (AC-4/note #5) holds — no path publishes a false-positive delete (empty-fetch, below-window, anchor-exclusion, abort-before-diff all verified correct); content-diff edits, BigInt snowflake ordering (note #7), never-log-content (AC-8), and the main.ts gating/chain/shutdown-drain (AC-1/AC-2/note #10) all confirmed. Auditor: 0 hard AC violations, 9/9 ACs satisfied. 0 decisions + 3 patches applied (all Low severity, all in new code): (P1) windowCapped hardcoded false on a short page that overshoots backfill.limit → falsely claimed full head-of-history coverage (AC-7); now `collected.length > limit` (+2 tests); (P2) abort landing DURING the inter-page throttle fired one extra REST fetch because waitOrAbort resolves (never rejects) and the signal.aborted check sat BEFORE the throttle — moved the check to after the throttle/before the fetch, matching AC-5 "before each page fetch" + the runBackfill template (+1 abort-mid-throttle regression test, closes the AC-5/AC-9 test-gap); (P3) fail-OPEN delete guard when the anchor cursor isn't BigInt-parseable (toIdKey null skipped the `>= lastSeenKey` exclusion → anchor row could be concluded deleted = false hard-purge) — diffChannel now concludes no deletes when lastSeenKey===null (fail-SAFE, +1 test). 1 deferred (offline edit to the newest/anchor message is never detected — deliberate exclusive-`before:lastSeen` design per notes #1/#6; recorded in deferred-work.md for a future full-reconcile story). 5 dismissed as noise (MessageContent-intent-off warn-spam = spec-sanctioned acceptable floor + bounded to edited msgs by the editedAt debug guard, no corruption; non-numeric-id reduce stall = unreachable; inter-channel throttle not reproduced = satisfies literal AC-1; no abort re-check in publish loops = not an AC-5 requirement, bounded/fast + handlers self-catch; backfillPromise reassignment escaping the drain = verified false, it's a `let` read at drain time). Gate re-run green: lint 0 / 608 unit+web (+4 regression tests) / build clean (5 pkgs) / bot-integration 9/9. Same pre-existing unrelated backend RBAC integration flake (2 tests, test-guild role leak — packages/backend untouched, `git diff --stat main -- packages/backend/` empty) — not blocking. Epic 6 fourth/last story 6.4 (notifier/security/graceful-shutdown) is next.
# story 6-3 ready-for-dev → review (bmad-dev-story 2026-07-08): Bot startup offline reconciliation implemented. New packages/bot/src/sync/{reconcile,offlineSync}.ts (pure diffChannel + orchestrator) + unit + integration tests; main.ts chains runOfflineSync onto backfillPromise, gated by sync.enabled && sync.sync_on_start. Found + fixed a real boundary bug during integration testing: the last_seen anchor row is excluded from the Discord re-fetch (before is exclusive) but is still the newest persisted row, so the original delete-window guard (rowKey > lastSeenKey) would have concluded a false delete for it every boot — tightened to rowKey >= lastSeenKey with a regression test. Also fixed a test-isolation defect (hardcoded snowflake ids collided with backfill.integration.test.ts's fixtures under parallel execution) by deriving run-unique ids from a per-run salt. Idempotency (AD-13, AC-6) explicitly asserted via a redundant-republish integration test. No packages/shared change, no migration. Gate green: lint 0 / 604 unit+web (+22) / build clean (5 pkgs) / bot-integration 9/9 (offlineSync 3/3). Same pre-existing unrelated backend RBAC integration flake as 6.2 (packages/backend untouched) — not blocking.
# story 6-2 ready-for-dev → review (bmad-dev-story 2026-07-08): Sync worker consumes discord.message.updated/deleted from the share2brain:sync group (two independent single-stream loops mirroring runIndexer, DECISION 5) on its own Redis client alongside the Indexer. Updates re-index standalone (messageIds=[id], DECISION 1 — no neighbor content to re-group) inside one DB transaction (delete old chunks by ANY(message_ids) → refresh dm.content/updated_at → re-chunk+embed+assert → UPSERT → stamp indexed_at); deletes are soft (deleted_at only, embeddings untouched — D1 anti-join already excludes it) or hard (superset of soft: purge embeddings+read-status FK-safe, then deleted_at, DECISION 3), gated by config.sync.delete_policy. New packages/workers/src/sync/{events,processUpdate,processDelete,consumer,types}.ts + tests; main.ts opens a 2nd Redis client gated by config.sync.enabled and drains both consumer promises on shutdown. No packages/shared change, no migration (discord_messages.deleted_at + embeddings.message_ids already existed). Gate green: lint 0 / 582 unit+web (+35) / build clean (5 pkgs) / 8 workers-integration (+4, real Postgres+Redis, fake embedder, verified idempotent redelivery + FK-safe cascade + D1 anti-join exclusion). Flagged 1 pre-existing unrelated backend RBAC integration-test flake (packages/backend not touched by this story) in the story's Completion Notes — not blocking.
# story 6-3 backlog → ready-for-dev (bmad-create-story 2026-07-08; Epic 6 third story). Startup offline reconciliation — BOT-side, PUBLISH-ONLY (no bot DB write). After the historical backfill settles, the bot walks each enabled channel's most-recent `backfill.limit` messages (backward from the derived `last_seen` = getChannelCursor; NO last_seen_message_id column, AD-5), re-fetches current Discord state, and diffs against persisted discord_messages: content-diff ⇒ edited (D2), absent-within-the-fully-covered-window ⇒ deleted (D3 conservative — id ≥ oldestFetchedId via BigInt, clean-walk only, so delete_policy=hard never purges a live vector). Republishes via the REUSED 6.1 handlers (handleMessageUpdate/handleMessageDelete) into share2brain:discord:messages:{updated,deleted} — the 6.2 Sync worker does the actual mutation (redundant republish = safe no-op, AD-13 convergence). Gated by sync.enabled && sync.sync_on_start (D5), sequential per channel + abortable throttle (reuse backfiller cadence), chained onto backfillPromise + covered by the existing bounded shutdown drain. Naturally catches messageDeleteBulk (deferred out of 6.1) within the window. 2 creation forks confirmed with Borja: (D1) reconciliation depth reuses backfill.limit — NO @share2brain/shared change; (D2) edit detection = content-diff not editedAt-timestamp. New packages/bot/src/sync/{reconcile,offlineSync}.ts + unit + integration tests; main.ts wiring. No packages/shared change, no migration. Last story before 6.4 (notifier/security/graceful-shutdown).
# story 6-1 ready-for-dev → review (bmad-dev-story 2026-07-08): bot now publishes discord.message.updated/deleted to their Redis Streams on live Gateway events (Partials enabled for uncached messages); publish-only, no DB write, no packages/shared change (contracts pre-existed). New channelGuard.ts (extracted, shared by all 3 handlers) + messageUpdate.ts + messageDelete.ts, each with unit tests. Gate green: lint 0 / 542 unit+web (+28 new) / build clean (5 pkgs). Unblocks 6.2 (Sync worker).
# epic-6 flipped backlog → in-progress and 6-1 → ready-for-dev on story creation (2026-07-08)
# story 5-4 code review → done (bmad-code-review 2026-07-08 — 3 adversarial layers: Blind Hunter + Edge Case Hunter + Acceptance Auditor). Auditor: 0 AC violations (AC1-10 + D1-D10 all honored). 8 patches applied, all real bugs in the new code: (1) newChat/selectConversation now abort the in-flight stream (was: a stale `done` frame could silently reassign activeConversationId to an abandoned conversation); (2) selectConversation gained a monotonic conversationLoadIdRef sequencing token (rapid double-clicks on 2 history rows could race); (3) a failed history load now appends a visible errored bubble instead of failing silently; (4) send()'s re-entrancy guard now reads a synchronous sendingRef instead of render-scope `sending` state (closed a narrow double-send race, e.g. Enter key-repeat); (5) onComposerKeyDown now ignores Enter during IME composition; (6) authorInitials is now Unicode-aware (`\p{L}\p{N}`) so accented/non-Latin author names don't collapse to "?"; (7) chat.ts's streamChat now throws instead of silently dropping a leftover unterminated frame when the stream closes early (was: a permanently-blinking cursor); (8) a suggestion-chip send no longer wipes an unrelated in-progress composer draft (new `send(text, clearDraft)` param). 6 dismissed as noise (schema-guaranteed frame-type exhaustiveness, spec-mandated auto-scroll behavior, spec-sanctioned citation placeholder link, unreachable multi-line SSE data: case given the backend's single-line JSON wire contract, no-op reader-lock nitpick, speculative crypto.randomUUID secure-context concern). Gate re-run green: lint 0 / 517 unit+web (unchanged) / build clean (5 pkgs) / 13 e2e chromium (unchanged). Epic 5 fully done (all 4 stories done).
# story 5-4 → review (bmad-dev-story 2026-07-08); branch feat/5-4-chat-messages-streaming-ui, baseline 5f75fd1. Chat streaming UI shipped end-to-end (frontend-only, extends the 5.3 shell). NEW api/chat.ts (streamChat fetch-streaming async generator, AD-4 not EventSource; parses data:<json>\n\n frames, buffers across chunk splits, SSEFrameSchema.parse each, throws typed ChatStreamError on pre-stream non-OK) + fetchConversation (detail client, no shared change — 5.2 exports ConversationDetailSchema) + SendIcon (LockIcon already existed from 2.2). ChatWidget gains: message state (D5), send flow (token→citation→done/error, mutate streaming bubble by id), abort ONLY on unmount not panel close (D6, unit-tested via AbortController.abort spy), launcherActive=sending&&!chatOpen, suggestion→send, history-row load via fetchConversation (system role filtered, closes 5.3 deferral), newChat clears to empty state; user/agent bubbles (agent hexagon avatar reuses CLIP_PATH/AMBER_GRADIENT inline like FAB), amber kh-blink cursor, Fuentes citation chips (placeholder discord href — CitationSchema has no message URL, documented), composer (auto-resize textarea ≤120px, Enter-send/Shift+Enter-newline, state-driven 40x40 send button, privacy footer). ChatWidget gains a required `user:{name,initials}` prop (D4) — App passes the same userIdentity it builds for AppLayout (only App change). Message-area content gated on !chatHistoryOpen (D10 focus-trap parity w/ 5.3 round-3); composer is a sibling below the overlay = legit visible trap boundary, 5.3 trap tests updated to compute focusable set (now incl. textarea). 3 CSS classes (kh-chat-input-row focus-within amber, kh-chat-citation hover blurple, kh-chat-send focus ring; base values in class per cascade rule AI#4). D1 trace panel (UX-DR20 tool_call/observation) DEFERRED — no SSEFrameSchema/graph.ts/shared/backend change; recorded in deferred-work.md as a future backend+shared story. E2E: Story 5.4 describe block reuses gotoChat; streaming test MUTATES (persists conversation) → ordered LAST (D9), no exact-count assertion after; chat.spec no longer read-only (README updated). Gate green: lint 0 / 517 unit+web (+ chat.test, conversations detail cases, 5.4 ChatWidget tests) / build clean (5 pkgs) / 13 E2E chromium (3 new 5.4). No new deps, no migration, no shared/backend change. jsdom gotcha: repo has no jest-dom → used .textContent.toContain not toHaveTextContent.
# story 5-4 ready-for-dev → in-progress (bmad-dev-story 2026-07-08)
# story 5-4 backlog → ready-for-dev (bmad-create-story 2026-07-08; Epic 5 fourth/LAST story). Mensajes de chat y UI de streaming SSE — FRONTEND-ONLY (@share2brain/web), extends the 5.3 shell. Builds: message composer (textarea auto-resize + send 40x40 disabled/amber states + Enter-to-send + privacy footer), message bubbles (user Discord-purple circle / agent amber hexagon avatars, name label, 15px/1.7 text), SSE `token` streaming with the amber kh-blink cursor, citation chips ("Fuentes"), suggestion→send wiring, FAB launcherActive pulse (sending && closed), and HISTORY LOAD (closes 5.3's deferral — GET /api/conversations/:id renders messages). HEADLINE DECISION D1: the "loop de ejecución" execution-trace panel (UX-DR20 tool_call/observation) is DEFERRED — the backend SSE contract (frozen 5.1) emits ONLY token/citation/done/error, there is NO tool_exec node (only a future-extension comment at graph.ts:110), and the prototype's trace steps are setTimeout fakes; building it for real is a backend+shared story (LLM tool-use + configurable tools + conditional graph loop + widen SSEFrameSchema). 10 design decisions. Wire order token*→citation*→done (citations post-graph, graph.ts:166-175). New api/chat.ts (fetch-streaming async generator, AD-4 not EventSource, parses `data:<json>\n\n`), api/conversations.ts +fetchConversation. ChatWidget gains a `user` prop (D4, bubble avatar). E2E determinism via harness fakeChatModel ("Hola desde Share2Brain.") + seeded conversation; streaming test MUTATES → ordered LAST, no exact-count assertions (D9); chat.spec no longer read-only. No new deps, no shared/backend change, no migration, no seed change.
# story 5-3 code review ROUND 3 (2026-07-07 — re-review of the round-2 patches as new code, diff isolated against reconstructed pre-round-2 snapshots of the 2 substantively-changed files). Found 1 more real, currently-reachable focus-trap gap that predates round 2 (traced to round-1's getFocusableElements): the Tab-trap's first/last boundaries could resolve to an empty-state suggestion button hidden behind the history overlay (the empty-state wrapper stayed mounted, just visually covered by the higher-z-index overlay) — reachable by clicking Historial then Shift+Tab. FIXED: the empty-state wrapper now only renders while chatHistoryOpen is false, so getFocusableElements naturally excludes it. Regression test added, verified to fail without the fix (failed even harder than expected, on a duplicate-testid query). Also added a direct test for a theoretical race Blind Hunter raised (the two focus-restore effects, wasOpenRef vs wasHistoryOpenRef, when closeChat flips both chatOpen and chatHistoryOpen in one tick) — investigated via React's commit-phase ref-nulling guarantee (a ref to an unmounting node clears during the synchronous mutation phase, strictly before any passive effect runs) and confirmed already-correct by construction, not by a code change. 2 patches applied, 0 deferred, 7 dismissed as noise (verified false: effect race, per-action-tailored refocus target, failed-restore-no-retry, generic-boundary-derivation, comment-dilution, brace-mismatch, stuck-atStart-on-repeated-press). Gate re-run green: lint 0 / 488 unit+web (+2) / build clean (5 pkgs) / 10 e2e chromium (unchanged pass count). Convergence: round 4 not warranted — remaining findings are hypothetical/future-proofing, not reachable bugs.
# story 5-3 code review ROUND 2 (2026-07-07 — re-review of the 6 round-1 patches as new code, same convention as Epic 3 retro AI#1, diff isolated against reconstructed pre-patch snapshots of the 5 touched files). Blind Hunter + Edge Case Hunter both converged on the same class of regression: the round-1 focus-trap/focus-restore patches had gaps of their own. FIXED: (1) Shift+Tab escaped the trap on the very first press after opening — the panel container itself (not a header button) holds focus right after opening, and getFocusableElements only returns descendants, so the container never matched first/last and the round-1 shiftKey check never fired; the trap now also treats activeElement === panelRef.current as the start-of-panel boundary. (2) Closing the history overlay (Escape/select/newChat) dropped focus to document.body with no restore, defeating the trap on the very next Tab; added a historyBtnRef + wasHistoryOpenRef-guarded effect (mirrors the existing chatOpen/fabRef pattern) that refocuses the historial button on any genuine chatHistoryOpen close. Both regression tests empirically verified to fail without their fix (temporarily reverted and re-ran). 3 more trivial doc/comment patches applied (relativeTimeEs docstring, gotoChat comment wording, a cross-reference comment on a hardcoded test literal). 1 deferred (aria-modal alone doesn't make AppLayout inert — additional hardening outside ChatWidget.tsx). 6 dismissed as noise (unreachable empty-focusable-list branch, already-working single-element case, "tests don't discriminate" disproven by empirical revert-and-rerun, deliberate future-timestamp coverage mischaracterized as scope creep, no-cost recomputation of getFocusableElements, out-of-scope IME/modifier-key handling). Gate re-run green: lint 0 / 486 unit+web (+2) / build clean (5 pkgs) / 10 e2e chromium (unchanged pass count).
# story 5-3 → done (bmad-code-review 2026-07-07 — 3 adversarial layers: Blind Hunter + Edge Case Hunter + Acceptance Auditor, over the uncommitted working tree vs baseline 7d4e42e). Auditor: 0 AC violations (AC1-AC6 faithfully implemented, D1-D6 documented deviations honored, seed SQL cross-checked against packages/shared/src/db/schema.ts). 1 decision resolved with Borja (chat panel role="dialog" has no focus trap/aria-modal — Tab/Shift+Tab could escape into AppLayout behind it → patch now, not deferred) + 6 patches applied: (1) Tab/Shift+Tab focus trap + aria-modal="true" on the panel; (2) relativeTimeEs returns '' instead of throwing RangeError on an unparseable ISO date (NaN duration); (3) Escape closes the history overlay first, then the panel on a second press, instead of always closing the whole widget; (4) ChatWidget.test.tsx's brittle toContain('245') active-row assertion replaced with exact-value equality; (5) relativeTime.test.ts gained week-division + future-timestamp + invalid-date test cases; (6) chat.spec.ts gained the gotoChat(page) helper Task 10 had claimed but never added, used in 3 of 4 tests. 4 deferred (seed.ts reset has no transaction — test-infra only, self-heals on next boot; fetchConversations page/limit=0 falsy-drop — copied verbatim from the story's own Dev Notes template, unreachable in 5.3; history overlay's total never surfaced — no pagination affordance, out of shell scope; conversations.ts generic Error/no JSON-parse guard — matches api/documents.ts convention verbatim). 12 dismissed as noise (seed.ts rows[0] access unreachable given Postgres RETURNING semantics; delete-predicate scope matches the function's own pre-existing convention; title/aria-label mismatch is a non-issue since aria-label wins the accessible name; launcherActive dead code is explicitly directed by Dev Notes D1; AbortError-only catch matches the standardized fetch/AbortController shape; Chromium-only CSS assertions match the harness's Story 4.5 D4 design; --bg/--on-accent equality is confirmed in the story's own token table; spec discovery-order coupling is documented pre-existing harness design; shared-schema shapes independently cross-checked by the Auditor; PlusIcon/CloseIcon strokeLinejoin has no visual effect on disjoint path segments; duplicated geometry constants are only 2 occurrences, no demonstrated collision; post-unmount setState from the history-fetch effect is a harmless no-op under React 18+). Gate re-run green: lint 0 / 484 unit+web (+9) / build clean (5 pkgs) / 10 e2e chromium (unchanged pass count, chat.spec refactored to use gotoChat).
# story 5-3 → review (bmad-dev-story 2026-07-07); branch feat/5-3-widget-flotante-fab-panel-base, baseline 7d4e42e. Chat widget SHELL shipped (FAB + panel + header + empty state + history overlay); composer/bubbles/SSE/trace/citations DEFERRED to 5.4 per scope. Self-contained ChatWidget state (D1, not lifted to App — single sibling after AppLayout). FAB = single amber hexagon reusing exported CLIP_PATH/AMBER_GRADIENT (D4); header logo + empty-state hexagon reuse the Hexagon component. Prototype --tx* → real --text-* mapping (D2); UX-DR16 header brand "Share2Brain"+"Agente de conocimiento" not literal "Chat" (D3). Cascade rule (Epic 4 AI#4): all :hover/active border/color base values in 5 new .kh-chat-* CSS classes, active history row amber tint the one deliberate inline state; suggestion :hover uses --text-primary not #fff (light-theme legibility). a11y: Escape closes, focus into panel on open / back to FAB on close (wasOpenRef guards initial mount). New: api/conversations.ts (list only, detail+SSE=5.4), lib/relativeTime.ts (Intl.RelativeTimeFormat es), ChatWidget.tsx, 4 icons (Chat/History/Plus/Close). E2E (retro AI#6 critical path): seed.ts +1 e2e- conversation for member (FK-order reset messages→conversations→users, never widened) + chat.spec.ts (AC1-5 getComputedStyle + screenshots); README spec discovery-order invariant (chat sorts before docs, read-only). No new deps, no shared-schema change, no migration, no router, no Sidebar/Header/AppLayout prop change. Gate green: lint 0 / 475 unit+web (+31) / build clean (5 pkgs) / 10 e2e chromium (4 new + 6 unchanged).
# story 5-3 ready-for-dev → in-progress (bmad-dev-story 2026-07-07)
# story 5-3 backlog → ready-for-dev (bmad-create-story 2026-07-07; Epic 5 third story, first FRONTEND story of Epic 5). Widget flotante FAB + panel BASE (shell only). SCOPE: FAB launcher (60px single amber hexagon, NOT the 3-layer Hexagon component; amber shadow; chat icon in --on-accent; fixed bottom/right 24, z-60, hover translateY(-2px)) + panel container (404×642, radius 18, kh-pop, border --border-strong, bg --bg) + header (Hexagon logo + "Share2Brain" + "Agente de conocimiento" + 3 icon btns historial/nueva/cerrar) + empty state (60px Hexagon + "Preguntá lo que quieras" + 3 suggestion chips) + conversation-history overlay reading GET /api/conversations. EXPLICITLY DEFERRED to 5.4: message composer (textarea/send/footer), bubble rendering, SSE streaming, execution-trace, citation chips, suggestion→send + history-item→load-messages wiring. 13 design decisions. Key reconciliations: (D2) prototype `--tx*` tokens are STALE → full mapping table to real --text-* names + dark rgb() values for toHaveCSS; (D3) epic AC "título Chat" is loose → UX-DR16/prototype authoritative: header shows brand "Share2Brain"+"Agente de conocimiento", no literal "Chat" heading; (D4) FAB is a single amber hexagon (export CLIP_PATH/AMBER_GRADIENT from Hexagon.tsx for reuse), header-logo/empty-state reuse the Hexagon component; (D1) ChatWidget self-contained state (NOT lifted to App like unread counts), mounts as sibling after <AppLayout> (overflow:hidden → fixed sibling correct). Chat is a FLOATING widget, NOT a sidebar nav item (UX-DR5) — panel opens via FAB. New: api/conversations.ts (list only; detail+chat/SSE=5.4), lib/relativeTime.ts (Intl.RelativeTimeFormat es), ChatWidget.tsx, 4 icons (Chat/Close/History/Plus), 5 kh-chat-* CSS classes (base borders in class per Epic 4 AI#4 cascade rule). E2E (Epic 4 retro AI#6 critical path): extend seed.ts with one e2e- conversation for memberId so history overlay is assertable (FK-safe reset, never widen beyond e2e-) + chat.spec.ts (getComputedStyle AC1-5 + screenshot). No new deps, no shared-schema change (5.2 contracts already exist), no migration, no router, no Sidebar/Header/AppLayout prop change.
# story 5-2 code review ROUND 3 (2026-07-07 — re-review of the round-2 patch as new code, same worktree-diff-isolation technique). Blind Hunter + Edge Case Hunter both converged: the round-2 "propagate abort" test didn't actually discriminate its own fix — respondNode's fake also threw unconditionally, so the test passed whether or not the round-2 guard existed. Empirically confirmed by temporarily deleting the guard and re-running (still passed) — root cause: LangGraph enforces the abort signal at its OWN super-step boundaries independent of node-level try/catch, so respondNode never runs after an abort either way. The guard's real, testable effect is suppressing a misleading "history compression failed" console.error for a plain client disconnect — rewrote the test to spy on console.error and verified (by re-deleting the guard) that this version DOES correctly fail without the fix. Non-obvious LangGraph discovery recorded in the story's Round 3 notes for future abort-related tests. 0 dismissed, 0 deferred this round. Gate re-run green: lint 0 / 444 unit (unchanged, 1 test rewritten) / 93 integration (unchanged) / build clean.
# story 5-2 code review ROUND 2 (2026-07-07 — re-review of the 5 round-1 patches as new code, Epic 3 retro AI#1; diff isolated via a disposable git worktree at baseline + the round-1 diff). Blind Hunter + Edge Case Hunter converged on 1 real regression: round-1's try/catch in reasonNode swallowed AbortError too, not just transient failures — a client disconnect mid-summarization would silently fall back and continue with paid downstream work instead of propagating. FIXED: catch now rethrows when config.signal.aborted. Also caught 2 vacuous round-1 regression tests (fallback test didn't check windowed content was actually used; tiebreak test only proved self-consistency, not the id-ASC order) — both strengthened. 2 new findings deferred (Array.from truncation is code-point- not grapheme-cluster-safe; the id tiebreaker is deterministic but not chronologically meaningful — both need out-of-scope changes, Intl.Segmenter / a monotonic column). 2 dismissed (uncapped fallback payload matches pre-5.2 behavior; LangGraphRunnableConfig import verified already present). Gate re-run green: lint 0 / 444 unit (+1) / 93 integration (unchanged) / build clean. Status stays done.
# story 5-2 → done (bmad-code-review 2026-07-07 — 3 adversarial layers: Blind Hunter + Edge Case Hunter + Acceptance Auditor, over the uncommitted working tree vs baseline 964eae1). Auditor: 0 AC violations, independently re-ran the full verification gate and it matched the Dev Agent Record exactly. 2 decisions resolved with Borja (concurrent getMessages/appendMessage race on the same conversationId → deferred, low-probability, matches Epic 4 TOCTOU precedent; compressIfNeeded error handling → patched, falls back to the memory_window-truncated history instead of failing the whole turn) + 5 patches applied: (1) try/catch fallback around compressIfNeeded in reasonNode; (2) AbortSignal threaded into compress.ts's summarize()/chatModel.stream (mirrors respondNode); (3) deriveTitle truncates by Unicode code point (Array.from) instead of UTF-16 code unit, so an 80-char boundary can't split a surrogate pair/emoji; (4) `, id ASC`/`, id DESC` tiebreakers added to the 3 affected ORDER BY created_at/updated_at clauses in conversationRepository.drizzle.ts; (5) fixed a vacuous compress.test.ts assertion (widened to a genuinely over-budget case). 2 deferred to deferred-work.md (unbounded getMessages fetch per turn; pagination's ~1e8-offset cost, pre-existing documents.ts convention). 8 dismissed as noise (verified false-positives/by-design: ephemeral-compression re-run per D8, toCitations/toIsoString defensive-but-unreachable given schema NOT NULL + jsonb driver guarantees, no-index-yet per the story's own Latest Tech note, controller-local UUID param schema, satisfies compile-time guard, unvalidated role unreachable, CitationType naming). Gate re-run green: lint 0 / 443 unit (+2) / 93 integration (+1) / build clean (5 pkgs).
# story 5-2 backlog → ready-for-dev (bmad-create-story 2026-07-07; Epic 5 second story). Gestión de conversaciones e historial — backend-only, no visual ACs (like 5.1). Read side: GET /api/conversations (paginated, title DERIVED from first user message — no title column, no migration) + GET /api/conversations/:id (ownership-checked 404, chronological messages), mirroring the documents triad. Closes 5.1 D13 (loads persisted history into the prompt, with a load-before-persist ordering guard so runChat doesn't double-append the current message) and adds compressIfNeeded() in the reason node (local COMPRESSION_TOKEN_BUDGET=4000 — no agent.memoryBudget in config per 5.1 D3; drains chatModel.stream() to summarize so the ChatModel port isn't widened; compression ephemeral, not persisted). 11 design decisions incl. extracting a shared CitationSchema (none exists yet). No new deps.
# story 5-1 code review ROUND 2 (2026-07-07 — re-review of the 5 round-1 patches as new code, Epic 3 retro AI#1). Auditor: patches preserve all 12 ACs, 0 regressions. Edge Case Hunter VERIFIED libraries: LangGraph rethrows AbortError out of graph.stream + LangChain honors RunnableConfig.signal → round-1 abort fix works end-to-end. 7 dismissed (signal wiring present+honored, silent-loss-without-done impossible, memoryWindow NaN = config scope). 5 NEW patches for gaps the round-1 patches introduced: skip empty-turn persist; best-effort touchConversation (a timestamp failure was failing committed turns → error frame + duplicate on retry); wrap res.write (sync EPIPE not caught by res.on('error')); symmetric res.end guard + logging; tests for the new guard paths. Gate: lint 0 / 398 unit (+6) / 83 integration / build clean. Status stays done.
# story 5-1 → done (bmad-code-review 2026-07-07 — 3 adversarial layers: Blind Hunter + Edge Case Hunter + Acceptance Auditor, Opus 4.8, over the uncommitted working tree). Auditor: 12/12 ACs SATISFIED. All findings converged on the streaming/error path. 1 decision (orphaned-turn-on-interruption → RESOLVED: persist partial answer) + 5 patches applied: (1) try/finally persist-partial + touchConversation so an interrupted turn never orphans the user message; (2) thread AbortSignal through the ChatModel port → adapter → provider so client disconnect cancels paid generation (was dead-ending at graph.stream); (3) persist assistant BEFORE emitting `done` (was a reload race + `done`-then-`error` contradiction on post-done persist failure); (4) guard res.write against a closed socket + res.on('error') swallow; (5) guard slice(-memoryWindow) against memory_window<=0 (latent for 5.2). 9 findings dismissed (verified false-positives: sse error variant exists, SIBLING_IMPORT_BAN defined + both bans fire; convention: ES/EN message mix, console.error, .parse fail-loud; accepted-by-spec: D13 history deferred to 5.2). 3 regression tests added. Gate re-run green: lint 0 / 392 unit (+3) / 83 integration / build clean (4 pkgs).
# story 5-1 → review (bmad-dev-story 2026-07-07); branch feat/5-1-endpoint-sse-chat-rag, baseline 9b06416. Endpoint SSE de chat con pipeline RAG shipped end-to-end: real LangGraph StateGraph (retrieve→reason→respond), token streaming via LangGraph's custom stream channel (getWriter, per-invocation-scoped — no cross-request state), RBAC-in-query retrieve (reuses searchByEmbedding, inherits AD-12 + deleted-message exclusion for free), conversations/messages persistence, ESLint langchain-legacy ban (proven to fire), spike removed (mountSpikeChatSse + web spec deleted). 4 implementation-time design refinements (D12-D15): LangGraph custom-stream-writer mechanism, chatService split into resolveConversation/streamChat for D8's pre-stream ownership check, history-loading deferred to 5.2 (no list-messages port scoped for 5.1), api-spec.yml 404 doc fix. Gate: lint 0 / 389 unit (+19 new) / 83 integration (+7 new chat SSE cases) / build clean (4 pkgs). Live curl -N exercise confirmed tokens→citations→done frame order on the real endpoint.
# epic-5 backlog → in-progress + story 5-1 backlog → ready-for-dev (bmad-create-story 2026-07-07; first story of Epic 5). Endpoint SSE de chat con pipeline RAG (LangGraph StateGraph retrieve→reason→respond). Mostly composition: reuses SSE schema, provider factory, pgvector RBAC search, conversations/messages tables (already migrated), nginx block (already correct). Net-new: @langchain/langgraph dep, chat request Zod schema, agent/ StateGraph, chatService/controller/route, conversation persistence, ESLint langchain-legacy ban, spike removal. 11 design decisions incl. config-key corrections (TD's knowledge.topK/agent.memoryBudget don't exist → local RETRIEVE_TOP_K=5 + memory_window truncation; summarization deferred to 5.2).
# epic-4 development_status flipped in-progress → done 2026-07-07 (the retro log below claimed done but left the development_status field unflipped; all 5 stories + retrospective were already done).
# epic-4 → done + epic-4-retrospective → done (bmad-retrospective 2026-07-07; epic-4-retro-2026-07-07.md). 5/5 stories done (4-5 added mid-epic via correct-course). Follow-through: 3/3 actionable Epic 3 items landed (deleted_at semantics, view-in-Discord link, treat-patch-as-new-code). 7 new action items recorded, 4 on the critical path before Epic 5's chat UI (fix inline-vs-:hover cascade, prefers-reduced-motion on kh-*, extend harness with chat/SSE specs, SSE end-to-end spike — spike elevated by Borja). Key lesson: a visual AC is not "done" until the harness asserts it (jsdom ignores external CSS → 4.3 shipped unverified → 4.5 harness found a real :focus defect). No re-plan for Epic 5, but a hard prep gate before 5.3/5.4.
# story 4-5 → review (bmad-dev-story 2026-07-07); branch feat/4-5-web-app-e2e-visual-harness, baseline a30406e. Playwright E2E visual-verification harness shipped end-to-end, ALL 8 ACs met, gate green. NEW: packages/backend/src/e2e/{server,seed}.ts (spawned deterministic backend, reset-then-seed 3 ch/5 msg/5 emb/2 read scoped `e2e-`, fake OAuth code→identity, NODE_ENV=production refuses to boot, no auth-bypass route — AD-2 intact, web spawns not imports); packages/web/playwright.config.ts (chromium-only, workers:1, 2 webServers, reuseExistingServer:!CI); tests/helpers/session.ts (loginAs via page.request shared cookie jar, forces dark theme); tests/{search,docs}.spec.ts (getComputedStyle assertions of 4.3 + 4.4 visual ACs, real token names, +full-page screenshots); tests/README.md. MODIFIED: vite.config.ts (NEW preview.proxy + SHARE2BRAIN_API_PROXY_TARGET — vite preview ignores server.proxy), web tsconfig (include tests, node types, drop rootDir), 4 minimal data-testids (similarity-bar/sidebar-badge/search-empty-state/docs-empty-state), root+backend+web package.json scripts, .gitignore, dev-guide (+chromium install prereq). 🔴 REAL 4.3 DEFECT FOUND + FIXED by the harness (the exact gap this story exists for): the search input's :focus border never turned amber — inline `border` shorthand on .kh-search-input outranks the .kh-search-input:focus{border-color} rule (probe: focused, border-color=rgb(42,49,61) base, box-shadow=amber glow DID apply). Fixed the PRODUCT (moved base border to the CSS class so :focus overrides color), not the assertion (Task 9 directive). Related NOT fixed (flagged for review, no AC asserts them): same inline-vs-:hover cascade disables .kh-result-card:hover + .kh-chip:hover border-color. Idempotency: Playwright tears down its webServers each run → next boot reseeds → mutating docs mark-all test safe (3 consecutive green runs). Gate: lint 0 / 360 unit / build clean (4 pkgs) / 76 integration (unchanged, coexists via distinct e2e- roles/channels — no RBAC leak) / test:e2e 4 passed chromium. Blocks 5-3/5-4 now UNBLOCKED.
# story 4-5 → ready-for-dev (bmad-create-story 2026-07-07); Web App — E2E visual-verification harness (Playwright). Baseline a30406e. Cross-cutting test-enablement; blocks 5-3/5-4; initial specs verify 4-3 + 4-4 visual ACs retroactively via getComputedStyle + screenshots. 5 creation decisions (autonomous, within correct-course scope): (D1) e2e backend = packages/backend/src/e2e/server.ts SPAWNED by Playwright webServer[0] — web never imports backend (AD-2); reuses test-helpers (buildTestAppOptions/openTestClients/fakeQueryEmbedder); NODE_ENV=production refuses to start (stronger than a guarded route). (D2) built SPA via `vite build && vite preview` + NEW preview.proxy block (vite preview does NOT inherit server.proxy — without it every /api call 404s) with SHARE2BRAIN_API_PROXY_TARGET env (test backend on :3100, preview on :4173). (D3) fake OAuth maps code→identity (e2e-member / e2e-empty): search has NO similarity threshold, so 4.3-AC6 empty state is only reachable with an empty-embedding channel scope (e2e-ch-void). (D4) chromium-only, workers:1 (specs share one seeded DB; docs spec mutates via mark-all — runs last), dark theme forced via addInitScript localStorage share2brain-theme (light theme changes --accent-ink #F5A623→#9A5B00 → OS-preference flake). (D5) @playwright/test ^1.61.1 (the old "already installed" comment was false, but 1.61.1 is the correct current version to install). Session bootstrap: page.request (shared cookie jar) drives login→state→callback, maxRedirects:0. Story carries the full computed-value table (box-shadow serializes color-first; grid 1fr resolves to px), exact selectors (data-read is the only data attr; chips have no modifier class → assert computed bg) and verbatim Spanish strings. Docs §3.4/frontend-standards/dev-guide already amended by the correct-course — do NOT rewrite. All process docs verified AMENDED on disk before authoring.
# story 4-4 → ready-for-dev (bmad-create-story 2026-07-07); Web App — vista Documentos, read-tracking UI y sidebar badge. Thin FULL-STACK (bulk = DocsView, + small additive backend). 2 creation decisions with Borja: (D1) filtering is SERVER-SIDE — add channelId?+unreadOnly? to DocumentsQuerySchema + repo/service (channelId = narrow allowedChannelIds to [channelId] in service, zero repo-sig change for it, AD-12; unreadOnly = `urs.embedding_id IS NULL` in listDocuments AND countDocuments — countDocuments gains userId+unreadOnly). Reason: /api/documents is PAGINATED (unlike /api/search), so client-side chip/unread filtering over loaded pages is broken; filter change resets to page 1 + refetch. (D2) row click = MARK-READ ONLY (one-way, optimistic dot→grey; DELETE/unmark endpoint out of scope). Unread state LIFTED to App.tsx (fetchUnreadCount map → totalUnread badge in Sidebar (already renders badge, just needs the prop) + per-channel map + refreshUnread cb → DocsView for "Sin leer · N" & mark-all). New: web api/documents.ts + api/readStatus.ts (no DELETE) + DocsView.tsx + CheckIcon. NO migration, NO app.ts wiring change, NO schemas/index.ts change. Carries 4.3 token-rename table (--tx*→--text-*; --line/--hover-row/--dot-read confirmed present) + 4.2/4.3 test-isolation gotcha (run-unique role/channel suffix, own-id cleanup, seed user_read_status). VISUAL ACs (dot glow, grid, fonts) unverifiable here (jsdom + no browser) → covered retroactively by Story 4.5 harness; flag as deferred, don't claim verified.
# FORMALIZED as Story 4-5 (bmad-correct-course 2026-07-07, sprint-change-proposal-2026-07-07.md): E2E VISUAL-VERIFICATION HARNESS (Playwright). Closes the recurring gap where frontend stories (4-3 done, 4-4 docs view, Epic 5 chat widget) cannot verify visual/CSS ACs — no browser automation + jsdom ignores external CSS. CORRECTION: Playwright is NOT installed (not in any package.json, not in node_modules, no bin) — the story adds @playwright/test from scratch. Placement decided with Borja: harness = 4-5 (appended after 4-4, NO renumber); 4-4 docs view ships next under the mandatory-steps §3.4 fallback flag and is verified retroactively; 4-5 blocks 5-3/5-4. Scope: (1) @playwright/test + playwright.config.ts + real `test:e2e` script; (2) harness boots createApp with an INJECTED fake DiscordOAuthClient (same pattern as *.integration.test.ts, opts.oauth) + a deterministic fake queryEmbedder, over test Postgres+pgvector/Redis seeded with channel_permissions + embeddings so /api/search returns fixed results; (3) browser session bootstrap — drive /api/auth/login → fake OAuth callback to obtain the session cookie (recommended: no prod auth-bypass route; a test-only route guarded to non-prod only as fallback); (4) point Vite preview at the test backend; (5) initial spec covers 4-3 AND 4-4 retroactively via getComputedStyle: 4-3 Búsqueda (AC1 Space Grotesk 600/25px title + 54px search bar, AC2 focus border-color var(--accent-ink) + box-shadow 0 0 0 3px rgba(245,166,35,0.12), AC4 badge/similarity-bar-gradient/avatar tokens, AC5 active/inactive chip styles, AC6 empty state) + 4-4 Documentos (table grid 1fr/130px/130px/96px, unread dot amber+glow 0 0 0 3px rgba(245,166,35,0.16) vs read var(--dot-read), row hover var(--hover-row), sidebar badge, "todo leído" empty state) — plus screenshots as CI artifacts. Token-name gotcha: assert the REAL names (--text-primary/-muted/-subtle), renamed in Story 2.1 from --tx/--tx4/--tx5. Blocker context: the SPA gates on a Discord OAuth session (App.tsx → /api/auth/me → LoginScreen → full-page /api/auth/login), so real-Discord login is impossible headless; fake-OAuth injection is the only viable path. Author via bmad-create-story before scheduling.
# story 4-3 → review (bmad-dev-story 2026-07-07); branch feat/4-3-web-app-vista-busqueda, baseline c7ef787. First frontend story of Epic 4; thin full-stack (SearchView + 2 small backend additions). Backend: GET /api/channels (RBAC array-overlap, mirrors search controller/router + rbacService.getAllowedChannels + repo.findAllowedChannels) + guildId on GET /api/auth/me (closes Epic 3 retro action item "view-in-Discord link convention before 4.3"). Frontend: SearchView.tsx — 250ms-debounced query with AbortController cancelling in-flight requests, client-side channel-chip filtering (no channelId param on /api/search), result cards (badge/date/similarity-bar/content/avatar/discord-link), dashed empty state; authorColor.ts (deterministic char-sum hash over fixed palette); mounted in AppLayout replacing the search placeholder (docs placeholder untouched, Story 4.4). No DB migration. Gate green: lint 0 / 327 unit (+14) / build clean / 71 integration (+3), plus a manual smoke of the real backend slice (real Postgres+Redis, fake OAuth) confirming guildId + RBAC-scoped channels + 401. GAP: real-browser visual verification (AC1/AC2/AC4/AC5 exact fonts/box-shadow/token colors) NOT performed — no browser-automation tool or real Discord credentials in this environment; flagged for Borja to check manually before merge.
# story 4-3 → ready-for-dev (bmad-create-story 2026-07-07); Web App — vista Búsqueda. First frontend story of Epic 4; thin FULL-STACK (bulk = SearchView, + 2 small backend additions decided with Borja). 3 creation decisions: (1) channel filter chips → NEW endpoint GET /api/channels ({channels:[{id,name}]}, RBAC array-overlap inside query, mirrors search controller/router + new rbacService.getAllowedChannels + repo.findAllowedChannels; chip FILTER stays client-side since /api/search has no channelId param); (2) "ver en Discord" link guildId → add guildId to AuthMeResponse (least wiring: getMe already has guildId in deps; SPA already fetches /me) — closes Epic 3 retro action item "view-in-Discord link convention before 4.3" (linked msg = anchor message_ids[0] = SearchFragment.messageId, D2 from 4.1); (3) author → render authorName as-is (=authorId today, 4.1 deferred display-names) + initials + deterministic authorColor, forward-compatible. 🔴 CRITICAL dev note: mockup/epic tokens --tx/--tx4/--tx5 were RENAMED by Story 2.1 to --text-primary/-muted/-subtle (same values) — full mapping table in story. No router (replace search placeholder in AppLayout), no data lib (useState+fetch+AbortController+250ms debounce, q≥2 chars gate), no migration. Carries 4.2 test-isolation gotcha (run-unique role/channel suffix + own-id cleanup for the /api/channels integration test that asserts full RBAC scope). Exhaustive analysis: 4 Explore subagents (web state, search contracts, backend integration points) + read mockup + all touched frontend files.
# story 4-2 → done (code review round 2, fresh independent pass over the round-1 patches 2026-07-06 — Blind + Edge + Acceptance Auditor; all 4 round-1 patches verified clean, both deferrals (2a anchor-JOIN asymmetry, TOCTOU) confirmed still-deferred, Auditor 7/7 AC satisfied 0 violations. 1 patch: api-spec `page` param gained `maximum: 1000000` to match the code cap P2 added (mirrors `limit` maximum:100). 2 dismissed: Blind's "page-cap rationale is false" (false positive — offset only stays ~1e8 BECAUSE of the cap; uncapped page≈1e18 overflows int8→500, Edge confirmed); countDocuments omitting the channel_permissions join (unreachable — allowedChannelIds derives from channel_permissions). No code/test change; gate unchanged: lint 0 / 300 unit / build clean / 68 integration.)
# story 4-2 → done (bmad-code-review 2026-07-06 — 3 adversarial layers: Blind Hunter + Edge Case Hunter + Acceptance Auditor; 0 hard AC violations, AD-12 RBAC-in-query + idempotency + D1 all verified). 2 decisions resolved with Borja: D1 channelId:null → 1b (drop `nullable:true` from api-spec; TS client only omits) not accept-null; D2 count/unread-count/mark-all lack the anchor INNER JOIN that listDocuments has → DEFER to Epic 6 (divergence only materializes with hard-delete of embeddings; anchors always exist today; spec prescribes count without JOINs; revisit with the same-family TOCTOU). 4 patches applied + gate re-run green: (P1) drop api-spec `nullable:true` on mark-all channelId; (P2) cap `page` at 1_000_000 in DocumentsQuerySchema so a huge page can't overflow the bigint OFFSET into a 500 (+2 regression tests); (P3) add missing `400 BadRequest` response to mark-all in api-spec; (P4) fix stray `-- D2: anchor = message_ids[0]` comment (code uses message_ids[1]). 1 deferred (TOCTOU markRead → FK 500, Epic 6 hard-delete family) + 1 dismissed (mark-all re-scans read rows — correct & deliberate, rare action). Gate green: lint 0 / 300 unit (+2) / build clean / 68 integration.
# story 4-2 → review (bmad-dev-story 2026-07-06); branch feat/4-2-backend-documents-read-tracking, baseline 11c240e. 2 hexagonal slices (documents, read-status) mirroring 4.1 exactly. 5 endpoints wired: GET /api/documents (isRead via LEFT JOIN user_read_status, D1+D4), POST/DELETE /api/read-status/:embeddingId (idempotent, D5), POST /api/read-status/mark-all (2-CTE keyset batch of 1000, D6), GET /api/read-status/unread-count (per-channel map, D7). No migration. GOTCHA found: max(uuid) is not a valid Postgres aggregate — mark-all cursor uses ORDER BY id DESC LIMIT 1 instead. Pre-existing test-isolation bug found+fixed: auth.integration.test.ts/rbac.integration.test.ts cleaned up with a broad `discord_id LIKE 'itest-%'`, racing with every itest-* suite; dormant until this story's user_read_status FK write made the race visible as a 500. Also found: RBAC expansion resolves against the WHOLE channel_permissions table (not test-scoped) — a shared literal role like 'member' leaks other suites' channels into scope; tests acting on the FULL scope (mark-all) need a suffix-unique role. Gate green: lint 0 / 298 unit (+53) / build clean / 68 integration (+35: 6 documentRepository + 13 readStatusRepository + 7 documents e2e + 9 readStatus e2e), run twice with no flakiness.
# story 4-2 → in-progress (bmad-dev-story 2026-07-06)
# story 4-2 → ready-for-dev (bmad-create-story 2026-07-06); backend documents + read-tracking API — 2 parallel hexagonal slices (documents, read-status) mirroring the 4.1 search slice. 5 endpoints: GET /api/documents (paginated, isRead via LEFT JOIN, RBAC+D1), POST/DELETE /api/read-status/:embeddingId (idempotent), POST /api/read-status/mark-all (batch 1000), GET /api/read-status/unread-count (per-channel map). KEY resolved unknown: req.session.userId IS the app users.id UUID (authService.upsertByDiscordId) — direct FK for user_read_status.user_id, no Discord→app mapping. NO migration/schema change (user_read_status already in schema.ts, composite PK backs ON CONFLICT DO NOTHING). Carries 4.1 D1 (exclude-if-any deleted, NOT EXISTS) + D2 (anchor=message_ids[0], authorName=authorId). New decisions: D3 DocumentFragment=SearchFragment−similarity+indexedAt+isRead; D4 pagination page/limit + ORDER BY created_at DESC,id DESC + total; D5 POST 404-on-not-visible (no existence leak) / DELETE idempotent 200; D6 mark-all channelId OPTIONAL (absent⇒all scope, out-of-scope⇒403); D7 unread-count = bare {channelId:count} map (api-spec.yml {unread} is stale, fix in-story). All 3 questions resolved with Borja 2026-07-06 as recommended (D5 404-undiff confirmed; D6 mark-all scope-based not embeddingIds[] — pagination makes client-side enumeration unfeasible; D7 per-channel map confirmed). Reuse only sql+inArray from shared/db (no new drizzle-orm re-export). Route ordering gotcha: /unread-count + /mark-all before /:embeddingId.
# story 4-1 → done (bmad-code-review 2026-07-06 — 3 adversarial layers: Blind Hunter + Edge Case Hunter + Acceptance Auditor; 2 decisions resolved with Borja + 5 patches applied + 5 dismissed; Auditor found 0 hard AC violations). Decisions: anchor JOIN → keep INNER JOIN + document + test (anchorless drop is intentional; LEFT-JOIN+degrade deferred to Epic 6 hard-delete); limit/400 → differentiate the message by failing field (q keeps "Query requerida" AC4; limit gets its own). Patches: P1 assertUsableQueryVector guard (all-zero → NaN cosine similarity=1 garbage search; non-finite → malformed ::vector 500 — the Story 3.0 net that width-only assert missed); P2 SEARCH_QUERY_MAX_LENGTH=1000 cap on q (cost/DoS); P3 new queryEmbedder.langchain unit tests (blind spot: adapter was untested); P4 anchor-absent integration test; P5 field-attributed 400 message. Gate green: lint 0 / 245 unit (34 files) / build clean / 33 integration (8 files).
# story 4-1 → review (bmad-dev-story 2026-07-06); branch feat/4-1-backend-search-api, baseline cf1054b. First pgvector similarity query in the repo; AD-12 RBAC-inside-query made concrete (inArray filter + cosine <=> ordering; proven by a denied fragment identical to the query never surfacing). D1 exclude-if-any via NOT EXISTS anti-join; D2 anchor = message_ids[0] + authorName = authorId. No migration. New slice: shared/schemas/search.ts + backend domain ports (embeddingSearchRepository, queryEmbedder) + Drizzle/LangChain adapters + searchService + searchController + searchRoutes, wired in app.ts/main.ts (injected queryEmbedder, fake in test-helpers). GOTCHAs: query vector bound as JSON.stringify(vec)::vector; drizzle sql expands JS arrays into comma params so RBAC uses re-exported inArray. Gate green: lint 0 / 237 unit / build clean / 32 integration (10 new: 6 adapter + 4 endpoint). Deferred: real author display-name/avatar (no data source; before 4.3).
# story 4-1 → ready-for-dev (bmad-create-story 2026-07-06); epic-4 backlog → in-progress (first story of epic 4). First pgvector similarity query in the repo; AD-12 RBAC-inside-query made concrete. 2 design decisions resolved with Borja at creation: (D1) deleted_at exclusion = EXCLUDE-IF-ANY (anti-join embeddings.message_ids → discord_messages, chunk content physically contains deleted text); (D2) response author/anchor = message_ids[0] anchor for messageId/authorId/createdAt + authorName falls back to authorId string (no display name persisted anywhere — real display-name/avatar deferred as epic follow-up before 4.3). No migration (read-only over embeddings/discord_messages/channel_permissions). Query embed via createEmbeddingsModel(config.embeddings)+assertEmbeddingDimensions; empty allowedChannelIds short-circuits to [] (inArray throws on empty); query vector bound as JSON.stringify(vec)::vector literal.
# epic-3 → done + epic-3-retrospective → done (bmad-retrospective 2026-07-06; epic-3-retro-2026-07-06.md). 4/4 stories done; FR5 closed; unblocks Epic 4. 5 action items recorded. Borja: review was a valuable safety net; Epic 4 discoveries handled as design notes for Story 4.1 (no dedicated design session).
# story 3-3 → done (code review round 2, fresh adversarial pass over the 8 round-1 patches, 2026-07-06 — 0 decisions + 6 more patches: RawStreamEntry.message type widened to admit null; chunk_size upper-bound cap added (mirrors grouping_window); Redis-connect-timeout exit(1) now guarded against a concurrent shutdown; shutdown's swallowed indexerPromise rejection now logged instead of silently discarded; both size caps now warn once at boot instead of clamping silently; test-coverage nits fixed (misleading comment, window-cap boundary, 3x-duplicate). Several round-1-reviewer claims verified as false positives and dismissed (a theoretical TDZ crash, a NaN-clamp concern, a boot-time client-teardown race) — none reproduced against real process/promise semantics. Gate green: lint 0 / 213 unit / 22 integration / build clean)
# story 3-3 → done (code review 2026-07-06 — 0 decisions + 8 patches applied: dedup duplicate messageId before grouping to stop a cross-window chunk_key collision from silently overwriting already-embedded content; null-message guard for tombstoned PEL entries (was an infinite crash loop); chunk_overlap/chunk_size clamped before the splitter; embedder vector/chunk count-mismatch guard; SIGTERM/SIGINT + uncaught handlers moved before all long-running boot work; shutdown no longer races an unguarded indexerPromise rejection; per-stage failure logging (chunk/embed/persist); grouping_window capped at 50; 6 new regression tests; 5 dismissed as noise; gate green: lint 0 / 209 unit / 22 integration / build clean)
# story 3-3 → review (bmad-dev-story 2026-07-06 — gate green: lint 0, 203 unit, build 5 clean, 22 integration; real-endpoint smoke: #general event → 1536-dim embeddings row + chunk_key + indexed_at stamped + XPENDING 0; redelivery deduped, count unchanged. Schema chunk_key + migration 0002 (spike-confirmed 1536); PEL-as-DLQ decision recorded)
# story 3-3 → in-progress (bmad-dev-story 2026-07-06)
# story 3-3 → ready-for-dev (bmad-create-story 2026-07-06); comprehensive context guide created — first Redis Streams consumer (share2brain:indexer, PEL replay + XREADGROUP loop), chunk_key unique column for idempotent UPSERT (shared migration), dimension-mismatch blocker flagged (config 4096 vs migrated vector(1536), pgvector HNSW ≤2000 dims); 4 open questions RESOLVED with Borja same day: dimensions 1536 (MRL, spike-gated), chunk_key + onConflictDoUpdate, per-batch by-channel grouping, chars/4 token heuristic
# story 3-2 → done (4th code review, fresh independent pass 2026-07-06 — 1 decision resolved (accept retry-amplified COMMIT-after-XADD race from Story 3.1, documented in persistMessage.ts) + 2 patches applied: cursor-resolution failure now skips the channel instead of downgrading to the bounded limit path, duplicate-channel-id check scoped to enabled channels only; 3 new tests; Acceptance Auditor found nothing new; gate green: lint 0 / 165 unit / 18 integration / build clean)
# story 3-2 → done (3rd code review, fresh independent pass 2026-07-06 — 0 decisions + 6 patches applied: BigInt('') empty-id fix in the 2nd pass's own sort fallback, distinguishable channel-not-found vs not-text-based errors, messagesFailed counter added to BackfillCompletedEvent, duplicate channel id validation in config schema, 4 new unit tests for previously-untested retry/cursor/abort paths; 1 deferred (SIGTERM/SIGINT registered late, pre-existing); 5 dismissed as noise; gate green: lint 0 / 162 unit / 18 integration / build clean)
# story 3-2 → done (2nd code review, fresh independent pass 2026-07-06 — 1 decision resolved (bounded per-message retry) + 9 patches applied: retry-before-giving-up, abort recheck on inter-channel throttle, throttle dedup, abort-safe channel bookkeeping, transitive comparator, cursor type-mismatch surfaced, partial-progress accounting, accurate persisted-vs-skipped log, bounded shutdown wait for in-flight backfill, plus a latent build error from the 1st pass fixed; 5 dismissed as noise; gate green: lint 0 / 158 unit / 18 integration / build clean)
# story 3-2 → done (code review passed — 2 decisions resolved + 11 patches applied: BigInt safety, whitespace guards, cursor type guard, full comparator, inter-channel throttle, cursor failure isolation, chunk abort check, schema max, inner abort check, per-message isolation, XADD error logging; gate green after patches: lint 0 + 56 unit + build clean; 8 test files, 56 tests passed)
# story 3-2 → review (bmad-dev-story — gate green: lint 0, 158 unit, build clean, 18 integration; smoke: initial backfill 60 msgs/2 channels, gap reconciliation 3 msgs no dupes, failure isolation with completed event channelsFailed=1)
# story 3-2 → in-progress (bmad-dev-story)
# story 3-2 → ready-for-dev (bmad-create-story 2026-07-06); comprehensive context guide created — cursor derived from newest discord_messages row (no last_seen column, per 3.1 decision), idempotent persistMessage (resolves 3.1 deferred onConflict finding), new BackfillCompletedEvent in shared
# story 3-1 → done (3rd code review 2026-07-06 — fixed that fail-fast was dead code: node-redis reconnectStrategy always returns a number so connect() never rejects; R1 bound initial connect with 10s timeout→exit(1), R2 bound client.destroy, R3 .catch on raced quit/end, R4 .catch on waitOrAbort; gate green)
# story 3-1 → done (2nd code review 2026-07-06 — 4 more patches: await+bound redis.quit, abortable backoff sleep, defensive handler try/catch, fail-fast on failed initial Redis connect; 2 decisions dismissed, 3 deferred; gate green)
# story 3-1 → done (code review passed — 7 patches applied: redis.quit, await connect, shutdown timeout, empty content guard, ClientReady on, abort signal for retry, console.error + stack)
# story 3-1 → review (bmad-dev-story)
# story 3-1 → in-progress (bmad-dev-story)
# story 3-1 → ready-for-dev (bmad-create-story); comprehensive context guide created
# story 3-0 → done (code review passed — 5 patches applied: schema validation, type signatures, test coverage, URL format, error messages)
# story 3-0 → review (bmad-dev-story)
# story 3-0 → in-progress (bmad-dev-story)
# story 3-0 → ready-for-dev (bmad-create-story); epic-3 backlog → in-progress (first story of epic 3)
# story 3-0 added (correct-course sprint-change 2026-07-05): provider config LLM/embeddings — blocks 3-3
# epic-2 → done (all 4 stories + retrospective complete)
# epic-2-retrospective → done (bmad-retrospective; epic-2-retro-2026-07-05.md)
# story 2-4 → review (bmad-dev-story)
# story 2-4 → in-progress (bmad-dev-story)
# story 2-4 → ready-for-dev (bmad-create-story)
# story 2-3 → done (code review passed and patches applied)
# story 2-3 → review (bmad-dev-story)
# story 2-3 → in-progress (bmad-dev-story)
# story 2-3 → ready-for-dev (bmad-create-story)
# story 2-2 → done (code review passed and patches applied)
# story 2-2 → review (bmad-dev-story)
# story 2-2 → in-progress (bmad-dev-story)
# story 2-2 → ready-for-dev (bmad-create-story)
# story 1-1 → done (code review passed and patches applied)
# story 1-3 → done (code review passed and patches applied)
project: share2brain
# story 1-3 → review (bmad-dev-story)
project_key: NOKEY
tracking_system: file-system
story_location: _bmad-output/implementation-artifacts

development_status:
  # ── Épico 1: Fundación del Sistema ──────────────────────────────────────────
  epic-1: done
  1-1-inicializar-el-repositorio-y-la-estructura-del-monorepo: done
  1-2-implementar-packages-shared-el-kernel-de-dominio: done
  1-3-docker-compose-servicios-base-y-endpoint-health: done
  epic-1-retrospective: done

  # ── Épico 2: Acceso y Autenticación ─────────────────────────────────────────
  # Re-opened 2026-07-11 via bmad-correct-course (sprint-change-proposal-2026-07-11-guest-access.md):
  # additive guest-access story (2-5) for demos. Config-gated (guest_access.enabled: false by
  # default → POST /api/auth/guest returns 404), synthetic `guest` role expanded via RBAC (AD-12
  # intact), seeded guest `users` row (discord_id="guest") satisfies conversations.user_id FK for
  # guest chat, session carries isGuest (AD-10, no sessions table). No existing story re-work.
  # 2026-07-12: flipped to done via bmad-correct-course housekeeping (i18n SCP §4/P6) —
  # story 2-5 converged done on re-review #4 (2026-07-11); the epic had been left open.
  # 2026-07-15: re-opened done → in-progress via bmad-create-story to append story 2-6
  # (login demo Discord invite link, SCP 2026-07-15). Additive, backward-compatible; no
  # existing story re-work. Same append precedent as story 2-5 (guest access).
  epic-2: in-progress
  2-1-sistema-de-diseno-en-packages-web: done
  2-2-layout-principal-sidebar-y-pantalla-de-login: done
  2-3-backend-discord-oauth2-y-sesiones-en-redis: done
  2-4-rbac-proteccion-de-rutas-y-conexion-ui: done
  2-5-acceso-de-invitado-para-demos: done
  2-6-web-backend-enlace-invitacion-discord-demo-login: done
  epic-2-retrospective: done

  # ── Épico 3: Pipeline de Indexación de Conocimiento ─────────────────────────
  epic-3: done
  3-0-config-proveedores-llm-y-embeddings: done
  3-1-discord-bot-conexion-al-gateway-y-listener-messagecreate: done
  3-2-discord-bot-backfill-historico-con-reconciliacion-por-snowflake: done
  3-3-workers-indexer-embeddings-y-pgvector: done
  epic-3-retrospective: done

  # ── Épico 4: Búsqueda, Documentos y Read Tracking ───────────────────────────
  epic-4: done
  4-1-backend-api-de-busqueda-semantica: done
  4-2-backend-api-de-documentos-y-read-tracking: done
  4-3-web-app-vista-busqueda: done
  4-4-web-app-vista-documentos-read-tracking-ui-y-sidebar-badge: done
  # 4-5 added by bmad-correct-course 2026-07-07 (sprint-change-proposal-2026-07-07.md): cross-cutting E2E test-enablement; blocks 5-3/5-4; initial spec verifies 4-3 + 4-4 visual ACs retroactively.
  4-5-web-app-e2e-visual-verification-harness: done
  epic-4-retrospective: done

  # ── Épico 5: Agente RAG y Chat ───────────────────────────────────────────────
  epic-5: done
  5-1-endpoint-sse-de-chat-con-pipeline-rag: done
  5-2-gestion-de-conversaciones-e-historial: done
  5-3-widget-flotante-fab-panel-base: done
  5-4-mensajes-de-chat-y-ui-de-streaming-sse: done
  epic-5-retrospective: done

  # ── Épico 6: Sincronización, Notificaciones y Fiabilidad ────────────────────
  epic-6: done
  6-1-bot-deteccion-de-ediciones-y-borrados-en-tiempo-real: done
  6-2-worker-sync-re-indexacion-y-purgado: done
  6-3-sincronizacion-al-arranque-y-reconciliacion-offline: done
  6-4-notificaciones-externas-seguridad-y-graceful-shutdown: done
  epic-6-retrospective: done

  # ── Épico 7: Índice Curado de Recursos con IA ───────────────────────────────
  # Added 2026-07-09 via bmad-correct-course (sprint-change-proposal-2026-07-09.md).
  # Post-roadmap product pivot: index ONLY messages containing a URL, one embedding
  # per URL enriched with AI title+description, stored with its link; drop `content`
  # + grouping/chunking. Approved by Borja incl. destructive migration + full re-index.
  epic-7: done
  7-1-shared-modelo-datos-contratos-config-enriquecimiento: done
  7-2-workers-indexer-extraccion-urls-fetch-ssrf-generacion-ia: done
  7-3-workers-sync-reindexacion-por-diff-de-links-y-purgado: done
  7-4-backend-proyeccion-search-documents-rag-citas-seed: done
  7-5-web-searchview-docsview-citas-title-description-link: done
  7-6-e2e-extender-harness-visual-campos-nuevos: done
  epic-7-retrospective: done

  # ── Épico 8: UX Polish ──────────────────────────────────────────────────────
  # Added 2026-07-10 via bmad-correct-course (sprint-change-proposal-2026-07-10.md).
  # Post-roadmap UX refinement: DocsView read/unread visual language was inverted
  # (dim-the-read → "disabled" feeling) vs the updated design Share2Brain Web.dc.html.
  # Frontend-only, Moderate. Approved by Borja incl. 6-column layout.
  # Reopened 2026-07-13 via bmad-correct-course (sprint-change-proposal-2026-07-13-docsview-documento.md):
  # design refinement — título + descripción merge into a single "documento" column (title on line 1
  # + "Nuevo" badge if unread, description stacked below), grid 6→5 tracks. Frontend-only, Moderate.
  # No schema/contract/backend; DocumentFragment unchanged. epics.md UX-DR12/13 + Historia 8.2 synced.
  epic-8: in-progress
  8-1-web-docsview-rediseno-estados-leido-no-leido-y-layout-columnas: done
  8-2-web-docsview-columna-documento-unificada: done
  epic-8-retrospective: optional

  # ── Épico 9: Estadísticas del Conocimiento (Analytics) ──────────────────────
  # Added 2026-07-10 via bmad-correct-course (sprint-change-proposal-2026-07-10-stats.md).
  # New feature: Stats view (3rd nav) per design Share2Brain Web.dc.html (isStats) —
  # KPIs + 14-day indexing activity + per-channel volume + read coverage. Aggregates
  # over existing data (no new ingestion, no new table). Moderate. AD-12 RATIFIED:
  # every aggregation filters by allowedChannelIds inside the SQL. Single GET /api/stats.
  epic-9: done
  9-1-shared-backend-contrato-stats-endpoint-agregacion-rbac-scoped: done
  9-2-web-statsview-tercera-entrada-nav: done
  9-3-e2e-extender-harness-visual-estadisticas: done
  # Added 2026-07-10 via bmad-correct-course (sprint-change-proposal-2026-07-10-topusers.md):
  # "Top 5 usuarios" promoted from the 9.1 review (D9) to its own layered pair. Sequence
  # (binding): 9-4 -> 9-5 -> (9-2 render, 9-3 e2e). author_name captured in the Bot, nullable +
  # COALESCE(author_name, users.username, author_id) fallback (no backfill). No new FR (FR24/FR25).
  9-4-bot-shared-captura-author-name-discord-messages: done
  9-5-shared-backend-bloque-topusers-stats: done
  epic-9-retrospective: done

  # ── Épico 10: Internacionalización de la UI (i18n) ──────────────────────────
  # Added 2026-07-12 via bmad-correct-course (sprint-change-proposal-2026-07-12-i18n.md):
  # UI language via Share2Brain.config.yml (ui.language, optional block, absent ⇒ es),
  # runtime GET /api/ui-config (no auth, general rate-limit tier), react-i18next +
  # static es/en JSON resources bundled (sync init). New FR26. Layered pair
  # (shared+backend → web), precedent 9-4/9-5. Critical guard: default es keeps the
  # 28 e2e + unit Spanish-literal asserts byte-identical. Binding sequence: 10-1 → 10-2.
  epic-10: in-progress
  10-1-shared-backend-config-ui-language-endpoint-ui-config: done
  10-2-web-react-i18next-extraccion-literales-locales-json: done
  epic-10-retrospective: optional

  # ── Épico 11: Responsive & Refresh Visual del Diseño Web ────────────────────
  # Added 2026-07-13 via bmad-correct-course (sprint-change-proposal-2026-07-13-responsive-refresh.md):
  # the updated design Share2Brain Web.dc.html introduces a full responsive system (breakpoint 760px,
  # mobile bottom-nav, adaptive header/padding/chat, safe-area) — 0 before — plus a refreshed design-
  # token palette + hover/focus states. Web impl was desktop-only (only @media = prefers-reduced-motion).
  # Frontend-only, Moderate, additive. No schema/contract/backend (AD-3 + AD-6 intact). No new dependency
  # (useIsMobile over matchMedia). New FR27. Binding sequence: 11-1 -> 11-2 -> (11-3, 11-4) -> 11-5.
  epic-11: in-progress
  11-1-web-refresh-design-tokens-estados-interaccion: done
  11-2-web-useismobile-shell-responsive-applayout-header: done
  11-3-web-adaptacion-responsive-vistas-search-docs-stats: done
  11-4-web-chat-widget-responsive-fab-panel: done
  11-5-web-e2e-harness-visual-movil-tema-claro: done
  epic-11-retrospective: optional

  # ── Operational backlog (post-roadmap) — no epic; see operational-backlog.md ─
  # Numbered ops-N to stay outside the epic sequence (Borja chose an explicit backlog
  # over a hardening epic at the Epic 6 retro, 2026-07-08). Not auto-discovered by
  # create-story/dev-story (they scan number-number keys); invoke these explicitly.
  ops-1-maxlen-stream-trimming: done
  ops-2-integration-test-isolation: done
  # Added 2026-07-10 via bmad-correct-course (sprint-change-proposal-2026-07-10-rename-share2brain.md):
  # full behaviour-preserving rename hivly/KeepHive → share2brain/Share2Brain. Single atomic PR
  # (chore/rename-share2brain), inner-first (shared→services→web→infra→docs→bmad). Ratifies AD-13
  # stream prefix hivly: → share2brain:. Safe (never deployed to prod; docker compose down -v clears
  # old volumes/keys). Repo/remote/local-config rename = Borja's ops steps (outside the code PR).
  ops-3-rename-hivly-share2brain: done
  # Added 2026-07-13 via bmad-correct-course + bmad-create-story (sprint-change-proposal-2026-07-13-sentry.md,
  # operational-backlog P1.3): Sentry observability for backend/bot/workers. Scope (Borja): FULL LOGS
  # (errors + all log lines → Sentry Structured Logs) + consolidate bot/workers onto @share2brain/shared/logger
  # (delete local logger.ts copies, Story 6.4 dup debt). @sentry/node ^9.41.0 in shared only (AD-2);
  # initSentry(dsn, service) in each main.ts after loadConfig (AD-8); empty DSN = no-op (S-5); dual sink
  # (stdout + Sentry.logger[level]); beforeSend/beforeSendLog reuse redactSecrets, never PII/content (NFR13).
  # Scaffolding already existed (config sentry_dsn, .env, compose wiring on all 3). Story: ops-4-sentry-observability.md.
  ops-4-sentry-observability: done

action_items:
  - epic: 1
    action: "Set up Vitest projects (root vitest.config.ts, packages/backend/vitest.config.ts) + real DB/Redis helper + first GET /health integration test against real Express — before Story 2.3. NOTE: Vitest 4 removed vitest.workspace.ts; used test.projects instead."
    owner: "Amelia (Dev)"
    status: done
  - epic: 1
    action: "AC verification pass against updated docs — confirm Epic 2 ACs match current state (8 tables, real topology) — before creating Stories 2.x. Report: epic-2-ac-verification-2026-07-04.md (consistent, no re-plan)."
    owner: "Amelia + John"
    status: done
  - epic: 1
    action: "Audit packages/web/src/ imports — only @share2brain/shared/schemas (browser-safe), never the root barrel that pulls pg — before Story 2.1. Fixed main.tsx + added ESLint guard on packages/web/**; bundle 408KB->252KB."
    owner: "Amelia (Dev)"
    status: done
  - epic: 1
    action: "Dedicated hardening story: uncaughtException/unhandledRejection handlers + real graceful shutdown for bot/workers. SCOPE REVISED in Epic 2 retro: minimum (uncaughtException/unhandledRejection + reconnect-with-backoff) pulled forward into Epic 3 stories 3.1/3.3; real graceful shutdown (drain consumer group) stays in Epic 6. DONE 2026-07-08 — delivered by Story 6.4 (createGracefulShutdown in packages/backend/src/lifecycle.ts; bot/workers bounded drain + shuttingDown guard; uncaughtException/unhandledRejection handlers)."
    owner: "Amelia (Dev)"
    status: done
  - epic: 2
    action: "Early validation spike of Epic 3's two external integrations — real smoke of the discord.js Gateway (connect, receive one messageCreate) and the embeddings API (one real call, verify 1536-dim vector) BEFORE building 3.1/3.3 logic on top. Addresses the ioredis-class late-validation risk."
    owner: "Amelia (Dev)"
    status: done
  - epic: 2
    action: "Carry the integration-test discipline into the pipeline — tests against real Redis Streams (consumer group, XREADGROUP, XACK only after success) and real pgvector (idempotent UPSERT, AD-13). Part of the definition-of-done for 3.1 and 3.3."
    owner: "Amelia (Dev)"
    status: done
  - epic: 2
    action: "Pull minimum hardening forward into Epic 3 — uncaughtException/unhandledRejection + reconnect-with-backoff in bot and workers, inside 3.1/3.3 (real graceful shutdown stays Epic 6)."
    owner: "Amelia (Dev)"
    status: done
  - epic: 2
    action: "Carried deferred debt (non-blocking): prefers-reduced-motion for the six kh-* keyframes (from 2.1) — if Epic 3 adds any pipeline-UI animation, remember it is still open; useTheme setState side-effects + concurrent-login guard (2.2); Discord 429 retry + log redaction (2.3); access_control.enabled/allow-policy branch (Epic 4+). Tracked in deferred-work.md."
    owner: "Amelia (Dev)"
    status: open
  - epic: 3
    action: "Treat every code-review patch as new, un-reviewed code — the next independent pass verifies prior patches with the same rigor as the original code; a patch is not done until an independent pass confirms it. (Epic 3 had 10 review passes; several defects were introduced by prior passes' patches and caught only by the next pass.) ADOPTED 2026-07-08 — applied in round-2 reviews across Epics 3–6; homed as a Standing DoD in operational-backlog.md."
    owner: "Amelia (Dev)"
    status: done
  - epic: 3
    action: "Fix the deleted_at exclusion semantics over grouped chunks — JOIN embeddings -> discord_messages via message_ids, and decide exclude-if-any-message-deleted vs exclude-if-all. Resolve when creating Story 4.1 (design note, not a blocker). RESOLVED at Story 4.1 creation (2026-07-06): EXCLUDE-IF-ANY (anti-join; chunk content physically contains deleted text) — documented as decision D1 in story 4-1."
    owner: "Winston + Amelia"
    status: done
  - epic: 3
    action: "Fix the view-in-Discord link convention for grouped chunks — which messageId of the group is linked (candidate: the one anchoring chunk_key). Before Story 4.3. RESOLVED in Story 4.3 (2026-07-07): the linked message is the anchor message_ids[0], already exposed as SearchFragment.messageId (Story 4.1 decision D2); added guildId to GET /api/auth/me so the SPA can build https://discord.com/channels/{guildId}/{channelId}/{messageId}."
    owner: "Amelia (Dev)"
    status: done
  - epic: 3
    action: "Stream trimming / retention for share2brain:discord:messages — no MAXLEN today, unbounded growth; a future story must decide the cap and confirm it cannot trim entries still pending in any group PEL. Stays deferred to Epic 6, tracked. DONE 2026-07-08 — superseded by ops-1 (PEL-safe XTRIM … MINID trimmer, merged PR #39)."
    owner: "Amelia (Dev)"
    status: done
  - epic: 3
    action: "Transactional outbox — would fix both the XADD-before-COMMIT window and the pg-connection-held-across-XADD pool risk. Architectural, deferred; revisit when the race/pool pressure is observed."
    owner: "Winston"
    status: open
  - epic: 4
    action: "A visual AC is not done until the E2E harness asserts it (getComputedStyle/screenshot). No frontend story closes with deferred visual ACs; if a spec is missing, the story adds it (as 4.5 established). CONSOLIDATED 2026-07-08 — standing DoD, homed in operational-backlog.md § Standing Definition-of-Done."
    owner: "Amelia (Dev) + Sally (UX)"
    status: done
  - epic: 4
    action: "Verify tooling/environment assumptions at story creation, not mid-implementation (is X actually installed/available?). Generalizes Playwright-not-installed + Epic 3 config-vs-reality. Add to the create-story checklist for any new tooling. CONSOLIDATED 2026-07-08 — standing DoD, homed in operational-backlog.md § Standing Definition-of-Done."
    owner: "Amelia (Dev)"
    status: done
  - epic: 4
    action: "Run-unique test isolation as definition-of-done: suffix-unique roles/channels per run + own-id cleanup; no broad LIKE cleanups that race sibling suites. CONSOLIDATED 2026-07-08 — standing DoD, homed in operational-backlog.md § Standing Definition-of-Done."
    owner: "Amelia (Dev)"
    status: done
  - epic: 4
    action: "CRITICAL PATH before 5.3/5.4 — close the inline-vs-hover cascade debt on result cards and chips (move base border/color off the inline shorthand into the CSS class, mirroring the 4.3 focus fix). DONE 2026-07-07 via PR #26 (fix/frontend-hover-reduced-motion), merged to main."
    owner: "Sally (UX) + Amelia (Dev)"
    status: done
  - epic: 4
    action: "CRITICAL PATH before 5.3/5.4 — prefers-reduced-motion wrapping all kh-* keyframes, including the new kh-pop/kh-blink (closes the debt carried from Story 2.1, now with a trigger). DONE 2026-07-07 via PR #26 (fix/frontend-hover-reduced-motion), merged to main."
    owner: "Sally (UX)"
    status: done
  - epic: 4
    action: "CRITICAL PATH during 5.3/5.4 — extend the 4.5 harness with chat + SSE specs as part of the definition-of-done (FAB hexagon, panel dims, streaming cursor/tool_call/observation colors). DONE — chat.spec.ts carries 7 e2e tests (4 from 5.3 + 3 from 5.4) asserting FAB/panel/streaming visual ACs via getComputedStyle (confirmed in Epic 5 retro 2026-07-08)."
    owner: "Amelia (Dev)"
    status: done
  - epic: 4
    action: "CRITICAL PATH before 5.1/5.4 (elevated by Borja) — SSE end-to-end verification spike: confirm Playwright can drive the fetch-streaming chat and observe token frames accumulate, before building the 5.4 streaming UI. DONE 2026-07-07 via PR #25 (spike/sse-streaming-harness), merged to main."
    owner: "Amelia (Dev)"
    status: done
  - epic: 5
    action: "CRITICAL PATH — complete the multi-system fix + structural guard. Flatten ALL system turns into a single index-0 message in reasonNode (the uncommitted fix missed compress.ts:98's summary, so the compression path still emitted two system turns -> same Anthropic 400 on 5.2 AC3). Add a guard/decorator that rejects >1 SystemMessage and fails loud in CI, plus a test asserting 'exactly one system at index 0' in BOTH the compressed and uncompressed paths. Blocks flipping epic-5 to done. IMPLEMENTED 2026-07-08 (branch fix/chat-multi-system-message: reasonNode folds all system turns; assertSingleLeadingSystem guard in the LangChain adapter; graph.test.ts asserts one-system-at-0 in both paths + new adapter test; empirically discriminated). DONE — merged to main via PR #33 (2026-07-08); epic-5 flipped to done."
    owner: "Amelia (Dev)"
    status: done
  - epic: 5
    action: "Env-gated smoke against the real LLM provider as DoD for any story touching the StateGraph/LLM: a test behind an env var (e.g. ANTHROPIC_API_KEY present) that runs a real /api/chat turn against Anthropic and asserts it streams without a 400. Fake-model green is necessary, not sufficient. CONSOLIDATED 2026-07-08 — standing DoD, homed in operational-backlog.md § Standing Definition-of-Done (re-arms when agentic work resumes)."
    owner: "Amelia (Dev)"
    status: done
  - epic: 5
    action: "'A test that lies' rule (assertion tautology): every new test must FAIL when the behavior it claims to cover is reverted — verify empirically by reverting. Generalizes the 5.1/5.3 round-3 findings where a test passed with its fix removed. Review-checklist / DoD gate. CONSOLIDATED 2026-07-08 — standing DoD, homed in operational-backlog.md § Standing Definition-of-Done."
    owner: "Amelia (Dev)"
    status: done
  - epic: 5
    action: "Cross-story contract consistency: when one story injects data into a structure another story consumes (5.2 compression summary -> 5.1 preparedMessages), review the COMBINED contract, not each in isolation. The multi-system bug is exactly this — each story was locally correct, the composed message-array shape was not. CONSOLIDATED 2026-07-08 — standing DoD, homed in operational-backlog.md § Standing Definition-of-Done."
    owner: "Amelia (Dev) + Winston"
    status: done
  - epic: 5
    action: "Adjust 5.4 AC and UX-DR20: the execution-trace panel (tool_call/observation) is explicitly 'deferred to a future agentic-capabilities epic' (NOT Epic 6) — a retained product goal with a future home, not a drop. Annotate the AC/UX-DR and record the backlog item."
    owner: "John (PM) + Sally (UX)"
    status: open
  - epic: 5
    action: "Housekeeping reminder: after the multi-system critical fix lands + is merged, flip epic-5 -> done (currently in-progress; NOT done because the provider-contract fix is incomplete + untested)."
    owner: "Amelia (Dev)"
    status: done
  - epic: 6
    action: "Create the explicit post-roadmap operational backlog (operational-backlog.md) as the home for reliability/observability/test-hygiene debt now that there is no next epic; rank by real operational impact, promote to a BMAD story when picked up. DONE 2026-07-08 at the Epic 6 retro (Borja chose the explicit-backlog option). File: _bmad-output/implementation-artifacts/operational-backlog.md."
    owner: "Amelia (Dev) + Winston"
    status: done
  - epic: 6
    action: "MAXLEN / stream trimming on all Redis Streams (backlog P1.1) — bounded xAdd/XTRIM on share2brain:discord:messages + :updated + :deleted, confirming the cap never trims a PEL-pending entry. Third deferral (Epic 3 AI#4 -> Epic 5 retro -> here); elevated to backlog P1. DONE 2026-07-08 — ops-1 (PEL-safe XTRIM … MINID, merged PR #39)."
    owner: "Amelia (Dev) + Winston"
    status: done
  - epic: 6
    action: "Fix backend integration-test isolation (backlog P1.2) — the recurring RBAC 'test-guild role leak' flake reported in the 6.2/6.3/6.4 Completion Notes; run-unique roles/channels + own-id cleanup, or stop running integration tests against a stack with live dev containers attached. Green when npm run test:integration is deterministic. DONE 2026-07-08 — ops-2 (stale test-guild assertion fixed + run-unique ids + fail-fast guard, merged PR #41)."
    owner: "Amelia (Dev)"
    status: done
  - epic: 6
    action: "Trace panel (tool_call/observation) is a RETAINED product goal for a future agentic-capabilities epic (confirmed with Borja 2026-07-08) — keep the annotation on Story 5.4 AC + UX-DR20; homed as backlog item F1. Not operational debt."
    owner: "John (PM) + Sally (UX)"
    status: open
  - epic: 6
    action: "Standing DoD (carry-forward from Epic 5 AI#2): any future story touching the LLM/StateGraph is not done until exercised against the real provider (env-gated smoke). No trigger in Epic 6; re-arms when agentic work resumes. CONSOLIDATED 2026-07-08 — same norm as the Epic 5 env-gated-smoke DoD, homed in operational-backlog.md § Standing Definition-of-Done."
    owner: "Amelia (Dev)"
    status: done
  - epic: 7
    action: "Tune bmad-code-review triage for Blind-Hunter severity noise (Borja concern): a Blind-only 'High' must be corroborated by the Edge Case Hunter or Acceptance Auditor before entering actionable triage at High; otherwise start it as needs-verification. Consider a bmad-customize override to down-weight uncorroborated Blind findings. Blind produced inflated-severity FPs in ~every Epic 7 story (7.1 'backend won't boot', 7.3 'High' empty-purge, 7.6 diff-only)."
    owner: "Amelia (Dev)"
    status: open
  - epic: 7
    action: "Close the residual load-sensitive integration intermittency (auth/documents/search under heavy load) — the class that survived ops-2 (the main RBAC test-guild leak was a stale assertion, fixed). Promote the deferred-work.md entry to a story OR codify 'stop running integration against a stack with live dev containers attached' as a hard gate precondition. Every future 'not yours' Completion Note must link a tracked backlog item."
    owner: "Amelia (Dev)"
    status: open
  - epic: 7
    action: "Standing DoD (retained, now PROVEN): any story touching the LLM/StateGraph/new generative path is not done until exercised against the real provider (env-gated smoke). First live trigger in 7.2 caught a real config bug (provider:'anthropic' + non-Anthropic-shaped ENRICHMENT_LLM_API_KEY). Re-armed for any future agentic work. Homed in operational-backlog.md § Standing Definition-of-Done."
    owner: "Amelia (Dev)"
    status: done
  - epic: 9
    action: "AI-1 — Down-weight uncorroborated Blind Hunter findings: a Blind-only 'High/Medium' starts as needs-verification unless the Edge Case Hunter or Acceptance Auditor corroborates it. Consider a bmad-customize override. 3rd appearance of the pattern (carried from Epic 7 AI, recurred in reviews of 9.2/9.3/9.4 — all refuted against source)."
    owner: "Amelia (Dev)"
    status: open
  - epic: 9
    action: "AI-2 — Formalize the post-roadmap 'addition mode': Epics 8 and 9 were both correct-course-born additions with no stable planning container. Explicitly decide whether to keep operating in continuous-addition mode or open a real planning container for the hardening work."
    owner: "John (PM) + Borja"
    status: open
  - epic: 9
    action: "AI-3 — Real deploy + observability (CRITICAL to the stated 'harden for production' goal): first deployment to a real environment with metrics/alerts beyond the crash-only Notifier; write the general deploy runbook (today only the Epic 7 migration runbook exists in operational-backlog.md). Product is functionally complete but has never run in a real production environment."
    owner: "Winston (Architect) + Amelia (Dev)"
    status: open
  - epic: 9
    action: "AI-4 — Promote the P2 correctness items in operational-backlog.md to BMAD stories, priority order: messageDeleteBulk (P2.3, silent data loss on bulk purges), DLQ/retry-cap on failing Sync entries (P2.2), transactional outbox (P2.1)."
    owner: "Amelia (Dev) + Winston (Architect)"
    status: open
  - epic: 9
    action: "AI-5 — Close the load-sensitive integration intermittency (the residual that survived ops-2): codify 'don't run integration against a stack with live dev containers attached' as a hard gate, or make the tests deterministic under load. Carried from Epic 7 AI (no trigger in Epic 9 — stats path is read-only aggregation)."
    owner: "Amelia (Dev)"
    status: open
