# Deferred Work

## Deferred from: code review of PR #71 oss-launch-docs (2026-07-12)

- **PROMOTED → PR #72 (2026-07-12): Documented local-dev flow cannot reach Redis (PRE-LAUNCH: fix recommended before OSS launch)** [`docker-compose.yml:54-72`; `.env.example:33-34`]. Fixed on branch `fix/local-dev-redis` exactly as scoped below and verified end-to-end (NOAUTH rejected / authed PONG); pending merge. The `redis` service publishes no host port (internal `data` network only) and runs `--requirepass ${REDIS_PASSWORD}`, while `.env.example` ships `REDIS_URL=redis://localhost:6379` with no auth. The by-the-book flow in README/CONTRIBUTING/development_guide (`docker compose up -d postgres redis` + `npm run dev`) fails at Redis connect for every first-time contributor. Fix, if promoted: publish `127.0.0.1:6379:6379` on the redis service and align `.env.example`'s REDIS_URL to include the password (mirroring the postgres loopback pattern at docker-compose.yml:42-43). Surfaced by the Edge Case Hunter, round 2.

- **Telegram notifications unreachable in the Docker deployment** [`docker-compose.yml:122/163/200`]. CHANGELOG/ROADMAP advertise optional Telegram/Slack notifications and the code implements both (`packages/shared/src/notifier/index.ts`), but docker-compose.yml forwards only `SLACK_WEBHOOK_URL` — no `TELEGRAM_BOT_TOKEN`/`TELEGRAM_CHAT_ID` passthrough. Since an unset `${VAR}` referenced by `Share2Brain.config.yml` aborts `loadConfig`, setting `notifications.provider: "telegram"` crash-loops bot/backend/workers under compose. Pre-existing compose gap, out of the docs PR's scope. Fix, if promoted: add the two `TELEGRAM_*` env passthroughs to the three app services. Surfaced by the Edge Case Hunter.

- **RESOLVED (2026-07-12): CODE_OF_CONDUCT enforcement contact needs a real confidential channel** [`CODE_OF_CONDUCT.md:61-64`]. Original finding: reports routed "through a GitHub private report or direct message" — GitHub has no DMs, and the private-report flow is the security-advisory queue, which SECURITY.md reserves for vulnerabilities. Surfaced by both review layers; escalated to pre-launch BLOCKER in round 2. Resolution: Borja designated borjaberrocal87@gmail.com as the enforcement contact and the Enforcement section now points there (same PR, post-review commit).

- **`SLACK_WEBHOOK_URL` warning noise on every compose command** [`docker-compose.yml:122/163/200`]. `${SLACK_WEBHOOK_URL}` is interpolated without a `:-` default while `.env.example` ships it commented out, so a by-the-book quick start prints WARN lines on each `docker compose` invocation. The compose header comment claiming `loadConfig` requires it is stale. Fix, if promoted: use `${SLACK_WEBHOOK_URL:-}` and update the comment. Surfaced by the Edge Case Hunter.

- **`VITE_COMMUNITY_NAME` silently ignored in the Docker build** [`packages/web/Dockerfile`; `docker-compose.yml` nginx build block]. `.env.example` documents it as build-time SPA branding, but the web Dockerfile declares no `ARG`/`ENV` and compose passes no build args, so the header falls back to "Share2Brain" (`packages/web/src/App.tsx:32`) in the flagship deploy mode; it only works under `npm run dev`. Fix, if promoted: thread a build arg through compose -> Dockerfile -> Vite build. Surfaced by the Edge Case Hunter.

## Deferred from: code review of story-10.2 (2026-07-12)

- **`locales/parity.test.ts` compares only the key tree, not interpolation placeholders or plural-form divergence** [`packages/web/src/locales/parity.test.ts:18-31`]. `keyPaths` recurses the key structure of `es.json` vs `en.json` (both directions), satisfying AC2, but does not compare the `{{...}}` placeholders inside values nor guard against one locale defining a `_one`/`_other` the other lacks. All current keys match, so there is no live break — deferred as test-hardening. A future edit that diverges a placeholder (e.g. `{{shown}}` vs `{{shownn}}`) or adds an asymmetric plural suffix would pass parity yet interpolate to empty / render the raw `{{count}}` at runtime. Fix, if promoted: extend the parity test to also assert placeholder-set equality per shared key. Surfaced by the Edge Case Hunter during code review.

## Deferred from: story-10.2 implementation (2026-07-12)

- **StatsView KPI `label`/`sub` stay Spanish under an `en` deployment (D3, known limitation, ratified in the story's Dev Notes)** [`packages/web/src/components/StatsView.tsx:190/218` (KPI grid, rendered verbatim from the API); `packages/backend/src/application/services/statsService.ts:96–121` (source of the Spanish text — `'Recursos indexados'`, `` `+${n} esta semana` ``, `'de ${n} accesibles'`, `'en tus canales'`, `'Tus consultas al agente'`, `'últimos 30 días'`, …)]. Web-only i18n cannot translate backend-produced strings, and the SCP pins backend response bodies unchanged for this epic. The contract has a stable `kpis[].key` enum, so the `label` alone could be client-mapped, but `sub` embeds backend-computed numbers (`+3 esta semana`, `de 6 accesibles`) not present anywhere else in the contract — a half-translated card (label in English, sub in Spanish) was judged worse than a consistent, documented limitation. Visually confirmed during the story's AC7 manual verification: under `en`, the Stats page renders "Indexing activity", "Resources by channel", "Reading coverage" etc. in English, but the 4 KPI cards' `label`/`sub` text (e.g. "Recursos indexados", "+40 esta semana") remains Spanish. Fix, if promoted: a shared+backend story adding either (a) a structured KPI contract (translation key + numeric fields, client renders the sentence) or (b) backend language-awareness (`Accept-Language` or the same `ui.language` config, service-side i18n for KPI strings) — out of Epic 10's web-only scope.

## Deferred from: code review of story-10.1 (2026-07-12)

- **No `Cache-Control`/`ETag` on the deployment-static `GET /api/ui-config` response** [`packages/backend/src/presentation/controllers/uiConfigController.ts`]. The `language` value is fixed for the life of the process (injected once into the controller at boot), yet every SPA load re-hits the endpoint with no caching hints, consuming the shared `rl:api:` budget and adding a round-trip before the login screen renders. Adding `Cache-Control` (or an `ETag`) is an optional enhancement — the caching policy (how long to cache a value that only changes on config edit + restart) is a design choice, so not auto-patched. Revisit if pre-auth `/api/ui-config` traffic becomes a measurable budget/latency concern.
- **No integration test drives the `/api/ui-config` non-GET catch-all with rate-limiting ACTIVE** [`packages/backend/src/uiConfig.integration.test.ts`]. The `POST /api/ui-config → 404` test builds its app with `buildTestAppOptions()` (no `rateLimit`), so `apiLimiters` is `[]`. It proves the catch-all terminates and returns the unified `{ error, code }` shape, but does not exercise the double-count guard under *active* limiters. Low value: the router's termination is method/path-based and limiter-independent (the limiter runs on the mount *before* the router regardless), so an active-limiter variant would largely test express-rate-limit's own `ERR_ERL_DOUBLE_COUNT` internals rather than our code. Edge Case Hunter classified it "a test-coverage gap, not an unhandled path." Revisit only if the ui-config limiter wiring is refactored.
- **`es`/`en` enum duplicated across the config schema and the response contract** [`packages/shared/src/config/index.ts` vs `packages/shared/src/schemas/uiConfig.ts`]. Two independent `z.enum(['es','en'])` declarations must be kept identical by hand. Today they match (story scope is es/en only), but adding a language to `config.ui.language` without adding it to `UiConfigResponseSchema` would let a valid config produce a body that fails `UiConfigResponseSchema.parse` → runtime 500 on the pre-auth endpoint. Low-priority coupling; consider extracting a single shared language-enum when languages expand beyond es/en (a multi-file change out of this story's scope). Revisit when a third language is introduced.

## Deferred from: code review of ops-3-rename-hivly-share2brain (2026-07-10)

- **Untracked local `Hivly.config.yml` is now un-gitignored** [`.gitignore:7`, `.dockerignore:17`]. Both ignore files now list only `Share2Brain.config.yml`, so the stale local `Hivly.config.yml` (the dev's old AD-8 runtime behavior config, ~6.5KB, git-untracked) is no longer ignored and could be accidentally `git add`ed — and per AD-8 it must never be committed. Not a rename defect (all tracked files are correct); resolved by Borja's Task 8 ops step (rename local `Hivly.config.yml` → `Share2Brain.config.yml` or delete it). Deferred: out of code-PR scope, operational step.
- **Slack channel `#Share2Brain-alerts` uses invalid casing** [`docs/context/PRD.md:569,1234`]. Slack channel names are lowercase-only, so `#Share2Brain-alerts` is invalid. Pre-existing (`#Hivly-alerts` was already capitalized in the source) and faithfully carried through by the case-preserving substitution — same normalization cluster as the PRD Postgres-casing patch. Doc-example only; can ride the PRD-casing patch if fixed. Deferred: pre-existing.
- **Story record commit count says "7", branch has 8** [`ops-3-rename-hivly-share2brain.md:174,221`]. Completion Notes + Change Log both say "7 commits"; the branch is `05e4a4f..0c264ee` = 8 (the 8th is `0c264ee` the story→review tracking commit, added after the described 7). Cosmetic record accuracy; self-corrects on the done-flip commit. Deferred: cosmetic.

## Deferred from: code review of story-9.4 (2026-07-10)

- **Out-of-order / stale edit redelivery can regress a stored `author_name`** [`packages/workers/src/sync/processUpdate.ts:159-165`]. The step-6 `UPDATE discord_messages SET content, updated_at, author_name` has no `WHERE updated_at <= ${timestamp}` monotonicity guard. If an older edit event is processed AFTER a newer one already committed (older event failed → reclaimed from the PEL, or two scaled Sync workers interleave), the older display name (and content/updated_at) overwrites the newer — last-write-wins is decided by processing order, not edit recency. The `authorName !== undefined` guard prevents *blanking* a stored name but not *reverting* it to a stale value. **NOT introduced by 9.4** — `content`/`updated_at` already ride the identical unguarded overwrite (pre-existing 6.2/7.3 Sync behavior); 9.4 simply extends the same staleness window to the new column. Raised independently by Blind Hunter + Edge Case Hunter in the review re-run. A fix is a monotonicity guard on the whole Sync UPDATE — a Sync-worker concern out of scope for an author_name capture story. Revisit if stale-edit reversion is ever observed in production or when the Sync overwrite semantics are revisited.
- **Edit-path producer has no runtime guard on `author.displayName` before `xAdd`** [`packages/bot/src/discord/handlers/messageUpdate.ts:124`]. The `messageUpdate` handler reads `message.author.displayName` off the post-`fetch()` message and puts it straight on the `MessageUpdatedEvent` XADD payload. If that value were ever runtime-`undefined`/non-string, `redis.xAdd` (string-only field values) would reject and the outer try/catch would silently drop the whole edit — content update included, not just the name. Unreachable under discord.js 14.26.4 (`User#displayName` = `globalName ?? username`, always a non-null string on a hydrated author) and it rides on the exact same accepted hydration assumption as the pre-existing `author.bot` read on the identical code path. Deliberately NOT patched: convention is "producer trusts discord.js types, consumer validates at the stream trust boundary" — `parseUpdatedEvent` already normalizes absent/blank → `undefined`, and hardening only `displayName` (leaving `newContent`/`guildId`, equally trusted, unguarded) would be inconsistent. Revisit only if a whole-producer stream-write hardening pass is undertaken.

## Deferred from: code review of story-7.5 (2026-07-09)

- **"ver recurso" mark-read misses middle-click and context-menu open-in-new-tab** [`packages/web/src/components/DocsView.tsx`]. The F2/D6 design marks a row read by letting the anchor's `click` bubble to the row handler — this fires for primary click, modifier (Cmd/Ctrl) click and keyboard-Enter, but NOT for middle-click (`auxclick`) or the context-menu "Open link in new tab" (fires no `click` at all). A user opening a resource via those common new-tab gestures leaves the row falsely unread, undercutting F2's "opening the resource is the strongest read signal" rationale. **[DECIDED with Borja, 2026-07-09]** accept as a known F2 limitation: the context menu is uncatchable so an `onAuxClick`-only fix would be partial, and the primary interactions already work. Revisit only if read-signal accuracy on new-tab gestures becomes a real complaint.
- **`.min(1)` widens the whole-response parse-poison blast radius.** `searchService.ts:52` and `documentService.ts:75` `.parse()` the entire Search/Documents response, so a single persisted resource with `title === ''` now 500s the whole page — whereas `ragRetriever.drizzle.ts:30` `safeParse`s per row and skips-and-warns. `.min(1)` adds `title` to the set of fields that trip this hard-fail path (previously only strict `link` did, since 7.4). Only theoretical given the enrichment write-path guard (empty → throw), and Story 7.4's review explicitly ratified this fail-fast/skip asymmetry (D3). Pre-existing; not reopened by 7.5. Revisit only if the two read surfaces need to agree on malformed-row degradation.
- **New per-card `<h3>` title may skip heading hierarchy (a11y)** [`packages/web/src/components/SearchView.tsx`]. Each result card now emits an `<h3>`; if the surrounding page lacks an `<h2>`, screen-reader heading navigation sees orphan h3s. Minor a11y nit, not a functional break. Low value; batch with any future web a11y pass.
- **AC-1 completion-note grep under-reports two `title: ''` raw seeds** [`packages/workers/src/sync/sync.integration.test.ts:589,633`]. `seedEmbedding(...)` raw-DB inserts for the soft/hard-delete path tests carry `title: ''` (and `link: ''`); they bypass the Citation/fragment Zod schemas entirely and live in `workers` (do-not-touch, ratified pre-pivot delete-path seeds). AC-1's substantive guarantee (no empty title in a contract-parsed fixture/seed) holds — only the completion note's literal phrasing is imprecise. Doc-accuracy correction, no code impact.

## Deferred from: story OPS-2 (integration-test isolation) (2026-07-08)

- **Load-sensitive backend integration intermittency (~1/10 under heavy load).** After OPS-2 fixed the deterministic RBAC `test-guild` stale assertion, a separate rare flake remains: a SHIFTING test (`auth` CSRF-state, `documents` channelId narrowing, `search` q-missing — all auth+session-backed) intermittently fails a status assertion. Investigation ruled out `flushAll`, cross-file parallelism (`fileParallelism:false` did not help, reverted), and live containers (fails with them stopped too). It reproduces ONLY under heavy concurrent machine load (rapid suite loops + docker ops + builds); when quiet, 28 consecutive clean backend-integration runs + 3/3 full `test:integration` (110 tests). Most consistent with a session write→read timing gap under contention (a just-written Redis session transiently unreadable → `requireAuth` 401s before the endpoint's own validation). Low priority; investigate IF it recurs in CI. Candidate fixes to explore then: assert-with-retry on the login/session step, a small session-write await/confirm in `loginMember`, or a dedicated per-file Redis key prefix. NOT the RBAC leak (that is fixed) — a distinct, pre-existing infra-timing issue.

## Deferred from: code review of story-6.2 (2026-07-08)

- **Row-missing update/delete acks and drops the event.** `processUpdate`/`processDelete` treat an unknown-message event as an ack+skip no-op (DECISION 4). Blind/Edge flagged this as a lost-edit / resurface race, but the **Bot** owns `discord_messages` (via `persistMessage`), not the Indexer — so the row exists for any live edit/delete and the branch only fires for messages the bot never persisted (historical / bot-was-down). That is **Story 6.3 offline-reconciliation scope** (note #8). The `deleted_at` anti-join further prevents resurface for the row-exists case. Low live-path risk; left to 6.3.
- **Edit-to-blank silently keeps stale chunks.** A message edited to whitespace-only parses to `null` → warn + ack, leaving the pre-edit chunks searchable. 6.1 does not publish blank edits (recon #6), so the live path can't trigger it. Whether a "cleared text, keeps embed/attachment" edit should PURGE the message is a product decision, not a 6.2 defect. Revisit if 6.1 begins publishing blank edits.
- **No DLQ / retry-cap / alert on permanently-failing Sync entries.** A deterministically-failing entry (e.g. `config.embeddings.dimensions` mismatch) stays in the PEL and re-fails on every boot replay, with only per-attempt error logs and no cap or alert. PEL-as-DLQ is the AD-13 design; retry caps + alerting are observability work, out of 6.2's scope.
- **Speculative / boot-time low-severity items.** (a) A precise interleave of update + hard-delete on the same message can re-INSERT embeddings a hard-delete just purged — storage-only, hidden by the `deleted_at` anti-join. (b) A non-BUSYGROUP `xGroupCreate` failure in one Sync loop rejects `runSync` while the sibling loop runs on — boot-time infra error, resolves via fail-fast + Compose restart.

## Deferred from: story creation of 6-1-bot-deteccion-de-ediciones-y-borrados-en-tiempo-real (2026-07-08)

- `messageDeleteBulk` (Discord moderation bulk-purge) is a **separate** Gateway event from `messageDelete` and is out of scope for Story 6.1 (the epic specifies `messageDelete` only). Consequence: if a moderator bulk-deletes N messages at once, those deletions are NOT published to `share2brain:discord:messages:deleted` and won't sync until offline reconciliation (Story 6.3) — or a dedicated follow-up. A follow-up would register `Events.MessageBulkDelete`, iterate the deleted collection, and publish one `discord.message.deleted` per message through the same `handleMessageDelete` path. **[DECIDED with Borja, 2026-07-08]** to defer.

## Deferred from: code review of 5-3-widget-flotante-fab-panel-base, round 2 (2026-07-07)

- `aria-modal="true"` on the chat panel (`packages/web/src/components/ChatWidget.tsx`) doesn't make `AppLayout` behind it `inert`/`aria-hidden` — assistive tech that doesn't fully honor `aria-modal` (e.g. touch/virtual-cursor browsing) can still reach it. The keyboard-Tab escape this was meant to prevent is closed by the round-2 focus-trap fixes; adding `inert` to `AppLayout` is additional hardening that touches a file outside `ChatWidget.tsx`.

## Deferred from: code review of 5-3-widget-flotante-fab-panel-base (2026-07-07)

- `resetAndSeed`'s new deletes/inserts (`packages/backend/src/e2e/seed.ts`) run as separate unguarded `db.execute` calls, no transaction — a mid-sequence failure leaves partial e2e state; self-heals on the next boot since delete-then-insert always re-runs first. Test-infra only, not production code.
- `fetchConversations`'s `if (params.page)` / `if (params.limit)` truthiness checks (`packages/web/src/api/conversations.ts`) silently drop an explicit `page: 0`/`limit: 0` — copied verbatim from the story's own Dev Notes §Conversations API template; currently unreachable since `ChatWidget` never passes these params in 5.3.
- The chat history overlay (`packages/web/src/components/ChatWidget.tsx`) fetches `total` but never surfaces it — no pagination affordance if a user ever has more conversations than the default page size. Out of the 5.3 shell's stated scope.
- `conversations.ts` throws a generic `Error` with just the HTTP status on a non-ok response (discards any error body) and doesn't guard a malformed-JSON parse failure — matches the existing `api/documents.ts` convention verbatim, not a new regression.

## Deferred from: code review of 5-2-gestion-de-conversaciones-e-historial, round 2 (2026-07-07)

- `Array.from`-based title truncation (`deriveTitle`) is Unicode-code-point-safe but not grapheme-cluster-safe — a ZWJ emoji sequence, flag pair, or base+combining-diacritic sequence spanning multiple code points can still be split at the 80-code-point boundary. A fully correct fix needs `Intl.Segmenter({granularity:'grapheme'})`; no AC requires grapheme-perfect truncation.
- The `id ASC`/`id DESC` SQL tiebreakers (`conversationRepository.drizzle.ts`) make ordering deterministic but not necessarily chronologically correct on an exact `created_at` tie — ids are random UUIDv4s unrelated to insertion order. A true fix needs a monotonic secondary column (serial or UUIDv7), a schema change out of scope for a review patch.

## Deferred from: code review of 5-2-gestion-de-conversaciones-e-historial (2026-07-07)

- Concurrent requests on the same conversationId can race `getMessages`↔`appendMessage(user)` (`chatService.ts:666-678`) — low-probability (a user firing two simultaneous turns on the same conversation, e.g. double-tab); D4 only guards single-request ordering, not cross-request concurrency; no AC requires handling it. Matches how similar low-probability races (TOCTOU) were deferred in Epic 4.
- Full, unbounded conversation history fetched on every `/api/chat` turn (`conversationRepository.drizzle.ts` `getMessages` has no `LIMIT`) — pre-existing design per D3/D7 (the `reason` node's `memory_window` cap + `compressIfNeeded` truncate downstream in JS); real cost only materializes for very long conversations.
- `page` cap still allows an expensive ~1e8 `OFFSET`; the "guards against a bigint OFFSET overflow" comment overstates the actual risk (a JS number/int4 offset doesn't overflow, it just forces Postgres to skip a lot of rows) — copied verbatim from the established `documents.ts` convention (Story 4.2), not introduced by 5.2.

## Deferred from: code review of 1-1-inicializar-el-repositorio-y-la-estructura-del-monorepo (2026-07-03)

- Dev scripts (`node --watch src/main.ts`) won't run .ts files without a TS loader — placeholder scaffold, real tooling (tsx/Vite) lands in later stories
- `noEmit: true` conflicts with `outDir: "dist"` — intentional at scaffold stage; real build artifacts come with domain code
- `SIBLING_SERVICES` array duplicated across ESLint config objects — code style, not a bug
- `"build": "tsc --noEmit"` is misleading — intentional at scaffold; documented in story completion notes
- No `vitest.config.ts` in any package — Vitest defaults work for scaffold; workspace config needed before cross-package tests
- `@share2brain/shared` exports raw `.ts` source — intentional decision documented in completion notes (avoids `composite: true` conflict)
- `exports` field blocks future subpath entrypoints — add wildcard when sub-exporters land
- New `@share2brain/*` package would not be auto-covered by ESLint cross-service ban — manual registration needed
- No `uncaughtException`/`SIGTERM`/`SIGINT` handlers in any service — scaffold stage, error handling framework lands later
- `--if-present` on root typecheck/build scripts could silently skip packages missing those scripts — all 5 packages currently have them
- `.env.example.*` variants not tracked by `.gitignore` negation — edge case, team can add rules if needed
- No root `tsconfig.json` (only `tsconfig.base.json`) — by design; each package extends base directly
- Multiple scaffold-appropriate omissions: no `.d.ts` generation, no type-aware ESLint rules, no structured logging format, no vitest workspace config

## Deferred from: code review of 1-3-docker-compose-servicios-base-y-endpoint-health (2026-07-04)

- DB/Redis connections created before HTTP listens — lazy redis + pool pattern mitigate this; not a real issue in practice
- Missing Share2Brain.config.yml crashes containers — standard Docker behavior for missing bind mounts; documented in Dev Notes
- No integration test for health handler — unit tests cover logic; HTTP-level tests can be added in a later story
- redis maxRetriesPerRequest: 1 is aggressive — tuning parameter; 503 on transient Redis outage is acceptable
- Timeout test not explicitly covered — promise that never resolves not tested; both paths fall into the same catch of probe()

## Deferred from: code review of 2-2-layout-principal-sidebar-y-pantalla-de-login (2026-07-04)

- Side effects inside setState updater in useTheme — `toggleTheme` performs DOM mutations and localStorage writes inside a `setState` updater (useTheme.ts:22-33). React state updaters should be pure; a `useEffect` keyed on `theme` is the conventional pattern. Not a real issue in current usage (no StrictMode effects in production).
- login() has no internal guard against concurrent calls — `login` unconditionally starts a new `setTimeout` (App.tsx:35-42). LoginScreen button is `disabled={loggingIn}`, but if a future caller invokes `login()` while a timer is pending, a second timer starts and the first is orphaned. A `if (loginTimer.current) return;` guard would prevent this.

## Deferred from: code review of 2-1-sistema-de-diseno-en-packages-web (2026-07-04)

- Respetar `prefers-color-scheme` — el tema oscuro se fuerza sin consultar la preferencia del SO. La lógica de detección y toggle persistente con `localStorage` está planificada para Story 2.2, donde tiene sentido añadirlo.
- `@media (prefers-reduced-motion: reduce)` — las 6 animaciones no tienen variante reduced-motion. Válido para accesibilidad pero aplica a todo el sistema de diseño, no solo a esta story.

## Deferred from: code review of 2-3-backend-discord-oauth2-y-sesiones-en-redis (2026-07-04)

- No 429 retry logic for Discord rate limits — unlikely at current scale; add retry with `Retry-After` header parsing if rate limits become frequent
- Error message leakage in logs — `console.error('[auth] callback failed:', ...)` may log Discord API error details; adopt structured logging with redaction in a future observability story

## Deferred from: code review of 2-4-rbac-proteccion-de-rutas-y-conexion-ui (2026-07-05)

- `access_control.enabled` not checked during materialization — explicitly out of scope per story Dev Notes ("Do not build an allow-all branch or a disable switch")

## Deferred from: code review of 3-0-config-proveedores-llm-y-embeddings (2026-07-06)

- `readEmbeddingDimensions()` duplica lógica de path resolution de `loadConfig()` — si el mecanismo de path cambia en el futuro, divergen silenciosamente. Pre-existente en `loadConfig()`.
- `superRefine` solo se ejecuta en `z.parse()` — si config se carga de una fuente cacheada que esquiva Zod, el invariante `custom`+`base_url` no se enforce. Pre-existente (toda validación Zod es parse-time).
- `EMBEDDING_DIMENSIONS` evaluado en module-load time — si la config es inválida o el file no existe, `schema.ts` crashea al importarse. Pre-existente en schema.ts (era módulo-top-level antes del cambio).
- `embeddingDimensions.ts` bypass del schema Zod deliberadamente — necesario para AD-5 (drizzle-kit generate no puede correr `loadConfig()`). Si las reglas de validación de dimensiones se extienden, este file debe actualizarse en sincronía.
- `discordRoles` typed as non-optional in `SessionData` — pre-existing pattern (mirrors `userId` which is also non-optional)
- Short-circuit in `findAllowedChannelIds([])` not integration-tested against real Postgres — intentional optimization; covered by unit tests in `rbacService.test.ts`

## Deferred from: code review of 3-1-discord-bot-conexion-al-gateway-y-listener-messagecreate (2026-07-06)

- Redis offline queue retiene transacción DB — con `enableOfflineQueue: true` (default de node-redis), un `xAdd` durante una caída de Redis no rechaza inmediatamente, manteniendo abierta la transacción de Postgres y consumiendo una conexión del pool. Comportamiento preexistente de node-redis, no introducido por esta story.

## Deferred from: code review of 3-1-discord-bot-conexion-al-gateway-y-listener-messagecreate (2026-07-06, 2nd pass)

- ~~XADD publica antes del COMMIT de Postgres — el `xAdd` corre dentro del callback de la transacción pero contra el cliente Redis (no transaccional), así que el evento es durable en Redis antes de que aterrice el COMMIT. Para Story 3.3: el Indexer debe leer `content` del propio evento (lo lleva) o tolerar un row-not-found transitorio sin hacer ACK.~~ **RESUELTO en Story 3.3** (2026-07-06): el Indexer parsea `content` del propio evento de stream (nunca re-lee la fila) y, si la fila de `discord_messages` aún no existe al procesar (COMMIT no aterrizado), deja la entrada **pendiente sin XACK** (`partitionByIndexState` → bucket `pending`); Redis la re-entrega y el ACK solo ocurre sobre ids devueltos por el `RETURNING` del stamp de `indexed_at`.
- ~~INSERT sin `onConflict` — una re-entrega de `messageCreate` en un RESUME de Discord dispara un duplicado de PK que aborta la transacción y loguea un `error: failed to persist message` falso (la fila+evento ya existen). `onConflictDoNothing` haría el productor idempotente. Camino poco frecuente.~~ **RESUELTO en Story 3.2** (2026-07-06): `persistMessage` ahora hace `onConflictDoNothing().returning()` y omite el XADD cuando la fila ya existía — productor idempotente en el camino compartido live + backfill.
- Recuperación del Gateway post-arranque delegada a discord.js — `connectWithRetry` corre una sola vez al boot; el reset/escalado de AC-4 solo se ejercita en el login inicial. Si discord.js agota sus propios reintentos tras el arranque, nada escala a `error` ni sale, y el bot queda idle. Coincide con el diseño documentado; revisar como mejora de observabilidad.

## Deferred from: code review of 3-2-discord-bot-backfill-historico-con-reconciliacion-por-snowflake (2026-07-06)

- No timeout on messages.fetch() — Discord API call has no AbortSignal. Pre-existing concern (same in live path); discord.js internal timebounds handle the common case.
- Gap pages have no max-page safety valve — A year-long offline gap runs unboundedly. By design per AC-6 ("the whole gap is covered").
- rateLimited listener without queue depth visibility — Discord.js internal queue depth is not observable via events.
- onConflictDoNothing silences edits from backfill — `onConflictDoNothing` salta inserciones de mensajes que ya existen, incluso si el backfill trae contenido editado. Caso estrecho: solo cuando el mensaje fue recibido live Y editado durante la ventana offline. Tradeoff aceptado (Borja, 2026-07-06): un `ON CONFLICT DO UPDATE` añadiría escrituras innecesarias para el caso común.

## Deferred from: code review of 3-2-discord-bot-backfill-historico-con-reconciliacion-por-snowflake (2026-07-06, 3rd pass)

- SIGTERM/SIGINT handlers registered after the per-channel cursor-resolution loop and Discord client setup in `main.ts` — a signal arriving during that window bypasses the bounded-shutdown logic entirely (Node's default SIGTERM handling has no cleanup). Pre-existing ordering from the original implementation. Fixing it means moving `shutdownSignal`/`shutdown()`'s definition and the `process.on(...)` registration earlier in `main()`, ahead of the cursor loop — a bigger structural reorder than a single review patch.

## Decisions & deferrals from: Story 3.3 (Workers — Indexer, 2026-07-06)

- **Retry-max / DLQ policy — DECIDED (SPINE §Deferred assigned this decision to this story):** the consumer group's PEL *is* the dead-letter queue. No retry-max and no per-entry poison quarantine: a failed entry stays pending (un-ACKed) and is re-processed by the PEL replay on the next worker boot. Malformed/foreign entries (which can never succeed) are the one exception — they are XACKed so they don't clog the PEL. No `MAXLEN`/`XTRIM` on `share2brain:discord:messages` yet.
- **Stream trimming (`MAXLEN`/retention) — STILL DEFERRED:** with no `MAXLEN`, `share2brain:discord:messages` grows unbounded. Accepted at self-hosted scale; a future retention/trimming story must decide the cap (and confirm it can't trim entries still pending in any group's PEL).
- **A stale extra chunk in one narrow crash window is accepted, not compensated:** if a crash lands between the upsert tx and the XACKs and a *later, differently-composed* batch regroups an unstamped message under a different first-id, a superseded chunk could linger. Only reachable through that window; overwritten/ignored at query time by similarity. Documented in `indexBatch.ts` / the story's Dev Notes — do not build compensation for it.

## Deferred from: code review of story-4.2 (2026-07-06)

- **TOCTOU in `markRead` → FK violation 500 instead of 404** [`packages/backend/src/application/services/readStatusService.ts:47`]: `findVisibleEmbeddingChannel` and the `INSERT INTO user_read_status` run as two separate statements; if the embedding is deleted in between, the FK violation surfaces as an opaque 500 rather than the uniform 404 that D5 intends. Unreachable today — nothing hard-deletes `embeddings` until Epic 6. Same family as the anchor-missing count/list asymmetry; revisit both when Epic 6 hard-delete lands (wrap the check+insert atomically or map FK violations → 404).

- **`total`/unread counts vs list anchor-JOIN asymmetry** [`packages/backend/src/infrastructure/documentRepository.drizzle.ts:72`, `readStatusRepository.drizzle.ts:102`]: `listDocuments` INNER-joins the anchor message (`message_ids[1]`) but `countDocuments`/`unreadCountByChannel`/`markAllInChannels`/`findVisibleEmbeddingChannel` do not, so an in-scope, non-D1-deleted fragment with a missing anchor is counted/marked but never listed. Divergencia solo materializable con hard-delete de embeddings (Epic 6); hoy los anchors siempre existen y el spec prescribe count sin JOINs. Revisar junto al TOCTOU de la misma familia — alinear las 4 consultas con el INNER JOIN del list, o adoptar LEFT-JOIN+degrade a la vez.

## Deferred from: code review of story-4.5 (2026-07-07)

- **`reuseExistingServer: !process.env.CI` + boot-only seeding** [`packages/web/playwright.config.ts:521,527`]: a stale, manually-left-running local `e2e:server` process (e.g. kept open in a separate terminal to watch logs) would answer the health check without re-seeding, so a fresh test run would see already-mutated data (the docs spec's mark-all-read test). Not a problem in the normal fresh-run flow (each `npm run test:e2e` invocation starts and tears down its own server, matching the story's "3 consecutive green runs" claim) — only a footgun for the "reusable harness" goal (AC8, Stories 5.3/5.4). Worth a README caution, not a behavior change.
- **`seed.ts` delete+insert sequence not transactional** [`packages/backend/src/e2e/seed.ts:322-375`]: a mid-sequence failure (e.g. a future migration changing `vector(1536)`) could leave partial `e2e-`-prefixed rows. Self-healing — the next boot's reset-then-seed cleans it up before the next run.
- **`tsconfig.json` drops `rootDir` and merges Node ambient types into the app tsconfig** [`packages/web/tsconfig.json`]: rather than giving the Playwright specs their own tsconfig, `tests/` was folded into the same config that type-checks `src/`. Verified empirically safe (`npm run typecheck -w @share2brain/web` passes clean, no DOM/Node type-declaration conflicts) — but still weakens the type-check boundary between Node-context tests and browser-context app code going forward; a dedicated `tests/tsconfig.json` would be cleaner.
- **`E2E_BACKEND_PORT` not validated as numeric** [`packages/backend/src/e2e/server.ts:413`]: a non-numeric override (e.g. `"abc"`) still throws inside `app.listen()`, caught by `main().catch` — fails loudly but without a clear custom message. (The empty-string case was fixed in the 2nd review pass by switching `??` to `||`; general numeric validation remains deferred.) Low-likelihood misconfiguration.
- **`session.ts` assumes the login redirect always has a `Location` header** [`packages/web/tests/helpers/session.ts:768`]: a missing header would throw a generic `new URL(undefined)` TypeError instead of a clear assertion failure. Only reachable via a backend bug elsewhere in the auth flow, not from this harness's own logic.

## Deferred from: code review of story-4.5 (2026-07-07, 2nd pass)

- **`reuseExistingServer: !process.env.CI` footgun also applies to the web `vite preview` server, not just the backend** [`packages/web/playwright.config.ts` webServer[1]]: a stale, manually-left-running `vite preview` process from an older build would be reused as-is, silently skipping `vite build` and testing outdated frontend code. Same class of issue as the already-deferred backend-reuse footgun, just on the other server; same mitigation (README caution, not a behavior change).
- **Mutating-test isolation depends on an unenforced accident** [`packages/web/playwright.config.ts`]: the docs spec's mark-all-read test running after every non-mutating assertion relies on Playwright's default alphabetical file discovery (`docs.spec.ts` < `search.spec.ts`) plus `workers: 1` — nothing pins this ordering explicitly. A new spec added for 5.3/5.4 could silently change file discovery order. Worth documenting as an explicit invariant in `tests/README.md` when the next spec is added, not a code change now.
- **`shutdown()`'s force-exit fallback exits `1` even for a merely-slow-but-otherwise-clean shutdown** [`packages/backend/src/e2e/server.ts`]: conflates "timed out" with "failed." Accepted as-is — Playwright doesn't inspect the webServer process's exit code during teardown, so this only affects log readability.

## Deferred from: code review of story-4.4 (2026-07-07)

- **Offset pagination skips/duplicates rows under `unreadOnly` after optimistic mark-read + "Cargar más"** [`packages/web/src/components/DocsView.tsx:81`]: with the "Sin leer" filter on, marking rows read optimistically shrinks the server-side `unreadOnly` result set, but the next `loadMore` still requests `offset = (page-1)*20` against the now-smaller set — an unread fragment that shifted below the offset is silently skipped (or duplicated in the reverse direction). Inherent to the offset pagination chosen in D1; low frequency (needs mark-read while filtered, then Cargar más). Correct fix is a cursor or a refetch-from-page-1 after read-state mutations under `unreadOnly` — a design change beyond this story's scope.

## Deferred from: Story 5.4 (Chat messages + streaming UI, 2026-07-08)

- **Execution-trace "loop de ejecución" panel (UX-DR20) — DEFERRED (D1):** the epic AC 5.4 and UX-DR20 describe an agent execution-trace panel with `tool_call` (`#F5A623`) and `observation` (`#3BA55D`) steps. The backend does **not** produce these: `SSEFrameSchema` (`packages/shared/src/schemas/sse.ts`) is a 4-variant union (`token`/`citation`/`done`/`error`), the StateGraph (`packages/backend/src/agent/graph.ts`) is `START → retrieve → reason → respond → END` with **no `tool_exec` node** (only a future-extension comment), and the prototype's trace steps are hardcoded `setTimeout` fakes. Building it for real is a future **backend + shared** story: LLM tool-use, tools defined in `Share2Brain.config.yml`, a conditional `tool_exec` loop in the StateGraph, and widening `SSEFrameSchema` with `tool_call`/`observation` frames (+ the frontend trace panel). Story 5.4 is frontend-only and deliberately did not touch that surface.

## Deferred from: code review of story-6.3 (2026-07-08)

- **Offline edit to the newest (anchor) message is never detected** [`packages/bot/src/sync/offlineSync.ts:68`]: the offline-sync backward walk seeds `before: lastSeen` (Discord's `before` is exclusive of the anchor id), so the newest persisted message — the `getChannelCursor` anchor — is never in the re-fetched set and its content is never diffed. If that message was edited on Discord during downtime and no newer message arrived to displace it from the anchor slot, the edit is permanently missed for a quiet channel (this boot and every subsequent boot). This is the deliberate design per reconciliation notes #1/#6 (anchor at `last_seen` to avoid re-reconciling the freshly-backfilled tail, and the exclusive boundary is what makes the delete-conservatism guard sound). Correct fix is a future full-reconcile / periodic-reconcile story, not a change to this bounded startup pass.

## Deferred from: code review of story-7.1 (2026-07-09)

- **`link` empty-or-URL refine is a weak URL validator** [`packages/shared/src/schemas/{search,documents,citation}.ts` + `config/index.ts` `base_url`]: the ratified D2 convention `v === '' || /^https?:\/\//.test(v)` is case-sensitive (rejects a valid `HTTP://…`) and prefix-only (accepts `https://` with no host, embedded whitespace/markup, or trailing garbage). Dormant in 7.1 (`link` is always `''`) but it is the shared contract Story 7.2 writes and 7.5 renders as an href. Compounding reliability concern: `packages/backend/src/infrastructure/ragRetriever.drizzle.ts` maps DB rows through `SearchFragmentSchema.parse` per-row with no try/catch, so a single malformed `link` would throw and abort the entire retrieval batch (chat 500). **Deferred by decision (2026-07-09):** robust URL validation belongs to Story 7.2, where URL extraction/normalization actually lives; the D2 refine stays aligned with the existing `base_url` convention and `link` is inert (`''`) throughout 7.1. When 7.2 populates real URLs, decide there whether to (a) normalize/lower-case the scheme + host-check at extraction time, and (b) wrap the per-row `SearchFragmentSchema.parse` so one bad row is skipped rather than failing the whole batch.

- **Schema/doc comments describe the post-7.2 `chunk_key`/`messageIds` state ahead of the code** [`packages/shared/src/db/schema.ts` + `docs/data-model.md`, `docs/context/TECHNICAL-DESIGN.md` ER diagram]: the `chunk_key` doc-comment now reads `"<messageId>:<urlIndex>"` (one row per URL) and `messageIds` is annotated as a length-1 anchor array, but the shipped worker code (`indexBatch.ts`) still writes grouped multi-message arrays with a chunk index (`${group.messageIds[0]}:${i}`). A maintainer trusting the "length-1 / urlIndex" invariant during the 7.1→7.2 window would be misled. This is sanctioned by AC-1 and the AD-13 note ("7.1 just documents it" — the forward-looking semantics are intentional), and is resolved when Story 7.2 rewrites the URL-extraction/chunking pipeline. Forward-documentation by design; no 7.1 code change warranted.

## Deferred from: code review of story-7.2 (2026-07-09)

- Response body always decoded as UTF-8 regardless of `Content-Type` charset (`packages/workers/src/enrichment/urlFetcher.ts:68`) — MEDIUM, quality-only. Non-UTF-8 pages (ISO-8859-1/Shift-JIS/UTF-16) produce mojibake in the hints fed to the LLM. Fix needs charset detection + `TextDecoder`.
- Truncation can split a surrogate pair / grapheme (`packages/workers/src/enrichment/htmlText.ts:42`, `enrich.ts:65`) — LOW, cosmetic. Same grapheme-safety class deferred in Story 5.2 (Intl.Segmenter).
- `extractMeta`/`extractTitle` regex run over full untruncated HTML (~2MB, `max_bytes`-bounded) (`packages/workers/src/enrichment/htmlText.ts:52`) — LOW. Mild backtracking risk on pathological input.

## Deferred from: code review of story-7.3 (2026-07-09)

- **`discord_messages` content UPDATE has no edit-timestamp ordering guard** [`packages/workers/src/sync/processUpdate.ts:150`]: the tx does `UPDATE discord_messages SET content, updated_at = ${timestamp}` with no `AND updated_at < ${timestamp}`. Out-of-order redelivery of two edits (E2 newer processed first, then E1 older replayed from the PEL / another consumer) overwrites content with the older text and rebuilds resource rows from stale content. Pre-existing since 6.2; the ratified model is last-write-by-arrival (AD-13). Fix, if ever promoted: guard the content/rows rebuild on a monotonic edit timestamp.
- **Concurrent Indexer `created` vs Sync `updated` race — slower writer's stale rows can win** [`packages/workers/src/sync/processUpdate.ts:89` (out-of-tx `oldRows` read) + `:159-181` (`onConflictDoUpdate`)]: a message created then quickly edited can have the Indexer still running its slow `created` fetch/LLM pipeline while Sync commits the edited rows; the Indexer then UPSERTs its pre-edit rows for the same `chunk_key` and last-write-wins picks the slower writer, silently losing the edit. `onConflictDoUpdate` prevents a crash but does not order writers. Inherent to the two-stream event model; the AD-13/D7 convergence contract is last-write-wins per `chunk_key`. The reuse optimization (stale `oldRows` snapshot read outside the tx) widens the window. Resolution belongs to a future cross-stream ordering / reconciliation story, not 7.3.
- **`newContent` length is unbounded before the regex scan** [`packages/workers/src/sync/events.ts:32`, consumed at `resourceRows.ts:69`]: the F3 relaxation dropped the blank/absent rejection but there is still no upper length bound; a crafted multi-MB `newContent` runs three global regexes + per-candidate punctuation stripping over the whole string before the `MAX_URLS_PER_MESSAGE` cap applies. CPU cost proportional to attacker-controlled content length, blocking the sync loop. Pre-existing pattern shared with `parseCreatedEvent` (Indexer); Discord caps message size in practice and the bot is the only producer. Fix, if promoted: a shared max-content-length guard on both parse paths.
- **`embedder.embedDocuments` is not passed the abort signal** [`packages/workers/src/sync/processUpdate.ts:123`]: `fetchUrl`/`enrich` receive `signal` but the embeddings HTTP call does not, so a shutdown during a hanging embedder is not honored until it completes/fails on its own — the graceful-shutdown deadline (7s in-process / 35s compose) can be exceeded. Pre-existing, shared with the Indexer (`indexBatch.ts` also omits it); the drain race is the safety net. Fix, if promoted: thread `signal` into the `Embedder` interface.

## Deferred from: code review of story-7.4 (2026-07-09)

- **RESOLVED by Story 7.5 (2026-07-09)** — **Citation/fragment `title: z.string()` accepts `''`** [`packages/shared/src/schemas/citation.ts:16`, `search.ts`/`documents.ts` fragment title]: the strict `link` refine (F1) enforces link quality, but `title` is plain `z.string()` per the ratified D2 convention (same as `channel`/`author`), so an empty title passes the contract, persists, round-trips, and renders as ` — description (link)` in `buildRAGContext` and (future) as an empty 7.5 sources-chip title — undercutting F3's stated "so the chip can show the resource title" intent. Only a *missing* title is rejected, not an empty one. Deferred: consistent with the descriptive-field convention; enrichment bounds title at generation (7.2). Fix, if 7.5 needs the guarantee: `title: z.string().min(1)` on Citation/fragment schemas. Fixed exactly as proposed: `title: z.string().min(1)` on `CitationSchema`/`SearchFragmentSchema`/`DocumentFragmentSchema`, reject-empty test cases added.
- **All-rows-malformed retrieval returns `[]` → chat answers a false "not enough information"** [`packages/backend/src/infrastructure/ragRetriever.drizzle.ts:57`]: F2's skip-and-warn drops every corrupt row and returns an empty batch; downstream `buildRAGContext([])` is indistinguishable from a genuine zero-result retrieval, so when relevant resources exist but are all unparseable the user gets a confident false negative, signalled only by per-row `logger.warn`s. Deferred: post-clean-slate this should not occur, and F2 correctly prioritizes availability. Fix, if promoted: a skipped-count return/metric so all-dropped is distinguishable from found-none (and optionally surfaces a degraded-retrieval signal).

## Deferred from: code review of story-8.1 (2026-07-10)

- **Read row has no non-visual accessible label** [`packages/web/src/components/DocsView.tsx:438`] — LOW, a11y enhancement. The read-state checkmark is a bare decorative `<CheckIcon>` with no text/`aria-label`; a screen-reader user hears "Nuevo" on unread rows and nothing distinguishing a read row. Not a regression (the pre-8.1 grey dot was equally decorative and unread *gained* announced "Nuevo" text) and no AC requires it. Fix, if promoted: a visually-hidden "Leído" or `aria-label` on the check span for read/unread symmetry.
- **Empty `description` renders a blank clamped cell with no placeholder** [`packages/web/src/components/DocsView.tsx:497`] — LOW, pre-existing. `packages/shared/src/schemas/documents.ts` types `description: z.string()` (empty string permitted, unlike `title`'s `.trim().min(1)`); Story 7.5 already rendered description, but Story 8.1 promoting it to its own labeled 6-col grid column makes an empty value more visually conspicuous. No test covers the `description===''` boundary for the cell. Fix, if promoted: an em-dash/placeholder fallback + a `''` test case.

## Deferred from: code review of story-9.1 (2026-07-10)

- **Stats aggregations have no upper time bound → future-dated rows are irreconcilable** [`packages/backend/src/infrastructure/statsRepository.drizzle.ts:74` (activity `AND e.created_at >= ${fromDate}`) + KPI/coverage queries which have no date bound] — LOW, robustness. A future `created_at` (clock skew between the ingesting container and the API, or a back/forward-dated seed) is counted in `resources`/`channels`/`authors`/coverage totals but its `day` bucket falls past `windowDays[13]`, so `countByDay.get(date)` never matches and it vanishes from the 14-day `activity` series — the chart and the KPI totals cannot be reconciled. Does not occur with `defaultNow()` inserts + NTP-synced clocks. Fix, if promoted: add a symmetric `created_at < ${upperBound}` (or `< fromDate + 14 days`) upper bound to the counts, or accept the one-sided window as documented.
- **Index-swap migration is not zero-downtime** [`packages/shared/src/db/migrations/0004_rapid_quicksilver.sql`] — LOW, operational. `DROP INDEX "idx_embeddings_channel"` has no `IF EXISTS` (the whole migration fails if the old index is already gone from a partial/re-run), and `CREATE INDEX … idx_embeddings_channel_created` has no `CONCURRENTLY` — it takes a write-blocking lock on `embeddings` for the build duration, and there is a window between the drop and the create where the hot vector-search RBAC filter (`channel_id = ANY(:ids)`) has no supporting index. Real availability blip on deploy for a table backing vector search. The file is drizzle-kit-generated and the story mandates never hand-editing it, so this belongs to deploy tooling / a migration-runner policy, not this change. Fix, if promoted: a zero-downtime migration path (create concurrently under a new name, then drop old) applied outside the standard generated migration, or a deploy-window policy. Cross-ref `operational-backlog.md`.

## Deferred from: code review of story-9.5 (2026-07-10)

- **SQL-vs-JS ordering-collation coupling in the `topUsers` contract** — `getTopUsers` emits `ORDER BY count DESC, d.author_id ASC` under the DB column collation; the `StatsResponseSchema.topUsers` `superRefine` independently re-checks the tie order with JS `curr.authorId <= prev.authorId` (UTF-16 code-unit compare). For pure-numeric Discord snowflakes the two orderings always agree, so there is no reachable 500 today. Latent risk only if `author_id` ever contained non-ASCII/case-varying characters. Inherent to the ratified P3 order-pinning pattern. Severity Low. Source: blind+edge.
- **Non-deterministic latest-name pick on `created_at` ties** (`statsRepository.drizzle.ts` getTopUsers) — `array_agg(NULLIF(d.author_name,'') ORDER BY d.created_at DESC)[1]` has no secondary tiebreak; if one author has two scoped non-deleted anchors with different non-blank names at the identical `created_at` (reachable via 6.3 offline reconciliation batch inserts or ms collisions), the displayed name is arbitrary among the tied rows. Cosmetic (picks between that same author's own names; no count/RBAC impact). Fix if revisited: add `, d.id DESC` as a secondary key in the array_agg ORDER BY. Severity Low. Source: edge.
- **Tier-3 `author_id` "never ''" asserted, not enforced** (`statsRepository.drizzle.ts` getTopUsers COALESCE; mirrored in `embeddingSearchRepository.drizzle.ts` / `documentRepository.drizzle.ts`) — `author_id` is `notNull` but has no CHECK forbidding an empty string; a row with `author_id = ''`, no captured `author_name`, and no matching `users` row would resolve `authorName` to `''` and 500 `GET /api/stats` via `StatsTopUserSchema.authorName.min(1)`. Not reachable from real Discord snowflakes. Same class as 9.4's deferred create-path `''` guard; not cleanly patchable (wrapping tier 3 in NULLIF just relocates the 500 to `authorId.min(1)` — real fix is a DB CHECK or accepting it as unreachable). Severity Low. Source: edge.

## Deferred from: code review re-run of story-9.5 (2026-07-10, 2nd pass)

- **COALESCE name tiers 2/3 assume non-empty strings enforced by no NULLIF/CHECK** (`statsRepository.drizzle.ts` getTopUsers) — the `NULLIF(..., '')` hardening is applied to tier-1 `author_name` but NOT to tier-2 `u.username` or tier-3 `d.author_id`. Both columns are `notNull` yet lack a CHECK against `''`, so a `users` row with `username = ''` (blank author_name, non-empty deny) or a `''` `author_id` would resolve `authorName` to `''` and 500 `GET /api/stats` via `StatsTopUserSchema.authorName.min(1)`. Theoretical only (OAuth usernames ≥2 chars; snowflakes are digits). Not cleanly patchable in isolation — hardening tier-2 alone leaves tier-3 exposed; the coherent fix is a DB CHECK constraint or accepting it as unreachable. Note: harmless on `/api/search` & `/api/documents` (their `authorName` is `z.string()` with no `.min(1)`, so a blank name is tolerated). Same class as 9.4's deferred create-path `''` guard and the first-pass tier-3 defer. Severity Low. Source: edge (both passes).
- **documentRepository `authorName` upgrade + search/docs tier-2/blank fallbacks are trusted-not-tested** — the identical `COALESCE(NULLIF(dm.author_name,''), u.username, dm.author_id)` + `LEFT JOIN users` landed in `documentRepository.listDocuments` with NO test assertion, and only tier-1 (`'Ada Lovelace'`) is asserted for `embeddingSearchRepository`; the `''`/username fallbacks are exercised only by `getTopUsers`'s stats integration test. Source-verified correct (identical to the tested search SQL; unique `idx_users_discord_id` rules out fan-out; `countDocuments` untouched) so residual risk is low — but the two repo projections are copies of tested code, and copies drift. AC5 only mandated the search-repo assertion, so closing this exceeds the ratified story scope. Quality follow-up: add a documents-repo `authorName` integration assert + a blank-name/username-fallback case to at least one repo suite. Severity Low (downgraded from Blind's Medium via source verification). Source: blind (2nd pass).

## Deferred from: code review of story-9.2 (2026-07-10)

- **No overflow/truncation on `authorName` / `channelName` spans** [`packages/web/src/components/StatsView.tsx:302` (channel `#name`), `:454` (top-user name)] — LOW, cosmetic. The name spans sit in a `justify-content:space-between` flex row; the parent right-block has `minWidth:0` but the child span has no `overflow/textOverflow/whiteSpace`, so a long unbroken display name or a raw-snowflake channel name (COALESCE fallback, ~18 digits) can wrap to a second line or push the count value out of place. Visual-only; belongs to Story 9.3's computed-style Playwright harness. Fix, if promoted: `overflow:hidden; textOverflow:ellipsis; whiteSpace:nowrap` on the two name spans — but the design spec mandates no ellipsis/tooltip in this screen, so the exact treatment is a design call, not spec-unambiguous.
- **`statsInitials` degrades on separator-only / emoji display names** [`packages/web/src/components/StatsView.tsx:51-55`] — LOW, cosmetic + rare inputs. A separator-only name (`"___"`) yields `parts=[]` and falls back to `authorName.slice(0,2)`="__"; a two-part emoji name (`"🎉_dev"`) hits `parts[0].charAt(0)` = a lone UTF-16 high surrogate, so `.toUpperCase()` is a no-op and the avatar renders the replacement glyph `�`. Inconsistent with the single-part branch, which uses `.slice(0,2)` and keeps both surrogate halves (single-part emoji looks fine). `authorName` is schema-`min(1)` so never empty; the realistic degradation (snowflake → 2 leading digits) is D4-ratified and correct. Fix, if promoted: `Array.from(str)` for surrogate-safe first-chars + a `?` fallback for all-separator names — but D4 pins the exact split/slice algorithm, so a change is a design deviation, not an unambiguous patch.
- **Unit-test coverage gaps vs enumerated boundaries** [`packages/web/src/components/StatsView.test.tsx`] — LOW, quality. Story 9.3's Playwright harness owns computed-visual coverage, and the state machine (loading/error/empty-scope/abort) IS unit-tested; but these enumerated boundaries have no assertion: single-part *alphabetic* initials (the `.slice(0,2)`-on-letters branch), emoji/unicode `authorName`, `coverage.readPct===100` (full donut), a non-empty channel row with `count===0` (0%-width fill in a rendered list), the real `ZodError` parse-failure path from `api/stats.ts:12` (the error test throws a generic `Error('boom')`, so the actual malformed-body edge is unexercised), and any nav→`StatsView` mount assertion (no `App.test.tsx` change shipped; the spec deferred nav coverage to App tests / 9.3, name-based). Fix, if promoted: add the parse-failure + `readPct=100` + alpha-initials unit cases and a nav-mount assertion; the rest are legitimately 9.3's.

## Deferred from: code review of story-9.3 (2026-07-10)

- **Activity chart has no positive-path (non-zero data) coverage** [`packages/web/tests/analytics.spec.ts:305-307` (member) + `:486-488` (empty)] — LOW, ratified coverage gap. Every activity-chart assertion in the harness binds to all-zero seed data (all 14 bars asserted computed `height 4px`); the only "today"-dated datum is the RBAC canary that must never surface. A regression where the chart ignored its `activity[]` data array and always emitted 4px stubs (plus the today-gradient cosmetic marker + `'0 recursos · últimos 14 días'` total) would pass green — the positive "per-day count → tall bar" direction is untested at the e2e level. Ratified out of scope by D5 ("activity stays all-zero — do NOT re-date the 5 existing embeddings; value-level activity math is 9.1 integration's job; the harness asserts the render"), because re-dating an embedding into the 14-day window would ripple into the Documentos ordering the 4.4/7.6/8.1 specs sit on. The shared `Math.round((count/max)*100)` bar-scaling logic (StatsView.tsx:244/297/420) IS exercised end-to-end via the channel fills (100%/67%) and top-user fills (100%/67%), so only the activity-chart's count→height% mapping is uncovered. Fix, if promoted: a dedicated seed identity with an in-window resource (isolated from the shared member scope so it can't perturb Documentos), or accept value-level activity coverage as 9.1 integration's remit. Source: blind (Medium, downgraded to Low ratified-scope via D5).
- **RBAC text canaries are narrower than the count-based canaries** [`packages/web/tests/analytics.spec.ts:449-450`] — LOW, mitigated. `getByText('#secreto')` / `getByText('Eve Intrusa')` `toHaveCount(0)` implement AC6 verbatim but would miss a hypothetical leak that rendered the raw channel name `secreto` (no `#`) or the `author_id` COALESCE fallback `e2e-author-eve` instead of the display name. Mitigated on two fronts: (1) the count/figure assertions (KPI resources 5, 2 `stats-channel-row`, 2 `stats-top-user-row`, authors 2) are the primary leak discriminators per the discrimination DoD and flip on ANY leak in any of the 5 blocks; (2) given the actual seed, a real leak renders as `#secreto` (StatsView always prefixes `#` on channel names — asserted for `#general`/`#random`) and `Eve Intrusa` (Eve carries a non-null `author_name`, so COALESCE tier-1 wins), so the text canaries DO catch the realistic leak. Fix, if promoted: add `getByText('secreto')` and `getByText('e2e-author-eve')` to the absence set for defence-in-depth. Source: blind.

## Deferred from: code review re-run of story-9.3 (2026-07-10, 2nd pass)

- **`page.route('**/api/stats', …)` glob would miss a future query-string variant** [`packages/web/tests/analytics.spec.ts:460`] — LOW, robustness. The AC7a error-state intercept globs `**/api/stats`, which Playwright anchors at the URL end: it matches `/api/stats` (the endpoint is param-free today — verified: `fetchStats` calls it with no query string, activity/queries windows are fixed server-side) but would silently NOT match `/api/stats?days=14` if a param were ever added, letting the real backend's valid response through and failing (or misattributing) the error-state assertion. Not a current defect. Fix, if promoted: `**/api/stats*`. Source: blind (2nd pass).

## Deferred from: code review of story-2.5 (2026-07-11)

- **No boot-time warning when the guest role maps to zero channels** (`packages/backend/src/main.ts`) — deny-by-default is per-spec (AC4); a startup warning is an ops-ergonomics enhancement, not a defect.
- **Public `POST /api/auth/guest` has no CSRF token (forced-guest-downgrade)** (`packages/backend/src/presentation/controllers/authController.ts:188`) — consistent with the existing unprotected `POST /api/auth/logout`; downgrade-only, demos-only. CSRF hardening across all auth POSTs is a separate concern.
- **Disabling `guest_access` does not revoke live guest sessions** (`packages/backend/src/presentation/controllers/authController.ts:181`) — sessions are TTL-bounded (≤120 min) and the story marks TTL auto-expiry acceptable demo behavior.

## Deferred from: code review of story-2.5 re-review#2 (2026-07-11)

- **`guestConversationIds` unbounded growth + concurrent lost-update** (`packages/backend/src/presentation/controllers/chatController.ts`) — the per-session guest conversation allowlist has no cap and its read-modify-write is not atomic; two concurrent new-conversation POSTs on the same guest session can last-write-wins and drop an id (that conversation becomes non-resumable). Deferred: TTL-bounded (≤120 min) + chat rate limiter; fails CLOSED (loses resumability of own conversation, never grants cross-guest access); express-session's end-of-response save self-heals the transient case. Demo-scoped, no security impact.
