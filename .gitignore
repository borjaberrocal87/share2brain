node_modules/
**/dist/
.env
.env.*
!.env.example
# Operator-specific behavior config; only the .example is tracked (AD-8).
Share2Brain.config.yml
# Operator-supplied TLS material (nginx mounts ./certs/{privkey,fullchain}.pem);
# a private key must never be committable.
certs/
*.pem
*.log
.DS_Store
# Local opencode session artifacts (plans, scratch)
.opencode/
*.tsbuildinfo
# Playwright E2E artifacts (Story 4.5)
test-results/
playwright-report/
